{
  "name": "circ-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run sync && bun run llm-mirror && astro dev",
    "build": "bun run sync && bun run llm-mirror && astro build",
    "preview": "astro preview",
    "sync": "bun run scripts/sync-reference.ts",
    "llm-mirror": "bun run scripts/build-llm-mirror.ts",
    "libcirc": "bun run scripts/build-libcirc.ts",
    "test": "bun test"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "circ-renderer": "github:jeffersonmourak/circ-renderer#6f31ea1"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
