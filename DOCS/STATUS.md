# Status — libcirc (compiler front end as a library)

Append-only log, one entry per shipped slice. Newest at the bottom. See `DOCS/PLANS_PROMPT.md` for the phase index and `DOCS/PLANS/PHASE_<N>_*.md` for each phase's spec.

## 2026-09-08 — Phase 0 — Slice 1: `Errors (n)` section in the AST dump

**What shipped:** `tests/helpers/ast_dump.zig::dumpFile` prints a trailing `Errors (n)` block, one `  Error "<message>" [f<id>:<sl>:<sc>-<el>:<ec>]` line per `ast.File.errors` entry with the raw span (zero-width marks stay `[f0:2:9-2:9]`; the `--analyze` widening is not applied). The 27 `expected-ast` goldens were regenerated; every diff is the single new last line `Errors (0)`.
**Files touched:** `tests/helpers/ast_dump.zig`, `tests/syntax/translate_test.zig`, `tests/fixtures/expected-ast/*.txt` (27), `DOCS/STATUS.md` (new).
**Tests:** added `errors: clean source has no marks` and `errors: truncated bus carries busvalue then busclose marks` (the latter pins the unwidened `2:9-2:9` spans on `input a\nand g(a=` without a trailing newline). `zig build test` green; `git diff --stat tests/fixtures/expected-ast` = 27 files, 27 insertions; `git diff … | grep '^+' | sort -u` = exactly `+Errors (0)`; nothing else under `tests/fixtures` moved; `expected-inspect/*` untouched (`lib/cli/inspect_dump.zig` is the CLI copy and does not gain the section).
**Next slice:** Slice 2 — the five label-recovery fixtures (`recovery_busvalue_eof` with no trailing newline, `recovery_busvalue_newline`, `recovery_busclose_midline`, `recovery_busclose_newline`, `recovery_empty_ports`) with goldens reviewed against the blocks in `PHASE_0`, plus the whitespace-only `InvalidProgram` test and the no-trailing-newline guard.
**Notes:** None.

## 2026-09-08 — Phase 0 — Slice 2: label-recovery fixtures

**What shipped:** Five `tests/fixtures/circuits/recovery_*.circ` sources with `expected-ast` goldens generated by the Go parser and checked byte for byte against the blocks in `PHASE_0` *Data & State*: `recovery_busvalue_eof` (16 B, no trailing newline; 2 marks, both `[f0:2:9-2:9]`), `recovery_busvalue_newline` (2 marks at `[f0:3:1-3:1]` — Spacing eats the newline before `PortRef`), `recovery_busclose_midline` (2 marks: zero-width `busclose` at `2:11`, then `RecoverLine` `[f0:2:11-2:15]`; `g` survives with `NamedRef a.out [f0:2:9-2:11]` absorbing the trailing space), `recovery_busclose_newline` (1 mark at `[f0:3:1-3:1]`; `NamedRef a.out [f0:2:9-3:1]` absorbs the newline and the following `output` still parses), `recovery_empty_ports` (4 marks: `busname` `[f0:2:10-2:11]`, `busassign` eats the entire next line `[f0:3:1-3:8]`, `busvalue`/`busclose` at `[f0:4:1-4:1]`; both declarations vanish).
**Files touched:** `tests/fixtures/circuits/recovery_{busvalue_eof,busvalue_newline,busclose_midline,busclose_newline,empty_ports}.circ`, `tests/fixtures/expected-ast/recovery_*.txt` (5), `tests/syntax/translate_test.zig`, `DOCS/STATUS.md`.
**Tests:** five fixture-table rows; added `edge: whitespace-only .circ is InvalidProgram` and `recovery: busvalue_eof fixture has no trailing newline` (guards the last byte `=`). `zig build test` green; `git status --short tests/fixtures` listed exactly the 10 new files.
**Next slice:** Slice 3 — the seven `RecoverLine`/quirk fixtures (`junk_line`, `keyword_prefix`, `connection_line`, `import_empty_path`, `width_overflow`, `multibyte_columns`, `trailing_ws_baseref`) and the corrected `#`-operator comment at the `subscript: name [i] with space` test.
**Notes:** None — every golden equalled its spec block on the first regeneration.

## 2026-09-08 — Phase 0 — Slice 3: RecoverLine and quirk fixtures + comment fix

**What shipped:** Seven more `recovery_*.circ` fixtures with Go-parser goldens, each byte-equal to its `PHASE_0` block: `recovery_junk_line` (one `RecoverLine` mark `[f0:2:1-2:13]`, all declarations kept), `recovery_keyword_prefix` (`andx g(...)` → `and` + instance `x`, then `RecoverLine` `[f0:2:6-2:17]`), `recovery_connection_line` (line 2 is a `Connection` dropped without a mark; line 3's inner `busclose` throw is discarded by backtracking, leaving exactly one `RecoverLine` mark `[f0:3:1-3:11]`), `recovery_import_empty_path` (`import x ""` is **kept** with `path="`, `Errors (0)`), `recovery_width_overflow` (every declaration with a 300 literal drops silently; `Input b width=[255]` survives, `Errors (0)`), `recovery_multibyte_columns` (byte columns: the mark ends at `2:15` on a 12-code-point line), `recovery_trailing_ws_baseref` (`NamedRef` spans absorb trailing whitespace; `a [2]` is `Indexed bit=2 [f0:3:16-3:19]`). The `subscript: name [i] with space` test is renamed and its comment now states the real cause (Spacing consumed by `BaseRef`'s failed `('.' Identifier)?`, not an ignored `#`).
**Files touched:** `tests/fixtures/circuits/recovery_{junk_line,keyword_prefix,connection_line,import_empty_path,width_overflow,multibyte_columns,trailing_ws_baseref}.circ`, `tests/fixtures/expected-ast/recovery_*.txt` (7), `tests/syntax/translate_test.zig`, `DOCS/STATUS.md`.
**Tests:** seven fixture-table rows (table now 39 entries). `zig build test` green; `git status --short tests/fixtures` listed exactly the 14 new files.
**Next slice:** Slice 4 — `tests/analyze/analyze_golden_test.zig` + `expected-analyze/{clean,truncated,junk_line,empty}.json`, the `analyze_golden_tests` block in `build.zig`, the `tests/README.md` line, and the fork's `diff-circ.sh` cross-check over this checkout.
**Notes:** None — all seven goldens equalled their spec blocks on the first regeneration.

## 2026-09-08 — Phase 0 — Slice 4: `--analyze` JSON goldens + build wiring + fork cross-check

**What shipped:** `tests/analyze/analyze_golden_test.zig` renders `analyze.renderJson(analyze.analyze(...))` for the four LSP overlays (`clean`, `truncated`, `junk_line`, `empty`) and compares byte for byte against `tests/fixtures/expected-analyze/*.json` (1320 / 959 / 1387 / 101 bytes, each equal to its `PHASE_0` block). `build.zig` gains the `analyze_golden_tests` artifact (named, so it shows as `run test analyze_golden_tests` in `--summary all`) wired into `test_step` after `analyze_tests`. `tests/README.md` lists `expected-analyze/`.
**Files touched:** `tests/analyze/analyze_golden_test.zig` (new), `tests/fixtures/expected-analyze/{clean,truncated,junk_line,empty}.json` (new), `build.zig`, `tests/README.md`, `DOCS/STATUS.md`.
**Tests:** `analyze json goldens for the lsp overlays`. `zig build test` green with the new step in the run. Fork cross-check: `CIRC_ROOT=<this checkout> langlang/go/zig/scripts/diff-circ.sh -v` → `TestGenZigDifferentialCorpus` 156/156 PASS (144 pre-existing + 12 `recovery_*`), `TestGenZigTablesMatchVendoredGo` PASS, 0 mismatches.
**Next slice:** Phase 0 is complete after the `zig build test-all` gate. Next is Phase 1 (`DOCS/PLANS/PHASE_1_vendor_zig_parser.md`): vendor the langlang-generated `lib/parser/parser.zig` and retire the Go/cgo archive; acceptance is `zig build test` green with `git diff --stat tests/fixtures` empty.
**Notes:** `build.zig` on this branch is not `zig fmt`-clean (pre-existing trailing whitespace around `:703-800`); the slice-4 hunk was applied without reformatting the file so the diff stays 22 insertions. For Phase 1: `grep -c 'linkParserArchive(b, ' build.zig` = 28 and `grep -c 'tests/helpers/golden.zig' build.zig` = 14 now.

## 2026-09-08 — Phase 1 — Slice 1: cut over to the generated Zig parser

**What shipped:** `lib/parser/parser.zig` vendored from `langlang v0.0.13-zig.2` (`-grammar lib/grammar/proto-circ.peg -disable-capture-spaces -output-language zig`; 1,639 lines / 75,915 B, sha256 prefix `61daddd228c39aeb`, header `Runtime: langlang_runtime.zig abi=1 sha256=007bc786…`; `zig fmt --check` clean). `build.zig`: `createParserModule(b, target, optimize)` replaces `linkParserArchive`; `parser:gen` re-pointed at the Zig backend behind a `langlang -version` gate (`expect_stdout_match = "v0.0.13-zig.2"`, `has_side_effects`); `parser_mod` imported into `translate_mod` as `parser`; a named `parser_tests` artifact (the generated `langlang tables` test) in `test`; the `parser:archive` step, the `GOOS/GOARCH/CGO_ENABLED/CC` block, 28 archive-link calls, 26 `.linkLibC()` calls and all 28 `addIncludePath` pairs deleted (`linkLibC()` survives only on `emit_behavior_tests` and `project_behavior_tests` for `wasm_run.zig:12` `std.c.getpid`). `lib/syntax/translate.zig`: `TranslationContext.tree: *const parser.runtime.Tree`, the six accessors are one-liners over the tree, `Range` is the runtime's `usize` range (the `@intCast`s went), `NodeType_*` → `.string/.sequence/.node/.err`, `pub const circ_label_messages` + `labelMessage` loop + comptime guards (every bound label exists in the grammar; every recovery label has a message; `entry_rule == .Program`; no LR rules), `translateTree` uses `tree.root() orelse error.ParsingFailed`, `parseSourceCapturing` runs a per-call `parser.Parser` bound to the message table, and the dead `translate()` is gone.
**Files touched:** `lib/parser/parser.zig` (new, generated), `build.zig`, `lib/syntax/translate.zig`, `DOCS/STATUS.md`. `lib/parser/{go.mod,parser.go,shim/}`, `lib/syntax/CParser.zig` and `lib/syntax/nodes/declaration.zig` stay on disk, unreferenced, until slice 2.
**Tests:** `zig build test` and `zig build test-all` green with `git status --porcelain tests/fixtures` empty (all 39 `expected-ast`, the 4 `expected-analyze`, and every ir/zig/wasm/diagnostics/preview/truth-table golden byte-identical through the Zig parser). Comptime guard proof: misspelling `busclose` fails the build with `translate.zig:641:13: error: translate.zig binds a label the grammar does not define: buscloze`. Go-free proof: with the Go directory pruned from `PATH`, `rm -rf .zig-cache zig-out && zig build circ-compile --verbose` succeeds and spawns no `go build`. `zig build parser:gen && git diff --exit-code lib/parser/parser.zig` → 0 (idempotent); with a fake `langlang` printing `Version: v0.0.12` the gate fails with `expected to find in stdout` and leaves `parser.zig` untouched. Fork differential: `TestGenZigDifferentialCorpus` 156/156 PASS, `TestGenZigTablesMatchVendoredGo` PASS (last run possible against `parser.go`; slice 2 deletes it).
**Next slice:** Slice 2 — `git rm` the Go sources and `CParser.zig`/`nodes/declaration.zig`, drop `.gitignore:6-7`, rewrite the prerequisite in `CLAUDE.md`/`README.md`/`DOCS/index.md`, add the `Remove Go from PATH` step to `pr-tests.yml`, and replay the `cli-tag.yml` matrix locally.
**Notes:** Sizes on this machine (aarch64-macos): `circ-compile` Debug 4,880,400 B (the `main` checkout's Debug binary with the Go archive is 6,605,840 B), ReleaseFast 1,572,128 B. The `2,186,432 B` figure in `PHASE_1` was not reproducible here. `build.zig` still carries the pre-existing trailing whitespace at `:703-800`; the edits were applied without reformatting so the diff is reviewable.

## 2026-09-08 — Phase 1 — Slice 2: delete the Go sources, drop the prerequisite from docs, prove it in CI

**What shipped:** `git rm` of `lib/parser/{go.mod,parser.go,shim/shim.go}`, `lib/syntax/CParser.zig` and the dead `lib/syntax/nodes/declaration.zig`; the untracked `parser.a`/`parser.h` removed and their `.gitignore` lines dropped (`git ls-files lib/parser` = `parser.zig`; `lib/syntax` = `ast.zig span.zig translate.zig`). `CLAUDE.md` (project sentence, toolchain list without Go, `parser:archive` row gone, `parser:gen` row names the version gate, pipeline diagram says "vendored Zig"), `README.md` (Go bullet gone, langlang bullet re-pointed at `parser.zig`), `DOCS/index.md` (`parser/` and `syntax/` rows). `.github/workflows/pr-tests.yml` gains a `Remove Go from PATH` step between the Node install and `Run all tests` that prunes every `PATH` directory holding an executable `go`, exports the pruned `PATH`, and fails the job if `go` still resolves.
**Files touched:** `.gitignore`, `.github/workflows/pr-tests.yml`, `CLAUDE.md`, `README.md`, `DOCS/index.md`, `DOCS/STATUS.md`; deleted `lib/parser/go.mod`, `lib/parser/parser.go`, `lib/parser/shim/shim.go`, `lib/syntax/CParser.zig`, `lib/syntax/nodes/declaration.zig`.
**Tests:** `grep -rn "CGo\|parser\.a\|CParser\|go build\|parser:archive\|Go 1\.21" CLAUDE.md README.md build.zig .gitignore DOCS/index.md .github` is empty. Local replay of the CI recipe with the Go directory pruned from `PATH`: `! command -v go && zig build test-all --summary all` green (62 test artifacts run), `git status --porcelain tests/fixtures` empty. Tag-matrix cross-builds from this one Zig install, ReleaseFast: `x86_64-linux-gnu` 6,877,232 B, `aarch64-macos` 1,572,128 B, `x86_64-windows-gnu` 2,024,448 B (`.exe`, plus a 4,415,488 B `.pdb`). `cli-tag.yml` needed no edit.
**Next slice:** Phase 1 complete. Next is Phase 2 (`DOCS/PLANS/PHASE_2_host_free_front_end_and_libcirc.md`): host-free front end, `lib/libcirc.zig` driver, native static library. Its spec assumes PR #79 (native memories) is merged; if it is not yet, the phase runs against this branch's pre-#79 tree and the rebase notes in `PHASE_1` Warning 3 apply later.
**Notes:** The fork's `diff-circ.sh` can no longer run `TestGenZigTablesMatchVendoredGo` against this checkout (it reads `lib/parser/parser.go`); reruns need `git show 4ce729b:lib/parser/parser.go > <tmp>/parser.go` and `CIRC_PARSER_GO` pointed at it — a langlang-side env override is the tidy fix. The first real run of the `Remove Go from PATH` step happens on the PR.

## 2026-09-08 — Phase 2 — Slice 1: overlay-first file loader

**What shipped:** `lib/resolver/file_loader.zig` consults the overlay by a normalised key (`normalizeKey` = `std.fs.path.resolvePosix` over the path) *before* `realpath`; `resolveImportPath` gains an `overlay` parameter, joins with `resolvePosix`, and returns the key when the overlay holds it, so an overlay-only sibling import resolves with no disk access; disk branches are compiled out on `.freestanding` (`has_disk`) and fall through to `error.FileNotFound` → `E009`. `scan_imports.zig` passes its overlay through. `analyze.zig` exposes `convertDiagnostic`/`convertDiagnostics` (split out of `appendDiag`) and `writeJsonString` as `pub` for the libcirc response codec.
**Files touched:** `lib/resolver/file_loader.zig`, `lib/resolver/scan_imports.zig`, `lib/analyze/analyze.zig`, `tests/resolver/file_loader_test.zig`, `tests/resolver/scan_imports_test.zig`, `build.zig` (the scan_imports test module imports `file_loader`), `DOCS/STATUS.md`.
**Tests:** added `resolveImportPath overlay-only sibling returns the key`, `resolveImportPath normalises dot segments`, `resolveImportPath disk sibling still realpaths`, `loadFileWithOverlay hits the overlay for a never-saved key`, `scan imports resolves an overlay-only sibling without E009` (7 files, `import_table[0].target_file == 1`), `analyze: overlay-only sibling import resolves without E009`. `zig build test` green; no file under `tests/fixtures` moved (the Phase 0 `expected-analyze` goldens and every diagnostics snapshot are byte-identical).
**Next slice:** Slice 2 — freestanding guards (`color.zig` `.auto`, `builder.zig` `drive_ns`, `resolve_bodies.zig` `std.debug.print`, `log.zig` arena) and `memory.reset()`.
**Notes:** This branch is pre-PR #79, so the memories-specific parts of `PHASE_2` (`Preload`, `writeImageError`, `stateful_ram` refusal, `rom` preloads in requests) do not exist here and are deferred to the post-#79 rebase; every other row is executed as written. One observable change on native: `files[].path` for an overlay-keyed file is now the normalised key, not its realpath (documented in slice 8).

## 2026-09-08 — Phase 2 — Slice 2: freestanding guards and `memory.reset`

**What shipped:** `lib/preview/render/color.zig` — `.auto` is `false` on `.freestanding` before the `isatty` call; `lib/truth_table/builder.zig` — `drive_ns` is measured only when `has_clock` (0 on freestanding; the `Table.drive_ns` doc says so); `lib/resolver/resolve_bodies.zig` — the specialization-failure `std.debug.print` is now `std.log.scoped(.resolver).warn` (no stderr on freestanding; the CLI's log filter formats it as `warning(resolver): …`); `lib/log.zig` — the wasm log arena is backed by `std.heap.page_allocator` instead of `memory.allocator` so it cannot dangle across a reset; `lib/memory.zig` — `pub fn reset()` (`arena.reset(.free_all)`, counters untouched) and the test-only `arenaCapacityForTest()`.
**Files touched:** `lib/preview/render/color.zig`, `lib/truth_table/builder.zig`, `lib/resolver/resolve_bodies.zig`, `lib/log.zig`, `lib/memory.zig`, `lib/circuit.zig` (test only), `DOCS/STATUS.md`.
**Tests:** added `memory.reset frees the arena and the allocator stays usable` (`lib/circuit.zig`: build, deinit, reset → capacity 0, rebuild and propagate `1 & 1 == 1`) and `truth_table_build_records_drive_ns_on_native` (`builder.zig`). `zig build test` green, no fixture moved; `zig build bench` → `golden matches` (56 fixtures, counters byte-identical).
**Next slice:** Slice 3 — `build/frontend_modules.zig` sharing the front-end module graph between the CLI and the future `libcirc` module; `build_info` materialised once with `grammar_sha256` and `parser_runtime_sha256`.
**Notes:** The `engine_session`/`sim/loop.zig` image-error move in the spec is memories-only (`writeImageError`, `Preload`, `ImageError` do not exist pre-#79) and is deferred to the post-#79 rebase.

## 2026-09-08 — Phase 2 — Slice 3: shared front-end module graph

**What shipped:** `build/frontend_modules.zig` — `create(b, opts) Modules` builds every front-end module once per target (parser → translate → IR → validator passes → resolver → analyze → topology → engine/session/truth table → preview layout + render → `build_info` → the `libcirc` root) with the exact `addImport` edges `build.zig` used to draw inline; `createParserModule` moved here. `build.zig` now aliases each front-end name to `fe.*` (the wasm runtime block and `build_info` were hoisted above it; the CLI, `libcirc`, and every per-module test artifact share the same module objects, so no type-identity split can arise), imports `build_info` as a module instead of `addOptions`, adds `libcirc` to the CLI, drops the never-referenced `preview_dump` CLI import, and runs the new `libcirc_tests` artifact. `build_info` gains `grammar_sha256` (sha256 of `lib/grammar/proto-circ.peg`) and `parser_runtime_sha256` (the 64 hex chars after `sha256=` in the generated parser's header). `lib/libcirc.zig` exists with the re-exports, `Version`, `version()` and `writeVersionJson`.
**Files touched:** `build/frontend_modules.zig` (new), `lib/libcirc.zig` (new), `build.zig` (−275 lines net), `cmd/circ-compile/main.zig` (one import), `DOCS/STATUS.md`.
**Tests:** `version reports the topology versions and a 64-hex grammar sha` (`lib/libcirc.zig`). `zig build test-all` green (415 tests), no fixture moved, `circ-compile --version` prints `v0.0.2 (rev:…)` exactly as before.
**Next slice:** Slice 4 — `lib/libcirc/frontend.zig`, `modes.zig`, the response half of `json.zig`, the four entry points, and `tests/libcirc/driver_test.zig`.
**Notes:** The per-module tests were migrated onto `fe.*` in this slice rather than Phase 5's optional slice: aliasing the existing names was smaller and safer than keeping two parallel graphs. `format.VERSION`/`FULL_VERSION` are `0x02` on this pre-#79 branch (the spec's `3` arrives with the rebase). The generated-parser header is read as a 4 KiB prefix (a whole-file `readFileAlloc` with that cap fails with `FileTooBig`).

## 2026-09-08 — Phase 2 — Slice 4: the Zig driver

**What shipped:** `lib/libcirc/frontend.zig` (`run(alloc, root, files, route, failure)`: overlay → root load → parse → resolve → project pipeline or single-module validation; `Stage.cliLabel` holds every label the CLI prints; `Front` carries the AST, IR module, optional project, file paths, merged diagnostics, `early_stop`, counts), `lib/libcirc/modes.zig` (`buildTopology`, `compile`, `buildLayout`, `countExpandDisplayFallbacks`, `renderPreview`, `Refusal`/`truthTablePreflight`, `buildTruthTable`, `renderTruthTable`, `analyze`/`renderAnalysis`), `lib/libcirc/json.zig` (response half: `writeError`, `writeDiagnostics` in the analyze-api shape, `writeSyntaxFailure`), and in `lib/libcirc.zig` the `Request`/`Options`/`Status`/`Outcome` types plus `analyze`, `compile`, `preview`, `truthTable` (which resets the engine arena after rendering).
**Files touched:** `lib/libcirc.zig`, `lib/libcirc/{frontend,modes,json}.zig` (new), `tests/libcirc/driver_test.zig` (new), `build.zig` (driver test artifact; the CLI's `golden` module is now a named const shared with it), `DOCS/STATUS.md`.
**Tests:** 12 driver tests — version/grammar sha; preview == in-process CLI for 24 render cases (+ the 5 pinned goldens); truth table == CLI and golden for 21 fixtures, plus csv/json/hex/decimal goldens; cap refusal text on both sides; compile == the CLI-written artifact byte for byte for `inverter`, `and_gate`, `chain`, `slice_basic`, `stress_grid_10x10`, `projects/full_adder`; the compiled inverter instantiated in Node (`0 1`); analyze == `renderJson` on disk and for an overlay-only sibling; status 1 shape for `E001`, `W001` with `warnings_as_errors`; empty root → syntax diagnostic; missing root → status 2; consecutive truth tables with the engine arena at 0 between them; root + overlay-only sibling compiles byte-identical to the disk project. `zig build test` green, no fixture moved.
**Next slice:** Slice 5 — `cmd/circ-compile/main.zig` dispatches compile/preview/truth-table/analyze through `libcirc.frontend`/`libcirc.modes`, with every message, exit code and golden unchanged.
**Notes:** Pre-existing CLI behaviour worth knowing: `compile` (and `--emit-zig`) take the import-free fast path, which does not resolve implicit builtin macros, so `circ-compile full_adder_from_builtins.circ -o x.wasm` exits 1 with `E001: undeclared name 'xor'` while `--preview`/`--truth-table` on the same file succeed. libcirc mirrors that route table exactly; the compile comparison uses builtin-free fixtures. Memories-only parts of the spec (`preloads`, `stateful_ram`, `bad_preload`) are deferred to the post-#79 rebase.

## 2026-09-08 — Phase 2 — Slice 5: CLI on libcirc

**What shipped:** `cmd/circ-compile/main.zig` runs the front end through `libcirc.frontend.run` (route: `.single_module` for `--inspect`, `.project_if_imports` for compile/`--emit-zig`, `.project` for preview/truth-table/sim — exactly the old `needs_project_resolution` gate) and dispatches compile, preview, truth-table and the sim topology build through `libcirc.modes`; `--analyze` calls `libcirc.modes.analyze`/`renderAnalysis`. Every stage label comes from `Stage.cliLabel`, printed as `"{s}: {s}\n"`; the only literal labels left in `main.zig` are the ones that were never stage labels (`emission failed`, `failed writing …`, `strict scan failed`, `sim:`, `analyze:`). The 18 front-end imports collapsed to `libcirc` (+ `diagnostics`, `emit_main`, `inspect_dump`, `sim_loop`, `cli_args`, `build_info`, `golden`), and `build.zig` no longer wires them into the CLI module.
**Files touched:** `cmd/circ-compile/main.zig`, `build.zig`, `DOCS/STATUS.md`.
**Tests:** added `compile fast path takes no project route` (`stress_grid_10x10.circ` through `.project_if_imports` → `project == null`, one file path). `zig build test-all` green: the 20 CLI integration tests, the 5 e2e CLI tests, all `main.zig` tests, every preview/truth-table/expected-wasm golden byte-identical; `git status --porcelain tests/fixtures` empty.
**Next slice:** Slice 6 — `json.parseRequest`/`describe` (the request half), `lib/libcirc/c_api.zig` with the ten `circ_*` exports, `tests/libcirc/c_api_test.zig`.
**Notes:** The CLI's pre-read of the input file stays as the exit-2 contract (`input file not found` / `failed reading input file`); the front end then re-reads the root through the loader, as the import scan always did. The `--expand-display` warning loop stays in `main.zig` (it is the CLI's stderr, not the library's).

## 2026-09-08 — Phase 2 — Slice 6: JSON codec and the C ABI

**What shipped:** `lib/libcirc/json.zig` request half — `parseRequest` over `std.json.Value` (`root` required; `files` must be an object of absolute keys → strings; `options` keys `expand_macros`, `expand_display`, `warnings_as_errors`, `color`, `format`, `value_format`, `truth_table_cap` 1..24; any unknown or mistyped key is `BadOption`), `ParseError`, `describe`. `lib/libcirc/c_api.zig` — the ten `pub export fn circ_*` (`alloc`, `free`, `version`, `analyze`, `compile`, `preview`, `truth_table`, `result_ptr`, `result_len`, `reset`) with `callconv(.c)`, a page-allocator-owned result buffer, a per-call arena reset with `.retain_capacity`, a `std_options.logFn` that drops everything, and status 4 whenever `OutOfMemory` escapes.
**Files touched:** `lib/libcirc/json.zig`, `lib/libcirc/c_api.zig` (new), `tests/libcirc/c_api_test.zig` (new), `build.zig` (`libcirc_c_api_tests`), `DOCS/STATUS.md`.
**Tests:** `json: parses the full request`, `json: each error has a message` (13 cases), `json: writeError escapes`; 8 `c_api` tests — version JSON, `circ_analyze` == `renderJson`, five bad-request bodies (invalid JSON, missing root, relative key, `color: auto`, unreadable root), `circ_compile` yields `\0asm` and equals the Zig API, E001 → status 1, preview/truth table == Zig API and cap refusal → status 3, result buffer replaced by the next call, alloc/free + `circ_reset` (engine arena 0). `zig build test` green.
**Next slice:** Slice 7 — `zig build libcirc` (static `libcirc.a` + installed `include/libcirc.h`), `examples/c/analyze.c`, `zig build libcirc-smoke`.
**Notes:** `preloads`/`PreloadNotHex`/`hexToBytes` wait for the post-#79 rebase (no memories on this branch). The exports are `pub export` so the test module can call them by name; the symbol names are unaffected.

## 2026-09-08 — Phase 2 — Slice 7: `zig build libcirc` and the C smoke

**What shipped:** `zig build libcirc` builds `zig-out/lib/libcirc.a` (static, root `lib/libcirc/c_api.zig`, `.name = "circ"`) and installs `include/libcirc.h` (hand-written: the six `CIRC_STATUS_*` codes and the ten prototypes with `uint8_t*`/`size_t`, i.e. pointer-sized rather than the plan's literal `u32`). `zig build libcirc-smoke` compiles `examples/c/analyze.c` with Zig's C compiler, links the archive, and runs it with `expectExitCode(0)`: it prints the version JSON, then the analysis JSON and the preview for an in-memory inverter, and calls `circ_reset`.
**Files touched:** `include/libcirc.h` (new), `examples/c/analyze.c` (new), `build.zig`, `DOCS/STATUS.md`.
**Tests:** `zig build libcirc` → `nm` lists exactly `_circ_alloc _circ_analyze _circ_compile _circ_free _circ_preview _circ_reset _circ_result_len _circ_result_ptr _circ_truth_table _circ_version`; `zig build libcirc-smoke` exits 0. Archive sizes on aarch64-macos: Debug 6,918,072 B, ReleaseSmall 558,384 B.
**Next slice:** Slice 8 — `DOCS/analyze-api.md` overlay semantics and the invalid-input section, `DOCS/libcirc-api.md`, CLAUDE.md/README build rows.
**Notes:** The smoke's request literal is the one `DOCS/libcirc-api.md` documents; slice 8 adds a `c_api` test over the same bytes so the doc and the code cannot drift.

## 2026-09-08 — Phase 2 — Slice 8: docs

**What shipped:** `DOCS/libcirc-api.md` (new: the two surfaces, the request and its options table, the status → body matrix per export, `circ_version`, the C ABI with its memory model and logging, build/link commands); `DOCS/analyze-api.md` — the `overlays` bullet now states the normalised-key, overlay-before-disk, sibling-resolution and `files[].path` semantics, and "Behavior on invalid input" describes declaration-level recovery with one `syntax` diagnostic per mark (the stale truncation/"Stage 7" text is gone); `CLAUDE.md` build table gains `zig build libcirc` and `zig build libcirc-smoke`, and "Files worth knowing about" gains `lib/libcirc.zig` + `build/frontend_modules.zig`; `README.md` mentions the library; `DOCS/index.md` lists the new reference.
**Files touched:** `DOCS/libcirc-api.md` (new), `DOCS/analyze-api.md`, `DOCS/index.md`, `CLAUDE.md`, `README.md`, `tests/libcirc/c_api_test.zig`, `DOCS/STATUS.md`.
**Tests:** added `c_api: the documented request literal compiles` (the literal from `DOCS/libcirc-api.md`/`examples/c/analyze.c` → `circ_compile` status 0 with `\0asm`, `circ_preview` renders the `NOT` box). Phase close: `zig build test-all` green (437 tests), `zig build bench` → `golden matches`, `zig build libcirc-smoke` exit 0, `git status --porcelain tests/fixtures` empty.
**Next slice:** Phase 2 complete. Next is Phase 3 (`DOCS/PLANS/PHASE_3_libcirc_wasm.md`): `zig build libcirc-wasm` — the `wasm32-freestanding` module over `c_api.zig`, the Node loader harness, and the size budget.
**Notes:** Deferred to the post-#79 rebase (memories do not exist on this branch): `Options.preloads` + `PreloadNotHex`/`hexToBytes`, the `stateful_ram`/`bad_preload` refusals, the `engine_session` image-error move, and `topology_version`/`full_version` reading `3`. One trap for test authors: inside a Zig `\\` multiline literal the JSON escape must be a single `\n`; doubling it feeds the parser a literal backslash-n and the "circuit" silently collapses to its first declaration.

## 2026-09-08 — Phase 3 — Slice 1: `-Dwasm-optimize` decoupled from `-Doptimize`

**What shipped:** `build.zig` gains `-Dwasm-optimize=<mode>` (default `ReleaseSmall`) and a derived `wasm_strip = wasm_optimize != .Debug`, applied to all eight wasm-target modules of the runtime graph (`runtime_module`, the `compiled.zig` dummy, `circuit`/`memory`/`transport`/`log`/`interpreter`/`format` twins) and to a new `runtime_embed_wasm_mod` (the same `@embedFile` over the just-built runtime, for the wasm target). `tests/e2e/topology_protocol_test.zig` now imports the native `format` module (`fe.format`) instead of the stripped wasm-target unit. CLAUDE.md and README carry one sentence each.
**Files touched:** `build.zig`, `CLAUDE.md`, `README.md`, `DOCS/STATUS.md`.
**Tests:** `zig build` → `circ-runtime.wasm` raw 19,628 B / gzip 8,715 B; `zig build -Doptimize=ReleaseFast` leaves it at 19,628 B (decoupled); `zig build -Dwasm-optimize=Debug` → 933,116 B. `zig build test-all` green on the ReleaseSmall runtime (both `section_writer` fixture suites and the topology host-protocol test pass), no fixture moved.
**Next slice:** Slice 2 — `lib/libcirc/wasm_root.zig`, the wasm-target front-end graph, and the `libcirc-wasm` step producing `zig-out/lib/libcirc.wasm` with exactly the ten `circ_*` exports plus `memory`.
**Notes:** Pre-#79 numbers; the memories branch adds eight exports to the runtime.

## 2026-09-08 — Phase 3 — Slice 2: `libcirc.wasm` freestanding module

**What shipped:** `lib/libcirc/wasm_root.zig` (silent `std_options.logFn`, `.log_level = .err`, address-of anchors for the ten exports); `build/frontend_modules.zig` takes `strip` and returns `.c_api` (the module rooted at `lib/libcirc/c_api.zig`, now shared by `libcirc.a` and the wasm build); `build.zig` creates a second front-end graph for `(wasm_target, wasm_optimize, wasm_strip)` with `runtime_embed_wasm_mod`, and `zig build libcirc-wasm` installs `zig-out/lib/libcirc.wasm` (`entry = .disabled`, `export_symbol_names` = the ten names, no `rdynamic`). `lib/log.zig` asks `debugEnabled()` before formatting, so a host with logging off never allocates for engine log lines. `c_api.zig` needed no change: `std.heap.page_allocator` is already `WasmAllocator` on wasm32.
**Files touched:** `lib/libcirc/wasm_root.zig` (new), `build/frontend_modules.zig`, `build.zig`, `lib/log.zig`, `DOCS/STATUS.md`.
**Tests:** `zig build libcirc-wasm` compiles for `wasm32-freestanding` on the first attempt (the Phase 2 guards were complete). Node: `WebAssembly.validate` true; exports exactly `circ_alloc circ_analyze circ_compile circ_free circ_preview circ_reset circ_result_len circ_result_ptr circ_truth_table circ_version memory`; imports exactly `env.debugEnabled env.onDebugLog`; `circ_version` → status 0 with `topology_version 2 / full_version 2` (pre-#79) and both sha256 fields; an in-memory inverter compiles (19,836 B artifact), previews, tabulates and analyzes with status 0; `circ_reset` → 0. Size: raw 383,300 B, gzip 144,939 B (against the 600 KB / 200 KB budget; hard cap 3 MB). `zig build test-all` green, `zig build bench` → `golden matches`, no fixture moved.
**Next slice:** Slice 3 — `tests/harness/libcirc_loader.js` and `tests/e2e/libcirc_wasm_test.zig` (surface, version, compile + vector drive, byte-equality with native, status 1/2/3).
**Notes:** `build_info.createModule()` is called once per graph (native, wasm); each graph is its own compilation, so the "file exists in modules" clash cannot arise.

## 2026-09-08 — Phase 3 — Slice 3: Node harness and behavioural proof

**What shipped:** `tests/harness/libcirc_loader.js` (instantiates `libcirc.wasm` with the two `env` stubs and hands the script a `circ` helper: `exports`, `imports`, `memoryBytes`, `gzipSize`, `version`, `reset`, `result` (re-reads `memory.buffer` on every copy), `call(op, request)` (alloc → copy → call → free → copy out), `emit` for the `RESULT <name> <status> <base64>` line protocol; scripts run as an async function so they can instantiate the artifacts they compile). `tests/e2e/libcirc_wasm_test.zig` with six tests; `build.zig` embeds the built module (`libcirc_wasm_embed`) into `libcirc_wasm_tests`, on `test`. `c_api.zig`: `circ_alloc(0)` returns a static in-bounds byte and `circ_free(_, 0)` is a no-op, so an empty request is a status-2 answer rather than an out-of-bounds copy.
**Files touched:** `tests/harness/libcirc_loader.js` (new), `tests/e2e/libcirc_wasm_test.zig` (new), `lib/libcirc/c_api.zig`, `build.zig`, `DOCS/STATUS.md`.
**Tests:** export surface exactly the ten names + `memory`, imports exactly `env.debugEnabled env.onDebugLog`; `circ_version` bytes equal native, 7 keys, versions from `format`/`full_format`, `version` from the `VERSION` file; compile + vector drive for `inverter`, `slice_basic`, `chain` and the two-file `projects/full_adder` (artifact bytes equal native `circ_compile`, both custom sections present, every `expected-wasm` vector line reproduced); analyze / preview / truth table (markdown + json) byte-equal to native for `chain`, `builtin_xor`, `alu_4bit_multibit`, `four_bit_adder`, `slice_basic`, plus the `chain` preview and `builtin_xor` table goldens; E004 → status 1 with the analyze-shaped body equal to native and `circ_analyze` agreeing; `{` / empty / root-not-in-files → 2, 17-bit truth table → 3 naming `17`, its compile → 0. `zig build test` green; `SKIP_WASM_E2E=1 zig build test` prints `SKIPPED (SKIP_WASM_E2E=1 set in environment)` for each of the 6 tests and passes.
**Next slice:** Slice 4 — the 50× compile / 50× truth-table memory bound and the size gate.
**Notes:** `four_bit_adder` is not a compile+drive case: `circ_compile` mirrors the CLI's import-free fast path, which does not resolve implicit builtins (`xor`), so it returns status 1 (`E001`) exactly like `circ-compile four_bit_adder.circ -o x.wasm`. Preview/truth-table/analyze on the same file succeed (project route). Worth a decision in Phase 5: the playground will hit this on its first `xor`.

## 2026-09-08 — Phase 3 — Slice 4: memory bound and size gate

**What shipped:** Two more tests in `tests/e2e/libcirc_wasm_test.zig`: 50 repeated `compile` (chain), `preview` (four_bit_adder, project route with macro expansion) and `truth_table` (builtin_xor, json) calls with linear memory sampled after the 5th and the 50th; and the size gate (hard fail over 3 MiB, raw + gzip logged).
**Files touched:** `tests/e2e/libcirc_wasm_test.zig`, `DOCS/STATUS.md`.
**Tests:** `MEM compile after5=1572864 after50=1572864`, `MEM preview after5=3538944 after50=3538944`, `MEM truth_table after5=3604480 after50=3604480` — no growth at all after warm-up (asserted `after50 <= after5 + 65536` and `<= 64 MiB`); `SIZE raw=383324 gzip=146549` (budget 600 KB / 200 KB: met; hard cap 3 MiB). `zig build test` green.
**Next slice:** Slice 5 — the Linux Docker bisect (`run.sh`/`Dockerfile`/`container-e2e.sh` pass-through, `drive-libcirc.mjs`, the `WASM_OPTIMIZE × NODE_IMAGE` matrix), time-boxed.
**Notes:** None.

## 2026-09-08 — Phase 3 — Slice 5: Linux Docker bisect (harness landed, matrix not run)

**What shipped:** `tests/e2e/linux-docker/run.sh` builds `libcirc.wasm` alongside the Linux CLI with `-Dwasm-optimize=${WASM_OPTIMIZE:-ReleaseSmall}`, stages it plus `drive-libcirc.mjs`, and passes `NODE_IMAGE` (default `node:22-bookworm-slim`) as a `--build-arg`; `Dockerfile` takes `ARG NODE_IMAGE` and copies the two new files; `container-e2e.sh` gains the `--- libcirc ---` block (`node /test/drive-libcirc.mjs /test/libcirc.wasm /test/inverter.circ`). `drive-libcirc.mjs` compiles the inverter in memory through `circ_compile` and drives the artifact like `drive-inverter.mjs` (`a=0 -> NOT a = 1`, `a=1 -> NOT a = 0`). The comments at `topology_protocol_test.zig` and `pr-tests.yml` point at the harness.
**Files touched:** `tests/e2e/linux-docker/{run.sh,Dockerfile,container-e2e.sh}`, `tests/e2e/linux-docker/drive-libcirc.mjs` (new), `tests/e2e/topology_protocol_test.zig` (comment), `.github/workflows/pr-tests.yml` (comment), `DOCS/STATUS.md`.
**Tests:** `node tests/e2e/linux-docker/drive-libcirc.mjs zig-out/lib/libcirc.wasm tests/fixtures/circuits/inverter.circ` on the host prints the two lines and exits 0. **The matrix did not run:** `docker build` fails at `FROM node:22-bookworm-slim` with `DeadlineExceeded` loading the image metadata, `docker pull` times out in the credential helper, and no Node image is cached locally — Docker Hub is unreachable from this machine right now. The `ReleaseSmall × node:22` cell (and the other five) remain to be run; `zig build e2e-linux-docker` is the command, with `WASM_OPTIMIZE=Debug|ReleaseSafe|ReleaseSmall` and `NODE_IMAGE=node:22-bookworm-slim|node:24-bookworm-slim`.
**Next slice:** Slice 6 — `DOCS/libcirc-api.md` wasm sections (exports, host imports, memory contract, Node/worker loading, version handshake, size budget), CLAUDE.md/README `zig build libcirc-wasm` rows, and the doc-example test.
**Notes:** `SKIP_WASM_E2E` stays in `pr-tests.yml`. sha256 of the macOS-built default `circ-runtime.wasm` for a later cross-host comparison: recorded by `shasum -a 256 zig-out/lib/circ-runtime.wasm` at the time of the next matrix run (the artifact is rebuilt per `zig build`, so the value is only meaningful next to that run).

## 2026-09-08 — Phase 3 — Slice 6: `DOCS/libcirc-api.md` completed

**What shipped:** The wasm sections of `DOCS/libcirc-api.md` — building (`zig build libcirc-wasm`, `-Dwasm-optimize`), the export table with wasm32 signatures, the two `env` host imports and the minimal import object, the memory contract (buffer detaches on grow, flat after warm-up), the Node example (marked `// libcirc-api.md: Node example`, executed by the test suite), the Worker note, the version handshake, how to measure size and the current numbers, and what is not exported. CLAUDE.md's build table gains `zig build libcirc-wasm`; README mentions the wasm build.
**Files touched:** `DOCS/libcirc-api.md`, `CLAUDE.md`, `README.md`, `tests/e2e/libcirc_wasm_test.zig`, `DOCS/STATUS.md`.
**Tests:** added `libcirc.wasm: doc example loads and compiles the inverter` (extracts the fenced block after the marker and runs it through the loader; expects `PASS`). `zig build test` green (9 wasm e2e tests).
**Next slice:** Phase 3 complete after the close-out gate below. Next is Phase 4 (`DOCS/PLANS/PHASE_4_renderer_sync_and_playground.md`).
**Notes:** The doc example names its helper `api` because the harness already binds `circ`; the protocol is otherwise the harness's verbatim.

## 2026-09-08 — Phase 4 — Slice 4: site pin bump + example regeneration

**What shipped:** `site/package.json`/`bun.lock` pin `circ-renderer` to `ae8658b` (upstream `main`, the v02 decoder — already on GitHub, no push needed); all 11 `site/public/wasm/*.wasm` regenerated by `scripts/compile-content.ts` with this branch's `circ-compile` (ReleaseSmall runtime): CIRF byte 4 is `0x02` in every file and sizes dropped from 920,917–925,159 B to 19,838–24,204 B.
**Files touched:** `site/package.json`, `site/bun.lock`, `site/public/wasm/*.wasm` (11), `DOCS/STATUS.md`.
**Tests:** `bun --bun run build` green (9 pages, `dist/wasm` holds the 11 artifacts). Preview check: every regenerated preview differs from the committed `examples.ts`/`tour.ts`/`index.astro` text **only by trailing spaces per line** (the compiler right-pads rows to the canvas width; the committed previews were pasted trimmed), so the content files stay untouched and the playground compares previews modulo trailing whitespace.
**Next slice:** Slice 5 — `scripts/build-libcirc.ts`, committed `public/wasm/libcirc.wasm` + `libcirc.manifest.json`, `bun test`.
**Notes:** (1) The renderer v03 slices (Phase 4 slices 1–3) are deferred to the post-#79 rebase — there is no v03 on this branch; the renderer host API (`setInputSignal`/`onPinToggle`) is handled in slice 8. (2) On this machine `node` is an x64 build under Rosetta (`v24.20.0`), so `bun run build` fails in rollup's native loader looking for `@rollup/rollup-darwin-x64`; `bun --bun run build` (bun as the JS runtime, what CI's `oven-sh/setup-bun` effectively does) works. (3) `compile-content.ts` also writes `tour-1..7.wasm`; no page references them, so they are not committed.

## 2026-09-08 — Phase 4 — Slice 5: `build-libcirc.ts`, committed `libcirc.wasm` + manifest, first `bun test`

**What shipped:** `site/scripts/build-libcirc.ts` (`bun run libcirc`): runs `zig build libcirc-wasm`, copies the module to `public/wasm/libcirc.wasm`, instantiates it and reads `circ_version()` for the manifest's identity fields, asserts `grammar_sha256` against `lib/grammar/proto-circ.peg`, measures raw + gzip, writes `libcirc.manifest.json`, exits 1 over the 600 KB / 200 KB budget; never part of `bun run build`. `src/scripts/libcirc-abi.ts` (the alloc/copy/call/read protocol as `callOp`/`callVersion`/`readResult`/`instantiateLibcirc`, shared by the worker and the tests) and `src/utils/split-files.ts` (`splitFiles` with `startLine`, `rootOf`, `requestFor` building the `/playground/<name>` request) landed early because the tests need them. `package.json` gains the `libcirc` and `test` scripts. **Library change:** a recovered syntax error (a dropped declaration) now makes `compile`/`preview`/`truthTable` return status 1 with the `syntax` diagnostics appended after the validator's, in `--analyze`'s shape and widening; the CLI still compiles such a file (pre-existing behaviour, unchanged).
**Files touched:** `site/scripts/build-libcirc.ts`, `site/src/scripts/libcirc-abi.ts`, `site/src/utils/split-files.ts`, `site/test/libcirc.test.ts`, `site/public/wasm/libcirc.wasm`, `site/public/wasm/libcirc.manifest.json`, `site/package.json` (all new but the last), `lib/libcirc/json.zig`, `lib/libcirc.zig`, `tests/libcirc/driver_test.zig`, `DOCS/STATUS.md`.
**Tests:** `bun run libcirc` → `libcirc.wasm: 383450 B raw, 146597 B gzip (budget 614400/204800)`; manifest `version 0.0.2`, `full_version 2`, `grammar_sha256` = `shasum -a 256 lib/grammar/proto-circ.peg`. `bun test` → 4 pass (version == manifest; inverter compiles with both custom sections and drives `1→0`, `0→1` under the runtime; tour step 6's two files compile with `root.circ` as root and the artifact names `ha1`/`ha2`/`half_adder`; the broken source is status 1 with the `syntax` mark at `3:1-3:2`). `SKIP_LIBCIRC_TEST=1 bun test` → 4 skipped. `zig build test` green with the new driver test `a recovered syntax error is status 1 with a syntax diagnostic`.
**Next slice:** Slice 6 — `src/workers/libcirc.worker.ts`, `src/scripts/libcirc-client.ts`, `src/utils/columns.ts`, the `compile-content.ts` refactor onto `split-files.ts`, and the remaining `bun test` cases (preview equality, truth table, memory bound).
**Notes:** The manifest's `revision` is the compiler HEAD at build time, so it names the commit *before* the one that commits the manifest (`a1d9532` here); that is inherent to committing a build product and is how the examples' `.wasm` files already behave. `WebAssembly.instantiate(module, imports)` returns an `Instance` directly (not `{instance}`) — the first test draft tripped on that.

## 2026-09-08 — Phase 4 — Slice 6: worker, client, split-files, columns

**What shipped:** `site/src/workers/libcirc.worker.ts` (owns the instance; `init` → `circ_version`, `analyze`/`compile`/`preview`/`truth_table` through the shared `callOp`, `circ_reset` after every truth table, artifact bytes transferred, `error` extracted for status ≥ 2, thrown errors as status −1); `site/src/scripts/libcirc-client.ts` (`LibcircClient`: lazy module-worker spawn via `new URL('../workers/libcirc.worker.ts', import.meta.url)`, promise per id, `init`/`call`/`reset`/`dispose`); `site/src/utils/columns.ts` (`byteColToUtf16`, `caretOffset`); `scripts/compile-content.ts` now imports `splitFiles`/`rootOf` from `src/utils/split-files.ts`.
**Files touched:** `site/src/workers/libcirc.worker.ts`, `site/src/scripts/libcirc-client.ts`, `site/src/utils/columns.ts`, `site/test/split-files.test.ts`, `site/test/columns.test.ts` (new); `site/scripts/compile-content.ts`, `site/test/libcirc.test.ts`, `DOCS/STATUS.md`.
**Tests:** `bun test` → 14 pass: the three `splitFiles` cases (single → `main.circ` at line 0; tour step 6 → `half_adder.circ`@1 + `root.circ`@9 with the request keys under `/playground/`; a marker after blank lines stays a comment), the three column cases, and four more module tests (preview equals every example's `preview` field modulo trailing padding; half-adder JSON table with `[1,1] → [0,1]`; 13-bit truth table refused with status 3 under `truth_table_cap: 12`; 50 repeated compiles keep `memory.buffer` ≤ 2× the first). Re-running `compile-content.ts` after the refactor changes no tracked artifact. `bun --bun run build` green.
**Next slice:** Slice 7 — `/playground` with the editor, Diagnostics, Preview and Truth table tabs, the version handshake, Nav link, `llms.txt` line.
**Notes:** The option key for the cap is `truth_table_cap` (Phase 2's `libcirc-api.md`), not the plan's `max_input_bits`.

## 2026-09-08 — Phase 4 — Slice 7: `/playground` with Diagnostics, Preview and Truth table

**What shipped:** `site/src/pages/playground.astro` (`mdTwin={null}`, manifest identity in the intro) and `site/src/components/Playground.astro`: example picker (10 examples + 7 tour steps, sources inlined as JSON), `<textarea>` editor (Tab → two spaces via `setRangeText`), 250 ms debounce with generation gating, `analyze` → `compile` (compile skipped on error diagnostics; a status-1 compile re-renders its diagnostics), Diagnostics rows `line:col CODE message` mapped through the split file's `startLine` and `byteColToUtf16` with a click that moves the caret, Preview (`color: never`) and Truth table (`format: json`, `truth_table_cap: 12`, a message instead of a table above 12 root input bits computed from `analyze.symbols`, `?` for undefined cells) rendered on their tab's activation and on every fire while active, the version handshake (`data-supported-versions` from `src/utils/renderer-versions.ts`, banner + Simulate disabled on mismatch), a status line, and an `onArtifact` hook (FNV-1a hash-gated) for slice 8. Nothing loads until the reader focuses the editor or picks an example. Nav gains Playground; `llms.txt` gains the Playground line; `global.css` gains the `.pg*` block.
**Files touched:** `site/src/pages/playground.astro`, `site/src/components/Playground.astro`, `site/src/utils/renderer-versions.ts`, `site/test/renderer-pin.test.ts` (new); `site/src/components/Nav.astro`, `site/scripts/build-llm-mirror.ts`, `site/src/styles/global.css`, `DOCS/STATUS.md`.
**Tests:** `bun test` → 16 pass (adds `the supported list names the manifest version` and `the pinned renderer decodes what libcirc.wasm emits`: a libcirc-compiled inverter's `.full` section decodes through the pinned `circ-renderer` into InputPin/NotGate/OutputPin). `bun --bun run build` green: `dist/playground/index.html`, `dist/_astro/libcirc.worker-*.js`, `dist/llms.txt` lists the Playground, the nav links to it.
**Next slice:** Slice 8 — the Simulate tab.
**Notes:** The pinned renderer (`ae8658b`) does not export a supported-versions list, so `renderer-versions.ts` hard-codes `[1, 2]` and the new test ties it to the committed module. The browser-side manual checklist (caret placement, debounce feel, the banner) could not be exercised in this session — no browser; everything above is proven by the build and `bun test` only.

## 2026-09-08 — Phase 4 — Slice 8: Simulate tab + renderer host API

**What shipped:** The Simulate tab in `site/src/components/Playground.astro`: lazy `import('circ-renderer')` + `import('../utils/circ-theme.mjs')` on first use, `renderCircuit({ bytes, cell: 14, padding: 16, interactive: true, theme: pickTheme(), onPinToggle })` into the reused `.lc-mount` styling, a rebuild only when the artifact hash changes (the `onArtifact` hook fires only then), pins kept **by root input-pin name** and replayed after every rebuild — through `view.setInputSignal` when the renderer has it, else through `runtime.setPinSignal` + `run()` + `refreshState()` with a canvas click listener that re-reads pin signals to keep the map current — plus the `data-theme` `MutationObserver` and `onAssetsReady` rebuild paths. In `circ-renderer` (separate repo), local branch **`host-pin-api`, commit `74f595d`** off `main`: `CircCanvas.setInputSignal(id, signal)`, `RenderOptions.onPinToggle`, `SUPPORTED_TOPOLOGY_VERSIONS = [0x01, 0x02]` exported and used by the version check, README rows, package `2.0.0-alpha.2`, a new topology test. `bun test` 9/9 and `bun run typecheck` clean there.
**Files touched:** `site/src/components/Playground.astro`, `DOCS/STATUS.md`; in circ-renderer: `src/render/canvas.ts`, `src/wasm/topology.ts`, `test/topology.test.ts`, `README.md`, `package.json`.
**Tests:** `bun --bun run build` green (`dist/_astro` carries the renderer chunk, the theme module, the worker and the page script); `bun test` 16 pass; `zig build test` green (no Zig change). **Manual checklist (a)–(i) not run** — no browser in this session. What is proven: the code paths above compile and bundle; the by-name pin replay and hash gating are exercised only by reading.
**Next slice:** Phase 4 complete except the browser checklist. Next is Phase 5 (`DOCS/PLANS/PHASE_5_decisions_and_docs.md`).
**Notes:** (1) The renderer branch is **not pushed** and the site still pins `ae8658b`; once `host-pin-api` is pushed (needs approval), `cd site && bun add 'circ-renderer@github:jeffersonmourak/circ-renderer#<sha>'` switches the playground onto `setInputSignal` automatically (feature-detected) and `renderer-versions.ts` can import the exported list. (2) Renderer v03 (rom/ram geometry, `portByteOfName`, memory exports) stays deferred to the post-#79 rebase.

## 2026-09-08 — Phase 5 — Slice 1: grammar header comment

**What shipped:** The 8-line `//` header at the top of `lib/grammar/proto-circ.peg` naming the fork, the `parser:gen` command, `lib/parser/parser.zig` and the fork's `go/zig/langlang_runtime.zig`; `Program` now sits on line 10. Because `grammar_sha256` hashes the raw grammar bytes, `site/public/wasm/libcirc.wasm` and `libcirc.manifest.json` were refreshed together (`bun run libcirc`).
**Files touched:** `lib/grammar/proto-circ.peg`, `site/public/wasm/libcirc.wasm`, `site/public/wasm/libcirc.manifest.json`, `DOCS/STATUS.md`.
**Tests:** `zig build parser:gen && git diff --stat lib/parser/parser.zig` → empty (comments never reach the bytecode; `langlang -version` = `v0.0.13-zig.2`); `zig build test` green, no fixture moved; the manifest's `grammar_sha256` equals `shasum -a 256 lib/grammar/proto-circ.peg`; `bun test` in `site/` 16 pass against the refreshed module.
**Next slice:** Slice 2 — decision records (`tooling.md` rewrite, new `libcirc.md`, cross-links).
**Notes:** The refresh is ~383 KB of binary churn for a comment; the alternative (folding it into the next tagged release) would have left the manifest check failing until then, so the refresh landed now.

## 2026-09-08 — Phase 5 — Slice 2: decision records

**What shipped:** `DOCS/decisions/tooling.md` rewritten around the Zig parser — five entries: "Parser generated straight to Zig by the langlang fork" (decision 1, with the History paragraph that is the only surviving mention of the retired c-archive bridge), "The fork's tag is the pin; the generated header is the skew signal", "Generated parser is vendored in the repo", "The contract was pinned before the switch" (decision 2), "Go retired in one cut-over" (decision 9). New `DOCS/decisions/libcirc.md` with the preamble and seven entries for decisions 3–8 and 10. Cross-links: `compiler-pipeline.md` (the pipeline sentence names `lib/parser/parser.zig`; the runtime entry gains the `-Dwasm-optimize` sentence), `runtime-api.md` (the `circ_*` exports belong to `libcirc.wasm`, never to an artifact), `cli.md` (the four modes are the library's entry points; three stay CLI-only), `index.md` (tooling bullets, a `libcirc.md` block, the wasm-optimize bullet).
**Files touched:** `DOCS/decisions/tooling.md`, `DOCS/decisions/libcirc.md` (new), `DOCS/decisions/compiler-pipeline.md`, `DOCS/decisions/runtime-api.md`, `DOCS/decisions/cli.md`, `DOCS/decisions/index.md`, `DOCS/STATUS.md`.
**Tests:** `grep -c "^### "` → `libcirc.md` 7, `tooling.md` 5; the stale-string sweep over `DOCS/decisions` returns only `tooling.md` lines inside the first entry (History and the cut-over decision); each of the four cross-linked files names `libcirc.md`.
**Next slice:** Slice 3 — CLAUDE.md and README.
**Notes:** The "renderer follows the compiler" entry is written for the topology-version step in general and names the v03 sync as the next instance, since that sync is deferred to the post-#79 rebase on this branch.

## 2026-09-08 — Phase 5 — Slice 3: CLAUDE.md and README

**What shipped:** `CLAUDE.md`: the project sentence names the generated parser and both library builds; the toolchain list names the harness that needs Node and the exact `langlang -version` line; the `parser:gen` row says what upstream fails with; the pipeline box reads "langlang-generated Zig parser (lib/parser/parser.zig, vendored; generated from lib/grammar/proto-circ.peg)"; invariant 6 describes the arena over `page_allocator` and `memory.reset()`; "Files worth knowing about" gains `lib/parser/parser.zig`, `wasm_root.zig`, and `DOCS/libcirc-api.md`. `README.md`: the opening paragraph mentions the library; Node joins the prerequisites (tests only); a "Library" section under Usage; the Documentation list gains `DOCS/libcirc-api.md`.
**Files touched:** `CLAUDE.md`, `README.md`, `DOCS/STATUS.md`.
**Tests:** `rg -n "CGo|parser\.a\b|CParser|go build|Go 1\.21|parser:archive|parser\.go|wasm_allocator|GeneralPurposeAllocator" CLAUDE.md README.md` is empty; `grep -n "zig build libcirc" CLAUDE.md README.md` finds both build commands in both; the CLAUDE.md build table has one `libcirc` row, one `libcirc-wasm` row and no `parser:archive` row.
**Next slice:** Slice 4 — `DOCS/index.md`, `DOCS/architecture.md`, `DOCS/circuit-format.md` and the site mirror.
**Notes:** None.

## 2026-09-08 — Phase 5 — Slice 4: DOCS index, architecture, grammar reference + site mirror

**What shipped:** `DOCS/index.md` (layout lines for `memory.zig`, `parser/parser.zig`, `syntax/`, `libcirc.zig`/`libcirc/`, a `build/` block and a `site/` line; Build section gains `zig build libcirc` / `libcirc-wasm`); `DOCS/architecture.md` (front-end box rewritten at the same 64-column width, `### Memory` on the arena + `memory.reset()`, Layer 3 bullets for the grammar, `lib/parser/parser.zig` and `lib/syntax/`, a new Layer 6 for the library, Build targets rows for `libcirc`/`libcirc-wasm` and the `-Dwasm-optimize` note); `DOCS/circuit-format.md` (Grammar sentence, node-type table as `parser.runtime.NodeType` values, the dead `declaration.zig` sentence deleted); `site/src/pages/reference/circuit-format.md` regenerated by `bun run scripts/sync-reference.ts` (the other four mirrors unchanged).
**Files touched:** `DOCS/index.md`, `DOCS/architecture.md`, `DOCS/circuit-format.md`, `site/src/pages/reference/circuit-format.md`, `DOCS/STATUS.md`.
**Tests:** the tree-wide sweep `rg -n "CGo|parser\.a\b|CParser|go build"` (archives, plans, STATUS, plan prompt excluded) returns only `DOCS/decisions/tooling.md` lines; the `--no-ignore` sweep over `site/src/pages/reference/` returns nothing; `rg -n "wasm_allocator|GeneralPurposeAllocator"` over `DOCS/index.md`/`DOCS/architecture.md` is empty; the architecture box at lines 11–16 measures `[64, 64, 64, 64, 64, 64]`; `git status --short site/src/pages/reference/` listed only `circuit-format.md`; `DOCS/index.md` and `README.md` both point at `libcirc-api.md`.
**Next slice:** Slice 5 is already done — Phase 2 slice 3 aliased every native module in `build.zig` to `build/frontend_modules.zig` handles (the per-module tests included), which is exactly the migration this slice describes. Slice 6 — close-out.
**Notes:** `rg` run from a shell whose stdin is a pipe waits on that pipe when it decides to read stdin; the sweeps are run with `< /dev/null` and an explicit path.

## 2026-09-08 — Phase 5 — Slice 6: close-out (initiative complete on the pre-#79 tree)

**What shipped:** The libcirc initiative is complete on this branch: the parser is `lib/parser/parser.zig` generated by the langlang fork (`go/v0.0.13-zig.2`), Go and the c-archive are gone, `zig build libcirc` / `zig build libcirc-wasm` share one ten-export ABI, the site's `/playground` runs `libcirc.wasm` in a Web Worker, and the decision records (`tooling.md`, `libcirc.md`), CLAUDE.md, README, and DOCS agree with the code. Slice 5 (native test wiring through `build/frontend_modules.zig`) was done in Phase 2 slice 3.
**Files touched:** `DOCS/STATUS.md`
**Tests:** `zig build test-all` green (no fixture moved); `zig build bench` → `golden matches`; with every `PATH` entry holding an executable `go` pruned (`command -v go` fails), `zig build -Doptimize=ReleaseFast -Dtarget=<t> circ-compile` links for `x86_64-linux-gnu` (6,138,128 B), `aarch64-macos` (847,056 B), `x86_64-windows-gnu` (1,307,136 B); `zig build libcirc` and `zig build libcirc-wasm` build (`libcirc.wasm` 383,450 B; `circ-runtime.wasm` sha256 prefix `786acf6e53c2ed4e`); the tree-wide sweep returns only the two `DOCS/decisions/tooling.md` lines and the `--no-ignore` mirror sweep returns nothing; `bun test` 16 pass and `bun --bun run build` green in `site/`; `git log --oneline` carries all 30 commits this log lists, `git status --porcelain` is clean.
**Next slice:** none on this tree. Archive with `DOCS/prompts/ARCHIVE.md` as `plan-libcirc` when asked — **after** the items below, which need PR #79 first.
**Notes:** Papercuts and deferred work the archive should carry: (1) everything memories-specific waits for the rebase onto merged PR #79 — `Options.preloads`/`hexToBytes`, the `stateful_ram`/`bad_preload` truth-table refusals, the `engine_session` image-error move, `topology_version`/`full_version` 3, the renderer v03 sync (rom/ram geometry, `portByteOfName`, memory exports; Phase 4 slices 1–2), and the `rom_basic`/`ram_basic` goldens gaining `Errors (0)`; the plan-file collision (`DOCS/PLANS*`, `DOCS/STATUS.md`) is resolved by archiving the memories plan on `main` first, then rebasing. (2) The circ-renderer branch `host-pin-api` (`74f595d`: `setInputSignal`, `onPinToggle`, `SUPPORTED_TOPOLOGY_VERSIONS`) is local and unpushed; once pushed and pinned (`bun add circ-renderer@…#<sha>`), the playground picks the setter up by feature detection. (3) The Linux Node `init()` hang is still open; the Docker matrix harness landed but did not run (registry unreachable). (4) `parser:gen`'s version gate is a substring match on one literal (`v0.0.13-zig.2`) that a tag bump changes together with `CLAUDE.md`, `README.md`, and the `.peg` header. (5) Two `.linkLibC()` calls remain for `tests/helpers/wasm_run.zig`'s `std.c.getpid`. (6) The parser quirks pinned in Phase 0 (keyword-prefix split, trailing-whitespace `BaseRef`, `import x ""` kept as path `"`, width literals > 255 dropped, `output o()` swallowing the next line) are a later grammar initiative. (7) `compile`/`--emit-zig` take the import-free fast path and do not resolve implicit builtins, so a single file using `xor` compiles only through preview/truth-table/analyze routes; the playground will surface this — a decision for the next initiative. (8) The library refuses a recovered syntax error (status 1) while the CLI still compiles the truncated file. (9) Deferred UI: CodeMirror, draft persistence/share links, `circ_inspect`, memory image loading. (10) The browser-side manual checklist of Phase 4 slice 8 was not run in this session.

## 2026-09-09 — Merge — native memories (PR #79, `2e15e97`) folded in; branch now stacks on `memories`

**What shipped:** `origin/memories` merged into `libcirc` so PR #80 can be based on `memories` (stacked PR). Conflicts resolved in nine files: `CLAUDE.md` (memories' project sentence + libcirc's parser/library parenthetical; invariant 6 keeps memories' arena wording + the `memory.reset()` clause), `README.md`, `DOCS/decisions/runtime-api.md` (both the memory-export omissions sentence and the `circ_*`-belong-to-`libcirc.wasm` sentence, then the Native memories section), `build.zig` (front-end aliases stay on `fe.*`; `memory_validation` joins `build/frontend_modules.zig`'s pass list with its `port_validation`/`validator_run`/`sub_circuit_validation` edges; `sim_golden_tests` loses the four archive/libc/include lines memories added against the old build), `cmd/circ-compile/main.zig` (libcirc's `run()` with memories' `resolvePreloads`, the `--sim` preloads argument, the `firstRamName` preflight, `.preloads` on the truth-table build and the `truth-table: preload failed` mapping through `Failure.cause == error.BadPreload`), `lib/circuit.zig` and `lib/truth_table/builder.zig` (both test blocks kept). Plan-file collision resolved per `DOCS/prompts/ARCHIVE.md`: the memories plan is archived as `DOCS/archive/plan-memories.md` (canonical commit `2e15e97`, index row added) and its `DOCS/PLANS_PROMPT.md`/`DOCS/PLANS/PHASE_*`/`DOCS/STATUS.md` give way to libcirc's. `tests/fixtures/expected-ast/{rom_basic,ram_basic}.txt` regenerated to gain `Errors (0)` (the only golden change).
**Files touched:** the merge itself (memories' 142 files) plus the resolutions above, `build/frontend_modules.zig`, `DOCS/archive/plan-memories.md` (new), `DOCS/archive/index.md`, `DOCS/STATUS.md`.
**Tests:** `zig build test-all` green (592 tests; `libcirc.wasm` now 400,852 B raw / 151,972 B gzip, memory still flat over 50 calls); `zig build bench` → `golden matches` (58 fixtures, memories' golden); `zig build libcirc` + `libcirc-smoke` ok; `cd site && bun test` 16 pass; `bun --bun run build` green.
**Next slice:** the library half of the memories work that the plan deferred to this rebase — `Options.preloads` (`"preloads": {"<name>": "<hex>"}`), the `stateful_ram`/`bad_preload` refusals (status 3), driver/c_api tests over `rom_lookup`/`ram_basic`; then the renderer v03 sync (Phase 4 slices 1–2) before the site's committed `libcirc.wasm` can be refreshed.
**Notes:** The site's committed `libcirc.wasm`/manifest and example artifacts are still the pre-merge (topology v02) build on purpose: the pinned renderer decodes v01/v02 only, so refreshing them now would break the examples page and trip the playground's version banner. `bun run libcirc` must wait for the renderer v03 sync. The archive step ran as part of the merge rather than after a separate review, because the stack cannot exist with two plans at the same paths.

## 2026-09-09 — Follow-up — memories through the library (post-merge)

**What shipped:** `Options.preloads` (`engine_session.Preload` slice; JSON `"preloads": {"<name>": "<hex>"}` via `json.hexToBytes`, `PreloadNotHex`), `modes.truthTablePreflight(alloc, topology, cap, preloads)` returning `Refusal.bad_preload` / `.stateful_ram` / `.too_many_bits` in the CLI's order (preloads, ram, cap) with the CLI's exact wording, `libcirc.truthTable` mapping each to status 3 and passing preloads to the builder. The CLI's truth-table arm now routes its ram preflight through the same function (byte-identical stderr). `ImageError`/`IMAGE_READ_CAP`/`writeImageError` (and their test) moved from `lib/sim/loop.zig` to `lib/engine_session.zig`; `loop.zig` re-exports them, so `main.zig` and the `--sim` `load` verb are untouched.
**Files touched:** `lib/libcirc.zig`, `lib/libcirc/modes.zig`, `lib/libcirc/json.zig`, `lib/engine_session.zig`, `lib/sim/loop.zig`, `cmd/circ-compile/main.zig`, `tests/libcirc/driver_test.zig`, `tests/libcirc/c_api_test.zig`, `DOCS/libcirc-api.md`, `DOCS/STATUS.md`.
**Tests:** added `driver: rom truth tables with and without a preload equal the CLI goldens` (`rom_lookup.truth.golden` with `mem/rom_lookup.bin`, `rom_basic.truth.golden` unloaded), `driver: ram and bad-preload truth tables are refused with the CLI's words` (`ram_basic` → status 3 == the CLI's stderr minus the newline; `nope` → `no memory named 'nope' (declared memories: rom code[8, 4])`; a 17-byte image → `17 words exceed capacity 16`), `c_api: truth table ram refusal is status 3 and a hex preload loads a rom`, json cases for `PreloadNotHex` and a non-object `preloads`. `zig build test-all` green, `zig build bench` → `golden matches`, `zig build libcirc-smoke` exit 0.
**Next slice:** the renderer v03 sync (Phase 4 slices 1–2 in `circ-renderer`), then `bun run libcirc` + `compile-content.ts` to move the site to topology v03 in one commit.
**Notes:** The playground already sends no `preloads`; loading a ROM image from the page stays deferred with the rest of the memory UI.

## 2026-09-09 — Follow-up — renderer v03 sync landed; site moved to topology v03 in one commit

**What shipped:** In `circ-renderer` (branch `host-pin-api`, pushed; `6f31ea1` on top of `74f595d`): CIRF `0x03` decode (`ComponentKind.Rom = 8`/`Ram = 9`, `PortName.Addr..Clk = 4..7`, `FullComponent.memory.addrWidth`, `SUPPORTED_TOPOLOGY_VERSIONS = [1, 2, 3]`, `isMemory`, `kindName` arms, one exported `portByteOfName` replacing the two private copies), `memoryLabel`/`memorySize` and the `rom`/`ram` place arms ported from `sizing.zig`/`place.zig` (rom 17×3 with one `addr` port; ram 9 rows with `addr/din/we/clk` on rows 1/3/5/7, output on row 4 — all four wires end on ports), `drawMemory` skin, the eight memory exports typed on `runtime.raw`, README, package `2.1.0-alpha.1`, fixtures `rom_lookup.wasm`/`ram_write_read.wasm` compiled by this branch's compiler; `bun test` 16/16, `bun run typecheck` clean. In `site/`, one commit: pin → `#6f31ea1`, `renderer-versions.ts` re-exports the package's `SUPPORTED_TOPOLOGY_VERSIONS`, `bun run libcirc` refreshed `libcirc.wasm` + manifest (revision `c311f08`, `full_version 3`, 405,662 B raw / 153,482 B gzip), `compile-content.ts` regenerated the 11 example artifacts at CIRF `0x03` (23,913–28,279 B).
**Files touched:** `site/package.json`, `site/bun.lock`, `site/src/utils/renderer-versions.ts`, `site/public/wasm/libcirc.wasm`, `site/public/wasm/libcirc.manifest.json`, `site/public/wasm/*.wasm` (11), `DOCS/STATUS.md`; circ-renderer as above.
**Tests:** every regenerated preview equals the committed text modulo trailing padding (content files untouched); `bun test` 16 pass — `the pinned renderer decodes what libcirc.wasm emits` now proves the v03 artifact decodes through the pinned package and `the supported list names the manifest version` holds with `full_version 3`; `bun --bun run build` green (the page's SSR import of `SUPPORTED_TOPOLOGY_VERSIONS` from the package works). The playground now finds `setInputSignal` on the renderer, so pins replay through the canvas's own setter after a theme flip.
**Next slice:** none pending from the plan. Open: the Linux Node `init()` matrix, the browser checklist, the `compile` fast-path decision for implicit builtins, and the renderer PR (`host-pin-api` → `main`) which is the maintainer's to open.
**Notes:** The compiler and the site's artifacts now come from one revision again (decision 7).
