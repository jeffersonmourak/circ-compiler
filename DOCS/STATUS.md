# Status — playground v2 (a workbench, not a demo)

Append-only log, one entry per shipped slice. Newest at the bottom. See `DOCS/PLANS_PROMPT.md` for the phase index and `DOCS/PLANS/PHASE_<N>_*.md` for each phase's spec.

## 2026-09-09 — Phase 0 — Slice 1: start the status log and repoint the libcirc decisions preamble

**What shipped:** This file. `DOCS/decisions/libcirc.md:3` now names the libcirc plan prompt's numbering through the archive (`DOCS/archive/plan-libcirc.md`) and the retrieval command `git show 3a81c361ad3b2f240c602e06f0e968faa56efe26:DOCS/PLANS_PROMPT.md`, so its `(decision 7)` / `(decision 8)` back-references no longer resolve to this initiative's fifteen locked decisions. No executable change.
**Files touched:** `DOCS/STATUS.md` (new), `DOCS/decisions/libcirc.md`.
**Tests:** none added; ran `zig build test-all` (pass), `bun test` in `site/` (16 pass), `bun --bun run build` (pass) — all on code identical to `8c07d29`.
**Next slice:** Slice 2 — make the compile fast path usage-aware (`lib/libcirc/frontend.zig`), regenerate `libcirc.wasm` + manifest in the same commit.
**Notes:** The stale `site/node_modules` trap in `DOCS/PLANS_PROMPT.md` (Recurring Traps → *Environment and tooling*) was already corrected in `8c07d29`, when the phase plans landed: `bun install` ran in this worktree on 2026-09-09 (astro `5.18.1`, vite `6.4.2`); no CodeMirror package is installed. The per-slice commit waiver of the Working Loop is in force for this phase, which the human approved on 2026-09-09.

## 2026-09-09 — Phase 0 — Slice 2: make the compile fast path usage-aware

**What shipped:** `lib/libcirc/frontend.zig`'s `.project_if_imports` arm is `ast_file.imports.len > 0 or usesBuiltinMacro(ir_module)`, where the new private helper scans the single-module IR for an `.unresolved_name` component that `builtins.isMacroImportAlias` accepts (the IR, not `ast_file.components[]`, so an anonymous nested instance counts). `build/frontend_modules.zig` gives the `libcirc` module the `builtins` import; `lib/libcirc.zig`'s test block references `frontend` so its inline tests run under `libcirc_tests`. The `Route` doc comment and the route comment in `cmd/circ-compile/main.zig` describe the new rule. New fixture `tests/fixtures/circuits/builtin_xor_anonymous.circ` (predicate only — it does not compile, see Notes). New CLI test pins `--inspect` on `tests/fixtures/projects/full_adder/root.circ` against `tests/fixtures/expected-inspect/canonical_full_adder_root.txt` (exit 1, `E001: undeclared name 'or'`). Docs: `DOCS/language.md` §3.4 and `DOCS/getting-started.md` §5 (auto-import rule rewritten; the *v0 papercut* narrowed to `--inspect`, not deleted), `DOCS/decisions/playground.md` created with `### The compile fast path resolves implicit builtins` and registered in `DOCS/decisions/index.md`. Same commit: `bun run libcirc` regenerated `site/public/wasm/libcirc.wasm` (405,662 → 405,855 B raw; 153,566 B gzip; manifest revision `67085c7`), `bun run scripts/compile-content.ts` regenerated the 11 committed example artifacts byte-identically, and `bun --bun run build`'s `sync` step refreshed the tracked mirror `site/src/pages/reference/getting-started.md`.
**Files touched:** `lib/libcirc/frontend.zig`, `lib/libcirc.zig`, `build/frontend_modules.zig`, `cmd/circ-compile/main.zig`, `tests/cli/integration_test.zig`, `tests/fixtures/circuits/builtin_xor_anonymous.circ` (new), `tests/fixtures/expected-inspect/canonical_full_adder_root.txt`, `DOCS/language.md`, `DOCS/getting-started.md`, `DOCS/decisions/playground.md` (new), `DOCS/decisions/index.md`, `site/public/wasm/libcirc.wasm`, `site/public/wasm/libcirc.manifest.json`, `site/src/pages/reference/getting-started.md`, `DOCS/STATUS.md`.
**Tests:** added `frontend: an import-free root that uses a builtin takes the project route` (project set, 0 errors/warnings, `file_paths` holds `<builtin>/xor.circ`, length 6 — the spike's expected count held, so it is asserted as `== 6`), `frontend: an anonymous-position builtin still takes the project route` (project set, no `E001`), `frontend: a non-macro undeclared name keeps the fast path` (`E001_undeclared.circ`: no project, 1 error), `frontend: a macro-free root keeps the fast path` (`stress_grid_10x10.circ`), `cli inspect full_adder project root still reports E001 for 'or'`. Ran `zig build test-all` (pass), `bun test` in `site/` (16 pass, incl. `circ_version matches the committed manifest` and the renderer pin against the regenerated module), `bun --bun run build` (pass).
**Next slice:** Slice 3 — `full_adder_from_builtins.circ` joins `compile_fixtures` in `tests/libcirc/driver_test.zig`; the `four_bit_adder` drive case joins `tests/e2e/libcirc_wasm_test.zig`.
**Notes:** (1) `canonical_full_adder_root.txt` was referenced by no test since `2d05d46` and had drifted with the parser swap: its `File` span read `[f0:1:1-7:28]` and the `Input` span `[f0:2:7-2:16]`; the langlang-generated parser prints `[f0:1:1-8:1]` and `[f0:2:1-2:16]`, exactly as the gated `canonical_half_adder_root.txt` already does. The golden was regenerated from the CLI's stdout (two lines changed, the `E001` line untouched); the `TODO(phase0)` in `DOCS/PLANS/PHASE_0_builtins_and_budget.md` about its currency is resolved. (2) The anonymous-instance specializer gap is real: `builtin_xor_anonymous.circ` takes the project route but validation reports three `E012`s, because `resolve_bodies.specializeCallSites` pairs AST instances to IR components positionally and never visits appended anonymous components; recorded in `DOCS/decisions/playground.md` as the known remainder. (3) The `tour-<N>.wasm` files written by `compile-content.ts` stay untracked and unstaged, per the trap. (4) The manifest's `revision` names the commit before this one, as expected for a committed build product.

## 2026-09-09 — Phase 0 — Slice 3: prove it byte for byte, and through the wasm module

**What shipped:** `tests/fixtures/circuits/full_adder_from_builtins.circ` joins `compile_fixtures` in `tests/libcirc/driver_test.zig` (the comment explaining its absence is gone); the `four_bit_adder` `DriveCase` joins `drive_cases` in `tests/e2e/libcirc_wasm_test.zig` (root `/playground/main.circ`, vectors `tests/fixtures/expected-wasm/four_bit_adder.txt`), and the test name lists it. `builtin_xor_anonymous.circ` deliberately stays out of `compile_fixtures` (slice 2, Notes).
**Files touched:** `tests/libcirc/driver_test.zig`, `tests/e2e/libcirc_wasm_test.zig`, `DOCS/STATUS.md`.
**Tests:** extended `driver: compile equals the CLI artifact byte for byte` (the in-process CLI now exits 0 on the import-free builtin root and `libcirc.compile` returns identical bytes) and `libcirc.wasm: compile inverter, slice_basic, four_bit_adder, chain, full_adder project and drive expected-wasm vectors` (the wasm build's artifact drives all 256 committed vectors in Node). Ran `zig build test-all` (pass); `bun test` and `bun --bun run build` unaffected (no site file touched) and green at the slice 2 tree.
**Next slice:** Slice 4 — the browser-module case in `site/test/libcirc.test.ts`.
**Notes:** The drive case runs locally only: CI sets `SKIP_WASM_E2E=1` (`.github/workflows/pr-tests.yml`), so only the native driver test covers the route change there.

## 2026-09-09 — Phase 0 — Slice 4: prove the browser module

**What shipped:** One new case in `site/test/libcirc.test.ts`, `compiles a builtin-using single file with no import`: tour step 5's half-adder with its `import xor` line deleted compiles through the committed `site/public/wasm/libcirc.wasm` with status 0 and both custom sections. No `.wasm` touched — the module was regenerated in slice 2.
**Files touched:** `site/test/libcirc.test.ts`, `DOCS/STATUS.md`.
**Tests:** added the case above; ran `bun test` in `site/` (17 pass, `circ_version matches the committed manifest` and the renderer pin still green), `bun --bun run build` (pass); `git status` shows no modified `.wasm`. `zig build test-all` unaffected (no Zig file touched) and green at the slice 3 tree.
**Next slice:** Slice 5 — `bun run bundle`: the dist walker and the committed budget.
**Notes:** Manual browser checklist — open `/playground`, pick tour step 5, delete the `import xor "<builtin>/xor.circ"` line, confirm the status line no longer says "Compile reported diagnostics." and Simulate renders a canvas — **unrun** (no browser in this session).

## 2026-09-09 — Phase 0 — Slice 5: `bun run bundle`: the dist walker and the committed budget

**What shipped:** `site/scripts/check-bundle.ts` (`bun run bundle`, added to `site/package.json` scripts): walks `dist/**/*.html`, seeds each page from its `<script type="module" src>` / `<link rel="modulepreload" href>` tags, follows static imports and re-exports through the built chunks (scanner written for esbuild-minified syntax), collects `import()` targets as ungated lazy rows, reports inline `<script>` bytes, compares per-file-gzip sums against `site/bundle-budget.json` and exits 1 over budget or when `/playground` reaches no chunk beyond its seeds. `site/bundle-budget.json`: `version 1`, `default { gzip: 10240 }`, `routes["/playground"] { raw: 368640, gzip: 122880 }`, and ten measured `baseline` rows. `DOCS/decisions/playground.md` gains `### The JavaScript budget is per page and gzip is the gate` (registered in `DOCS/decisions/index.md`). The resolved `TODO(phase0)` (modulepreload) is deleted from `DOCS/PLANS_PROMPT.md`'s Open items.
**Files touched:** `site/scripts/check-bundle.ts` (new), `site/bundle-budget.json` (new), `site/package.json`, `DOCS/decisions/playground.md`, `DOCS/decisions/index.md`, `DOCS/PLANS_PROMPT.md`, `DOCS/STATUS.md`.
**Tests:** `bun --bun run build && bun run bundle` exits 0. Measured (files / raw / gzip): `/` 2 / 3,056 / 1,613; `/examples` 2 / 3,056 / 1,613; `/playground` 2 / 11,193 / 4,781; `/download`, `/tour`, `/reference`, `/reference/{getting-started,circuit-format,wasm-api,preview}` 0 / 0 / 0. Lazy chunks: `_astro/circ-theme.*.js` 27,630 / 16,715 and `_astro/index.*.js` (the renderer) 17,451 / 6,437; inline `<script>` bytes across all pages 21,460. Non-vacuity holds: `/playground`'s island chunk statically imports `_astro/preload-helper.*.js`. Negative proof: with `default.gzip` set to `1` the script exits 1 naming `/examples` and `/` (the 0-byte routes stay under 1), then restored. `bun test` (17 pass) and `zig build test-all` unaffected (no tested source touched).
**Next slice:** Slice 6 — the build-free CodeMirror tripwire, `site/test/bundle-graph.test.ts`.
**Notes:** Spike answer, verbatim for the record: Astro renders pages itself and emits no `<link rel="modulepreload">` for a static build — `grep -c modulepreload` over every `dist/**/*.html` is 0 — so decision 14(a)'s assumption does not hold and the walker's chunk scanner is the design that shipped (the modulepreload seed is kept, harmlessly empty). Vite inlined the island's own site modules (`libcirc-client.ts`, `split-files.ts`, `columns.ts`) into the entry chunk, so the only static edge out of `/playground`'s seed is `preload-helper`; the renderer and `circ-theme` are `import()` chunks and the worker is a `new Worker(new URL(...))` reference that the walker does not follow. Lazy rows report each chunk alone, not its own static closure (`circ-theme` imports `skins`, which is listed by neither), per the plan's "never followed" rule.

## 2026-09-09 — Phase 0 — Slice 6: the build-free CodeMirror tripwire

**What shipped:** `site/test/bundle-graph.test.ts`: a source-level walk from `src/layouts/Base.astro`, `src/components/Nav.astro` and `src/components/Footer.astro` that parses `.astro` frontmatter and every `<script>` block, follows relative specifiers through `.ts` / `.mjs` / `.js` / `.astro` / `/index.ts` (skipping `.css`), collects bare specifiers, and asserts none matches `/^(?:@codemirror\/|@lezer\/|codemirror$)/`. Non-vacuity is asserted too: at least six files visited, `src/utils/url.ts` reached (frontmatter parsed), `src/utils/sillyname.ts` reached (an `.astro` `<script>` block parsed, via `PostHog.astro`), and no unresolved relative specifier. A third case pins the scanner against every import form the walk relies on (default, named, side-effect, `import type`, `export … from`, `import()`).
**Files touched:** `site/test/bundle-graph.test.ts` (new), `DOCS/STATUS.md`.
**Tests:** added `bundle graph > nothing in the base layout imports CodeMirror`, `bundle graph > the walk is non-vacuous`, `bundle graph > the scanner sees every import form the walk relies on`. Ran `bun test` (20 pass across 5 files), `bun --bun run build` (pass), `bun run bundle` (ok, numbers unchanged from slice 5). Negative proof: with `import { EditorView } from '@codemirror/view';` added to `Footer.astro`'s frontmatter the first case fails naming `@codemirror/view`; reverted, `git status` clean for that file. `zig build test-all` unaffected (no Zig file touched).
**Next slice:** none — Phase 0 is complete. Phase 1 (`DOCS/PLANS/PHASE_1_editor.md`) starts with the CodeMirror install + spike slice and needs the human's go-ahead as a new phase.
**Notes:** The test walks source, not `dist/`, so it also catches an import that Vite would later tree-shake away; that is intended — the rule is "the base layout never depends on the editor", not "the base bundle happens to be small".

## 2026-09-09 — Phase 1 — Slice 1: install + spike + two-line editor

**What shipped:** The six CodeMirror packages, exact-pinned by `bun add --exact` (`@codemirror/state 6.7.4`, `@codemirror/view 6.43.11`, `@codemirror/language 6.12.4`, `@codemirror/commands 6.11.0`, `@codemirror/lint 6.9.7`, `@lezer/highlight 1.2.3`; bun also resolved `@lezer/common 1.5.2`, `@lezer/lr 1.4.10`, `style-mod 4.1.3`, `crelt 1.0.7`, `w3c-keyname 2.2.8`, `@marijn/find-cluster-break 1.0.4` transitively, none of them a direct dependency). `site/src/scripts/circ-editor.ts` declares the full `EditorOptions` / `EditorHandle` surface of the phase plan's *Data & State* — `ThemeMode` declared locally and exported (slice 3 moves it to `circ-editor-theme.ts` and re-exports it from here), `currentThemeMode()`, `createEditor(parent, options)`, `getDoc` / `setDoc` / `setDiagnostics` / `setTheme` / `select` / `focus` / `destroy` — with the theme `Compartment` seeded with `EditorView.theme({}, { dark })` and reconfigured in place by `setTheme`, `setDoc` carrying a module-level `Annotation` so it never re-enters `onChange`, and `setDiagnostics` dispatching `@codemirror/lint`'s `setDiagnostics(view.state, list)` (a `TransactionSpec` that enables the lint state itself, so no `linter()` and no `lintGutter()` yet). The language (slice 2), the compartment's real content (slice 3) and `lintGutter()` (slice 4) fill in behind that surface unchanged. `Playground.astro` gains the `.pg-editor-wrap` / `#pg-editor` markup around the textarea, `state.source` as the pipeline's single source of truth (read by `fire()`, `locate()` and `renderDiagnostics()`, written by the textarea's `input`/Tab handlers, the example `pick` and the editor's `onChange`), the idle dynamic `import('../scripts/circ-editor.ts')` with its reject fallback (`Rich editor unavailable; using the plain text box.`), `editor?.setTheme(currentThemeMode())` inside the existing `MutationObserver`, and the first-focus `ensureReady()` hook moved off the textarea onto the editor host's `focusin`. `DEBOUNCE_MS = 250` and the single `state.gen` counter are untouched (decision 9 is Phase 3). New `.pg-cm*` / `.pg-editor-wrap` geometry inside the `.pg*` block of `global.css`; no colours, `.lc-mount` untouched. `DOCS/decisions/playground.md` gains `### The editor is CodeMirror 6 with a hand-written StreamLanguage` and `### The editor chunk loads on idle, not on interaction`, both registered in `DOCS/decisions/index.md`; both `TODO(phase1)` entries are deleted from `DOCS/PLANS_PROMPT.md`'s Open items (the two `TODO(phase1)` back-references inside decisions 1 and 14 stay).
**Files touched:** `site/package.json`, `site/bun.lock`, `site/src/scripts/circ-editor.ts` (new), `site/test/codemirror-pin.test.ts` (new), `site/src/components/Playground.astro`, `site/src/styles/global.css`, `DOCS/decisions/playground.md`, `DOCS/decisions/index.md`, `DOCS/PLANS_PROMPT.md`, `DOCS/STATUS.md`.
**Tests:** added `codemirror pin > codemirror packages are pinned exactly` (each of the six is a bare `x.y.z` in `dependencies` and resolves at that version in `bun.lock`) and `codemirror pin > no meta-package, autocomplete or search is installed`. Ran `bun test` in `site/` (22 pass across 6 files, `bundle-graph.test.ts` still green — no `@codemirror/*` reachable from `Base`/`Nav`/`Footer`), `bun --bun run build` (10 pages, pass), `bun run bundle` (ok; `/playground` 2 files / 11,857 raw / 5,052 gzip against 368,640 / 122,880). `zig build test-all` unaffected (no Zig file touched).
**Next slice:** Slice 2 — `site/src/utils/circ-tokens.mjs`, both consumers (`circ-lang.mjs`'s `match` strings, which fixes `rom`/`ram`), `circStreamParser` / `circLanguage` in `circ-editor.ts`, and `circ-tokens.test.ts` + `circ-editor-parser.test.ts`.
**Notes:**
1. **The 20 % rule fired, the keymap-only split cleared it, and it is applied.** With the planned `keymap.of([indentWithTab, ...defaultKeymap, ...historyKeymap])` the emitted chunk measured **303,788 bytes raw / 98,646 gzip** — 80.28 % of the 122,880-byte ceiling, i.e. *inside* the 20 % band by 342 bytes. Per the corrected rule in `DOCS/PLANS/PHASE_1_editor.md`'s *Open Questions*, slice 1 measured the keymap-only split: `defaultKeymap` and `historyKeymap` are replaced by a hand-rolled `circKeymap` of individual commands and `history()` is kept (Phase 2's per-file undo is `historyField`). Measured **286,539 / 93,363 gzip** (75.98 %) with a minimal binding set; adding `Mod-/ → toggleComment` back (so slice 2's `languageData.commentTokens` keeps its consumer) gives the **shipped** number, **289,367 bytes raw / 94,370 gzip = 76.80 %** of the ceiling — clear of the band by 3,934 bytes. No human gate was opened, `site/bundle-budget.json` was not edited, and the chunk stays an ungated informational line of `bun run bundle`. The shipped bindings are `Tab`/`Shift-Tab` → `indentMore`/`indentLess` (`indentWithTab` verbatim), `Enter` → `insertNewlineAndIndent`, `Mod-[`/`Mod-]` → `indentLess`/`indentMore`, `Mod-/` → `toggleComment`, `Ctrl-m` (`Shift-Alt-m` on macOS) → `toggleTabFocusMode` (bound exactly as `defaultKeymap` binds it, so Tab is not a keyboard trap), `Mod-z` → `undo`, `Mod-y` / `Mod-Shift-z` and `Ctrl-Shift-z` (Linux) → `redo`. What is *not* bound any more, relative to the plan: everything else in `defaultKeymap` and `standardKeymap` — cursor and selection motion (native contenteditable selection), `Backspace` / `Delete` / plain `Enter` (the view translates them from `beforeinput`, `PendingKeys` at `@codemirror/view/dist/index.js:4722-4727`), and the line-manipulation extras (`Alt-ArrowUp`, `Shift-Mod-k`, `Escape` → `simplifySelection`, `Mod-i`, `Alt-A`, `Mod-Enter`, `undoSelection` / `redoSelection` on `Mod-u` / `Alt-u`).
2. **Extension and API names, re-stated against the real `site/node_modules` (this is what makes later phases' CodeMirror claims non-provisional).** Every name below was read out of the installed `.d.ts` at the pinned version and every one used in this slice compiles and builds. `@codemirror/state`: `Annotation.define` (`dist/index.d.ts:771`), `Transaction.annotation` (`:970`), `Compartment.of` / `.reconfigure` / `.get` (`:732-748`), `EditorSelection.create` (`:490`) / `.range` (`:499`) / `.single` (`:485`), `EditorState.create` (`:1193`) / `.tabSize` (`:1208`) / `.readOnly` (`:1242`), `TransactionSpec.{changes,selection,effects,annotations,scrollIntoView}` (`:862,868,878,882,891`), `Extension` — all present in the export list at `:1726`. `@codemirror/view`: `EditorView.theme(spec, { dark })` (`:1403-1407`), `.baseTheme` (`:1422`), `.updateListener` (`:1275`), `.editable` (`:1284`), `.contentAttributes` (`:1435`), `.lineWrapping` (`:1445`), `.scrollIntoView` (`:1130`), `dispatch` (`:835-837`), `focus` (`:1112`), `destroy` (`:1124`), `ViewUpdate.transactions` (`:557`) / `.docChanged` (`:597`), plus `keymap`, `lineNumbers`, `highlightActiveLine`, `highlightActiveLineGutter`, `gutter`, `drawSelection` and the `KeyBinding` type in the export list at `:2416`. `@codemirror/language`: `indentUnit` (`:434`), and `StreamLanguage`, `StreamParser`, `StringStream`, `HighlightStyle`, `TagStyle`, `syntaxHighlighting` in the export list at `:1219` (slices 2 and 3 use them). `@codemirror/commands` export list at `:658` carries `defaultKeymap`, `standardKeymap`, `history`, `historyField`, `historyKeymap`, `indentWithTab`, `indentMore`, `indentLess`, `insertNewlineAndIndent`, `toggleComment`, `toggleTabFocusMode`, `undo`, `redo`. `@codemirror/lint` export list at `:195` carries `setDiagnostics` (`:128`, `(state, diagnostics) => TransactionSpec`), `lintGutter` (`:185`), `linter`, and the `Diagnostic` interface (`:9-49`) whose `severity` is `Severity = "hint" | "info" | "warning" | "error"` (`:5`); `maybeEnableLint` at `dist/index.js:126-138` is what makes `setDiagnostics` self-install the lint state without `linter()`. `@lezer/highlight` exports `tags` (slice 3). One correction to the plan's *Data & State* block: `TransactionSpec.selection` takes an `EditorSelection` or `{ anchor, head? }`, **not** the `SelectionRange` that `EditorSelection.range` returns, so `select()` dispatches `EditorSelection.create([EditorSelection.range(a, b)])`.
3. **Two small departures from the slice-1 text, both deliberate.** (a) The `ensureReady()` `focusin` listener sits on `.pg-editor-wrap`, the element that *contains* `#pg-editor`, not on `#pg-editor` itself: `focusin` bubbles, so one listener covers the CodeMirror mount and the fallback textarea both, and blocking the editor chunk cannot leave the wasm ungated. (b) `EditorHandle` is typed as `ReturnType<EditorMod['createEditor']>` over `type EditorMod = typeof import('../scripts/circ-editor.ts')` rather than through an `import type`, matching the existing `typeof import('../utils/circ-theme.mjs')` pattern at `Playground.astro:344` and keeping the island's source free of any static specifier for the editor module.
4. **Manual checklist — unrun** (no browser in this session): the editor mounts on `/playground` and accepts two lines; typing still runs the analyze → compile pipeline through `state.source`; the textarea still edits and still compiles when the editor chunk is blocked; the `Rich editor unavailable; using the plain text box.` status text on a rejected import.

## 2026-09-09 — Phase 1 — Slice 2: `circ-tokens.mjs` + both consumers + tokenizer test

**What shipped:** `site/src/utils/circ-tokens.mjs`, the one token table: `DECLARATION_KEYWORDS` (`input`, `output`, `import`), `BUILTIN_TYPES` (the ten `circ-lang.mjs` carried plus `rom` and `ram`), `OPERATORS` (`['<>', '..', '=']`, longest first), `alternation()`, `KEYWORD_MATCH` / `BUILTIN_MATCH`, and the pure tokenizer `startState` / `copyState` / `nextToken` / `tokenizeLine` implementing the phase plan's 14-step state machine in order. Steps 4, 5 and 8 are one loop over `OPERATORS`, matched longest-first; the three literals appear nowhere else in the module, and step 13's `invalid` catch-all is what makes `end > pos` unconditional. `circ-lang.mjs`'s `keyword` and `type` `match` literals (`:49`, `:57`) are now `KEYWORD_MATCH` / `BUILTIN_MATCH` — **the `rom` / `ram` bug fix**, which lights up memories in every docs code block on the site — and its stale header pointer to the never-existing in-tree `tools/circ-lsp/syntaxes/circ.tmLanguage.json` now names `https://github.com/jeffersonmourak/circ-lsp`. Everything else in the grammar object is byte-identical (`scopeName`, `fileTypes`, the eleven-entry `patterns` include order, every other repository entry), so `astro.config.mjs` and `CodePreview.astro` consume it unchanged. `circ-editor.ts` gains `circStreamParser` (`name`, `startState`, `copyState`, `token`, `languageData: { commentTokens: { line: '//' } }`) and `circLanguage = StreamLanguage.define(circStreamParser)`, with `circLanguage` first in the extension set; the `token` adapter reads `stream.string` / `stream.pos`, calls `nextToken`, assigns `stream.pos = end` and returns the tag or `null`. `DOCS/decisions/playground.md` gains `### One token table, two consumers`, registered in `DOCS/decisions/index.md`.
**Files touched:** `site/src/utils/circ-tokens.mjs` (new), `site/src/utils/circ-lang.mjs`, `site/src/scripts/circ-editor.ts`, `site/test/circ-tokens.test.ts` (new), `site/test/circ-editor-parser.test.ts` (new), `DOCS/decisions/playground.md`, `DOCS/decisions/index.md`, `DOCS/STATUS.md`.
**Tests:** `site/test/circ-tokens.test.ts` — `the shipped corpus is the 10 examples and the 7 tour steps` (the count the other cases rest on), `tokenizeLine partitions every shipped source` (over all 10 `examples[].source` and 7 `tour[].source`: tokens contiguous from 0 to `line.length`, no gap, no overlap, slices concatenate back to the line), `nextToken always advances` (every position of every such line, with `startState()` and with `{ params: true }`, plus `\t`, `%`, `@`, `#`, `;` — the last four landing on `invalid`), `rom and ram tokenize as builtin types` (`rom code[8, 4](addr = pc.out)`, the `ram` twin, `bus`, `xnor`, `led`, and every word of both tables standing alone), `markers and builtin imports tokenize as expected` (`// half_adder.circ` is one `comment`; `import xor "<builtin>/xor.circ"` is `keyword`, `typeName`, `string`; `not n(in=a)` is `typeName`, `variableName.function`, `punctuation`, `propertyName`, `operator`, `variableName`, `punctuation`), `the TextMate grammar is built from the token table` (both `match` strings are the exported constants, both contain `rom` and `ram`, `alternation` shape, `scopeName === 'source.circ'`, the include order unchanged), `operators come from the token table` (`OPERATORS` exactly `['<>', '..', '=']` and non-increasing in length; each operator matched by exactly one of `connection.patterns[0]` / `operator.patterns[0]` / `operator.patterns[1]` and each pattern by exactly one operator; each tokenizes as `operator`). `site/test/circ-editor-parser.test.ts` — `the stream parser always advances` (imports `circ-editor.ts` headlessly and drives `circStreamParser.token` over every line of all 17 sources with a duck-typed `{ string, pos, start }`: `stream.pos` strictly increases, the return is `null` or a string, the walk ends exactly at `line.length`, and more than 100 tokens were read), `the parser state round-trips through copyState`, `the parser spec is accepted and carries its language data` (`StreamLanguage.define(circStreamParser)` does not throw and `languageData.commentTokens` equals `{ line: '//' }`). Ran `bun test` in `site/` (32 pass across 8 files, 11,271 assertions), `bun --bun run build` (10 pages, pass — the build is the proof that Shiki still accepts the grammar object), `bun run bundle` (ok; `/playground` 2 files / 11,857 raw / 5,054 gzip against 368,640 / 122,880). Negative proofs, each reverted: deleting `rom` and `ram` from `BUILTIN_TYPES` fails `rom and ram tokenize as builtin types` and `the TextMate grammar is built from the token table`; making step 13 return `end: pos` fails `nextToken always advances`; making the identifier branch return `end: pos` fails both of the parser test's advance cases. `zig build test-all` unaffected (no Zig file touched).
**Next slice:** Slice 3 — the theme binding: `site/src/utils/circ-editor-theme.ts` (`TAG_SCOPES`, `scopeStyle`, `styleSpecs`, `editorPalette`, `themeFor`), filling the theme `Compartment` slice 1 seeded, and `site/test/circ-editor-theme.test.ts`.
**Notes:**
1. **The `input_pin` / `output_pin` spike is resolved: they stay out of `BUILTIN_TYPES`, and the reasoning is in `circ-tokens.mjs`'s header comment.** The grep the plan asks for was run over `DOCS/*.md` and `site/src/content/*.ts`. The plan's premise ("neither occurs") is half right and the correction is recorded here: the two names occur **17 times across `DOCS/*.md`** — `architecture.md`, `simulation-engine.md`, `preview.md`, `wasm-api.md`, `getting-started.md` — and **zero times in `site/src/content/{examples,tour}.ts`**. Every one of the 17 is a Zig identifier (`input_pin_gate`, `output_pin`), a `--preview` box label, or prose about the topology encoding; **zero** are inside a ```circ fence of any of the five docs the site publishes (`language.md` → `/reference`, plus the four under `/reference/`), and `DOCS/language.md` contains neither name at all. So adding them would highlight nothing that exists today while changing how every future docs code block renders an identifier that happens to be named that — a scope change, not a bug fix, which is what `rom` / `ram` is. Note also that `\b(…|input_pin|…)\b` would not match inside `input_pin_gate` anyway (`_` is a word character), so even the prose occurrences are unaffected either way; the decision rests on scope, not on collateral.
2. **First JSDoc types in an `site/src/utils/` `.mjs`.** `circ-tokens.mjs` is the first of the five `.mjs` files there (`circ-assets.mjs`, `circ-lang.mjs`, `circ-theme.mjs`, `shiki-themes.mjs` are the others) to carry `@typedef` / `@param` / `@returns`, which is what makes `import { type CircTokenState } from '../utils/circ-tokens.mjs'` resolve in `circ-editor.ts`. Nothing in CI typechecks it — `bun test` and `astro build` both strip types with esbuild and `site/package.json` has no `typecheck` script — so the JSDoc is for the editor and for Phases 2 and 5, and its correctness is not gated. The file stays `.mjs` because `astro.config.mjs` reaches it through `circ-lang.mjs` and an `.mjs` Astro config cannot import `.ts` (decision 15).
3. **The editor chunk grew and is still clear of the 20 % band, by less than before.** `_astro/circ-editor.*.js` went from 289,367 raw / 94,370 gzip (slice 1) to **299,112 raw / 97,593 gzip** — `+9,745` raw, `+3,223` gzip for `StreamLanguage`, `StringStream` and the tokenizer. That is **79.42 %** of the 122,880-byte gzip ceiling, clear of the band's 98,304-byte edge by **711 bytes** (slice 1's shipped number cleared it by 3,934). No budget row was added and `site/bundle-budget.json` was not touched — the chunk is `import()`-only and stays an ungated informational line — but slice 3's `HighlightStyle` + `@lezer/highlight` `tags` import is the next addition to this chunk and it will very likely cross the edge. Per the plan's corrected rule the keymap-only split is already applied, so there is no cheaper measure left inside slice 3; if the number lands inside the band there, that is a phase-boundary escalation to report, not a silent ceiling raise.
4. **A tokenizer bug must fail, not hang.** The parser test's `copyState` case originally drove the stream with an unbounded `while (stream.pos < line.length)`; a deliberately non-advancing tokenizer turned it into an infinite loop instead of a red test. Both driving loops in that file are now bounded by the line length and assert the advance on every step. Added to `DOCS/PLANS_PROMPT.md`'s traps in spirit only — it is test hygiene, not a repo constraint, so nothing was appended there.
5. **Manual checklist — unrun** (no browser in this session): `/playground` shows a syntax-highlighted editor and `rom code[8, 4](addr = pc.out)` colours `rom` like `and`; `/reference` renders `rom` / `ram` highlighted in its code blocks; `Mod-/` comments a line with `//`.

## 2026-09-09 — Phase 1 — Slice 3: the theme binding

**What shipped:** `site/src/utils/circ-editor-theme.ts` — `ThemeMode` (moved here from `circ-editor.ts`, which re-exports it so no consumer's import changed), the eleven-entry `TAG_SCOPES` table, `scopeStyle` (last `tokenColors` entry naming the scope or a dotted prefix of it; throws when none does), `styleSpecs` (the TextMate `fontStyle` string split word by word into `fontStyle` / `fontWeight` / `textDecoration`, throwing on any other word), `editorPalette` and `themeFor`. In `circ-editor.ts` the theme `Compartment` that slice 1 seeded is now filled with `EditorView.theme(chromeSpec(palette), { dark })` + `syntaxHighlighting(HighlightStyle.define(specs.map(tagStyle)))`, where `resolveTag` turns each tag string into a `@lezer/highlight` `Tag` (`'variableName.function'` → `tags.function(tags.variableName)`); `setTheme` still reconfigures in place and the view is never destroyed. The chrome names `.cm-content ::selection` / `.cm-content::selection` explicitly, because `drawSelection()` is not in the extension set and `.cm-selectionBackground` therefore never exists in the DOM.
**Files touched:** `site/src/utils/circ-editor-theme.ts` (new), `site/src/scripts/circ-editor.ts`, `site/test/circ-editor-theme.test.ts` (new), `DOCS/decisions/playground.md`, `DOCS/decisions/index.md`, `DOCS/STATUS.md`.
**Tests:** added ten cases in `site/test/circ-editor-theme.test.ts` — tag resolution through `@lezer/highlight`, totality of the palette over every tag `tokenizeLine` emits across all 17 shipped sources, one spec per `TAG_SCOPES` entry in order, every colour pinned against `shiki-themes.mjs` (`keyword` `#0a6634` / `#1ee17d`, `comment` `#8a7e9a` / `#7d6e94` italic, `string` and `number` `#8e4a0e` / `#e17d1e`, `operator` `#6e49ab` / `#b097d1`, `typeName` the keyword green, `invalid` `#a83737` / `#ff6b8a`, backgrounds `#e8def0` / `#1f1438`), the prefix precedence (`keyword.operator` beats `keyword`), the throw on an absent scope, the `fontStyle` split, the lint underline colours, the selection being distinct from the background, and `themeFor`'s mode pick. Ran `bun test` (42 pass across 9 files), `bun --bun run build` (pass), `bun run bundle` (ok). Negative proof: passing the raw TextMate word straight through as `fontStyle` fails `the TextMate fontStyle is split, not passed through`; reverted.
**Next slice:** Slice 4 — `site/src/scripts/circ-diagnostics.ts`, `lintGutter()`, the `underline(color)` helper and the two `backgroundImage` rebuilds, and both diagnostics producers in `Playground.astro` behind the `snapshot !== state.source` guard.
**Notes:**
1. **The editor chunk has crossed the 20 % band, and the remedy the rule names is spent.** `_astro/circ-editor.*.js` measures **308,196 B raw / 100,131 B gzip** — 81.49 % of the 122,880-byte figure, i.e. 1,827 bytes inside the band, up from 97,593 after slice 2. The increment is `syntaxHighlighting` + `HighlightStyle` + `@lezer/highlight`'s `tags`, which *is* the phase's Goal; there is no version of this phase that ships without it, and the keymap-only split was already applied in slice 1. Nothing was raised and nothing was trimmed: **`bun run bundle` is green**, because the 122,880 figure is `/playground`'s *eager*-graph ceiling (measured 4,981 gzip, 2 files) and decision 14(a) excludes lazily imported chunks by construction — the editor chunk is reported on the ungated informational line and has no budget row. `site/bundle-budget.json` is untouched. The band is a planning heuristic comparing a lazy chunk against an eager ceiling, so crossing it is a signal to decide, not a regression: it is reported to the human at the end of the phase, and gating the chunk (or not) is a decision-14 amendment with its own `###` entry, not a slice-local call.
2. `circ-editor-theme.ts` is the fourth Phase 1 module where decision 15 named three. The justification is in `DOCS/decisions/playground.md` under `### The editor palette is derived from the shiki themes`: `createEditor` builds DOM unconditionally and `bun test` has no `document`, so the palette derivation is the only part of the theme binding a test can reach, and it is exactly the part that must not drift from `shiki-themes.mjs`.
3. Manual checklist — **unrun** (no browser in this session): flipping the theme recolours the editor in place with cursor, scroll position and undo history intact; `rom code[8, 4](addr = pc.out)` colours `rom` like `and`.

## 2026-09-09 — Phase 1 — Slice 4: diagnostics → squiggles + gutter

**What shipped:** `site/src/scripts/circ-diagnostics.ts` — the widened analyze types (`AnalyzeRange`, `AnalyzeDiagnostic` with `related`, `AnalyzeSymbol` with `range` and `addr_width`, `AnalyzeReference`, `Analysis`) plus `indexDoc`, `fileNameFor`, `docLine`, `offsetAt`, `rangeToSpan`, `mapDiagnostics` and `toLintDiagnostics`. `circ-editor.ts` gained `lintGutter()` and an exported `underline(color)` that rebuilds `.cm-lintRange-error` / `-warning` from the palette (the package bakes its colours into SVG data URIs, so recolouring is a no-op); the lint gutter markers keep their defaults. `Playground.astro` dropped its three local interfaces for the imported ones, added `state.mapped`, captured `const snapshot = state.source` in `fire()`, and routes **both** diagnostics producers — the analyze reply and the status-1 compile reply, the latter mapped as its own `Analysis` because it carries its own `files[]` — through one `publishDiagnostics(snapshot, analysis)`, which pushes spans into the editor only while `snapshot === state.source`.
**Files touched:** `site/src/scripts/circ-diagnostics.ts` (new), `site/test/circ-diagnostics.test.ts` (new), `site/src/scripts/circ-editor.ts`, `site/src/components/Playground.astro`, `DOCS/decisions/playground.md`, `DOCS/decisions/index.md`, `DOCS/PLANS_PROMPT.md`, `DOCS/STATUS.md`.
**Tests:** added 13 cases in `site/test/circ-diagnostics.test.ts` — nine pure (byte-column conversion over `éé x` and `😀x`, file-marker crossing on tour step 6, the builtin file that keeps its row but yields no span, clamping/inversion/empty-widening, surrogate pairs never split, the lint `Diagnostic` shape, file-local list positions, `docLine` reproducing today's combined line and `from` reproducing today's `caretOffset` jump, and the status-1 compile body mapping identically to the same diagnostics from analyze) and four wasm-driven against the committed module (exact source text under the mapped span, an error in the **first** of two files landing before the second file's offset, multi-byte padding not shifting the mapping, and the headline example's real code and span). Ran `bun test` (55 pass across 10 files), `bun --bun run build` (pass), `bun run bundle` (ok).
**Next slice:** Slice 5 — rewrite `renderDiagnostics()` over `state.mapped` with file-local labels, per-severity counts and `editor.select(from, to)` jumps.
**Notes:**
1. **Spike resolved — the headline example does not report what the plan promised.** `DOCS/PLANS_PROMPT.md` claimed `input b / not n(in=a) / output o(in=n.out)` would squiggle "under exactly `a`" from `E001`. Measured against the committed module it reports **`E004` "required input 'in' is unconnected" spanning the whole declaration `not n(in=a)`**, plus **`W001` spanning exactly the unused `b`**. Validator codes carry declaration or connection spans, never a bare identifier (`E001` is raised for an unresolved component *type*: `mystery n(in=b)` reports `E001` over the whole declaration). Both are pinned in `the headline example reports E004 over the whole declaration`, and the Phase Index row in `DOCS/PLANS_PROMPT.md` was corrected to match. The editor's contract — the squiggle covers exactly the analyze range — is unaffected.
2. The staleness guard is new in this phase and is now a Recurring Trap under Pillar 4(d) in `DOCS/PLANS_PROMPT.md`.
3. Manual checklist — **unrun** (no browser): a red squiggle at the compiler's range with a marker in the lint gutter, in both themes.

## 2026-09-09 — Phase 1 — Slice 5: the diagnostics list and its jump

**What shipped:** `renderDiagnostics()` in `Playground.astro` is rewritten over `state.mapped`, which both producers refresh through `publishDiagnostics`. Each row is a button labelled `<file>:<line>:<col> <code> <message>` from the compiler's own file-local position, carrying its severity class; clicking calls `editor.select(from, to)` — which focuses and scrolls — or falls back to `setSelectionRange(from, to)` on the textarea when the editor chunk never loaded. A row whose span is `null` (a `<builtin>/…` file) still renders, disabled, rather than vanishing. A per-severity count line heads the list and the `No diagnostics.` empty state is unchanged. `locate()` and the now-unused `byteColToUtf16` / `caretOffset` imports are gone from the island — that arithmetic lives in `circ-diagnostics.ts`. `.pg-diag-warning`, `.pg-diag-counts` and a disabled-button rule join the `.pg*` block.
**Files touched:** `site/src/components/Playground.astro`, `site/src/styles/global.css`, `site/test/circ-diagnostics.test.ts`, `DOCS/STATUS.md`.
**Tests:** added `the rendered row string is the file-local label`, pinning the exact row text `root.circ:1:1 E004 required input 'in' is unconnected`; the label change is also covered by `list rows carry the compiler's own file-local position` and the old numbers by `docLine reproduces today's combined line, and the jump does not regress` (both slice 4). Ran `bun test` (56 pass across 10 files), `bun --bun run build` (pass), `bun run bundle` (ok).
**Next slice:** none — Phase 1 is complete. Phase 2 (`DOCS/PLANS/PHASE_2_file_tabs.md`) is the next phase and needs the human's go-ahead.
**Notes:**
1. The jump now selects the whole diagnostic range rather than placing a bare caret at its start. `from` is unchanged from what `caretOffset` produced before (pinned by `the jump target does not regress`), so nothing moved; the selection is what makes the squiggle's extent visible when the reader arrives.
2. Manual checklist — **unrun** (no browser): clicking a row focuses the editor with exactly that range selected in both themes, and `Tab` still indents while `Ctrl-m` / `Shift-Alt-m` releases the trap.
3. Final editor chunk for the phase: see the phase close-out note below.

## 2026-09-09 — Phase 1 — close-out

**What shipped:** Phase 1 is complete in five slices (`ab1ac23`, `e36e7b1`, `6e18122`, `9dc43bb`, `8bd23d9`). `/playground` runs a CodeMirror 6 editor with a circ `StreamLanguage` built from the one token table that also feeds the docs' TextMate grammar, a palette derived from the shiki themes and reconfigured in place on a theme flip, `circ_analyze` diagnostics as squiggles plus lint-gutter markers at the compiler's exact ranges, and a diagnostics list whose rows carry file-local positions and jump to their span. The `<textarea>` remains as the fallback when the editor chunk fails to load.
**Files touched:** none beyond the five slices; this entry is the phase's measurement of record.
**Tests:** `bun test` 56 pass across 10 files (up from 20 at the end of Phase 0); `bun --bun run build` green; `bun run bundle` green; `zig build test-all` untouched by this phase and green.
**Next slice:** Phase 2 (`DOCS/PLANS/PHASE_2_file_tabs.md`), which needs the human's go-ahead as a new phase.
**Notes:** **Final editor chunk: `_astro/circ-editor.*.js` at 311,531 B raw / 101,166 B gzip — 82.33 % of the 122,880-byte figure the 20 % band is drawn around, i.e. 2,862 bytes inside it.** It grew across the phase as the phase's own Goal landed: 94,370 after slice 1's keymap-only split, 97,593 after the language, 100,131 after the palette, 101,166 after the diagnostics wiring. No gate is red and nothing was raised: decision 14(a) excludes lazily imported chunks by construction, the chunk has no budget row, `site/bundle-budget.json` is untouched, and `/playground`'s *gated* eager graph measures 5,632 gzip against its 122,880 ceiling. The band compares a lazy chunk against an eager ceiling, so crossing it is a planning signal, not a regression — but the remedy the rule named (the keymap split) was spent in slice 1, and nothing further can come out without removing highlighting or diagnostics, which are the phase. **This is reported to the human as a phase-boundary item:** either accept the chunk as it stands and note it in decision 14, or amend decision 14 to give lazy chunks their own budget rows with a ceiling chosen from this measurement. Either way it is a decision entry, not a slice-local call.

## 2026-09-09 — Phase 2 — Slice 1: `joinFiles`, the name predicate, the round-trip contract

**What shipped:** `site/src/utils/split-files.ts` gains `NamedFile { name, body }` (with `SplitFile extends NamedFile`, keeping `startLine`), `joinFiles(files)` as the exact inverse of `splitFiles`, `isFileName(name)` derived from the same `MARKER` regex, and `JoinConflict` / `joinConflicts(files)` reporting `blank-body`, `marker-in-body`, `illegal-name` and `duplicate-name` by index. `rootOf` is now generic over `readonly T[] extends NamedFile` and `requestFor` takes `readonly NamedFile[]`, so a tab array with no `startLine` drives the worker requests unchanged. `MARKER`, `splitFiles` and `PLAYGROUND_DIR` are untouched. Two decision entries landed in `DOCS/decisions/playground.md`.
**Files touched:** `site/src/utils/split-files.ts`, `site/test/split-files.test.ts`, `DOCS/decisions/playground.md`, `DOCS/decisions/index.md`, `DOCS/PLANS_PROMPT.md`, `DOCS/STATUS.md`.
**Tests:** added 13 cases to `site/test/split-files.test.ts` — the 17-source byte-identical round trip, the left inverse over five hand-built conflict-free arrays, the `// main.circ` drop and the shielded-marker keep, the trailing-marker and non-canonical-marker normalisations, each of the four conflicts (with the failure each predicts actually reproduced), `splitFiles` output always being conflict-free, the `isFileName` accept/reject table, and `requestFor` over a bare `{name, body}[]` equalling `requestFor(splitFiles(…))`. Ran `bun test` (69 pass across 10 files), `bun --bun run build` (pass).
**Next slice:** Slice 2 — `site/src/scripts/file-tabs.ts`: `FileTabsState`, `fromSource`, `toSource`, `rootIndex`, `activeFile`, `select`, `setBody`, and its test.
**Notes:**
1. **One plan correction.** `DOCS/PLANS/PHASE_2_file_tabs.md` predicts that an illegal file name makes `splitFiles(joinFiles(files))` "collapse the pair" to one file. It does not: for `[{name:'foo'},{name:'b.circ'}]` the count is preserved and the *identity* is lost — the first file comes back as the invented `main.circ` with its own `// foo` line stranded in its body. The conflict is real and still worth reporting; the test asserts the true failure.
2. The `bun --bun run dev` requirement is now a Recurring Trap in `DOCS/PLANS_PROMPT.md`: plain `bun run dev` fails exactly like plain `bun run build`, because the x64 node under Rosetta cannot load rollup's native binding. The dev server serves on `http://localhost:4321`.
3. `TODO(phase2)-A` and `-B` are both resolved before slice 3a needs them: the shipped `circ-editor.ts` matches Phase 1's specified handle exactly, and Phase 1's budget verdict kept `@codemirror/commands` (only the keymap was hand-rolled), so `history()` / `historyField` survive and per-file undo is available to the state-per-file model.

## 2026-09-09 — Phase 2 — Slice 2: the tab-state model

**What shipped:** `site/src/scripts/file-tabs.ts` — `FileTab` (an alias of `NamedFile`), `FileTabsState { files, active }` with the never-empty and in-range invariants, `fromSource` (splits and activates tab 0), `toSource` (`joinFiles`), `rootIndex`, `activeFile`, `select` (clamped, identity when the tab is already active) and `setBody` (identity for an out-of-range index or an unchanged body; shares untouched entries). No DOM, no editor package, no browser storage. The remaining reducers are slices 4-6.
**Files touched:** `site/src/scripts/file-tabs.ts` (new), `site/test/file-tabs.test.ts` (new), `DOCS/STATUS.md`.
**Tests:** added nine cases in `site/test/file-tabs.test.ts` — the 17-source byte-identical `toSource(fromSource(s))` round trip, the empty-string single-`main.circ` case, tour step 6 as two tabs with the root last and no join conflicts, `select` clamping both directions and returning identity for the current tab, `select` sharing the files array, `setBody` purity (new object, shared untouched entries, input byte-identical afterwards), `setBody` identity on an out-of-range index or an unchanged body, an edit surviving the round trip back through the combined source, and the source-text purity scan. Ran `bun test` (78 pass across 11 files), `bun --bun run build` (pass).
**Next slice:** Slice 3a — `site/src/scripts/doc-registry.ts` and the document registry in `circ-editor.ts` (`setDocuments`, `showDocument`, `insertDocument`, `removeDocument`, `moveDocument`, `textOf`, `setDiagnosticsFor`, the theme fan-out, the widened `onChange(doc, index)`).
**Notes:** The purity case scans the module's own source text, so the literal token `localStorage` in the file's header comment failed it on the first run. The comment was reworded rather than the test loosened — the bluntness is the point, and the file now says so.

## 2026-09-09 — Phase 2 — Slice 3a: the document registry

**What shipped:** `site/src/scripts/doc-registry.ts` — pure index bookkeeping over an opaque `T`: `insert`, `remove`, `move` (all clamping, all returning new arrays) and `activeAfter(active, op)` for `insert` / `remove` / `move`, on the rule that the previously active *entry* stays active. `site/src/scripts/circ-editor.ts` gained the registry built on it: `setDocuments`, `showDocument`, `insertDocument`, `removeDocument`, `moveDocument`, `textOf`, `setDiagnosticsFor`, one `EditorState` per file (so selection and undo history are per file by construction), a saved scroll snapshot per file, `setTheme` fanning the compartment reconfigure out over every stored state before dispatching on the visible one, and `onChange` widened to `(doc, index)` by appending the index. Phase 1's `getDoc` / `setDoc` / `setDiagnostics` / `select` keep their meaning against the visible document, so a single-document consumer is unaffected. No rendered HTML changed.
**Files touched:** `site/src/scripts/doc-registry.ts` (new), `site/test/doc-registry.test.ts` (new), `site/src/scripts/circ-editor.ts`, `DOCS/STATUS.md`.
**Tests:** added seven cases in `site/test/doc-registry.test.ts` — purity and permutation (every op returns a new array, mutates neither input nor entries, `move` composed with its inverse restores the input), `insert` at the root index keeping the root last, clamping for every out-of-range index, `activeAfter` across all three `remove` branches plus the last-entry case, and exhaustive sweeps proving the previously active entry stays active for **every** `(active, at)` insert pair and **every** `(active, from, to)` move triple over a four-element array. Ran `bun test` (85 pass across 12 files), `bun --bun run build` (pass), `bun run bundle` (ok; editor chunk 313,340 B raw / 101,750 B gzip).
**Next slice:** Slice 3b — the tab strip in `Playground.astro`, `state.tabs` replacing `state.source`, per-tab diagnostics mapping, and the `.pg-tabs [role=tab]` narrowing.
**Notes:**
1. **One test-spec correction.** The phase plan asks `doc-registry.test.ts` to assert the module's source matches no `/\bdocument\b/`. That forbids the English word, which a module whose subject is documents cannot satisfy in its own prose. The case asserts host *access* instead — `/\bdocument\.|\bwindow\.|\bglobalThis\.|localStorage/`, the dotted form the sibling `file-tabs.test.ts` purity case already uses — which is what actually catches a DOM reach.
2. The update listener mirrors `update.state` back into the registry on **every** update, not only `docChanged` ones: a selection move or a lint transaction on the visible file would otherwise be lost when that file is swapped out.
3. `makeState` seeds the theme compartment from the *current* content rather than from a captured extension, so a file created after a theme flip is born in the new palette.

## 2026-09-09 — Phase 2 — Slice 3b: the tab strip

**What shipped:** `Playground.astro` renders a `.pg-files` strip (server-rendered empty and `hidden`, `role="tablist"`, `aria-label="Project files"`) directly above `.pg-editor-wrap`, which gains `id="pg-editor-panel"` and `role="tabpanel"`. The island's `state.source` is retired: `state.tabs: FileTabsState` is the single source of truth, and the combined string is computed on demand with `toSource`. Every request is built from `requestFor(state.tabs.files)`; `rootInputBits()` reads `rootOf(state.tabs.files)`. Tabs render with a roving tabindex, a `root` chip on the last file, click selection and `ArrowLeft`/`ArrowRight`/`Home`/`End`/`Enter`/`Space` keyboard operation (every consumed key prevented). Selecting a tab calls `editor.showDocument(i)` and schedules nothing — the source did not change. The strip is unhidden only once `createEditor` resolves; on the fallback path the textarea keeps editing the combined text and re-splits on every keystroke. `Playground.astro`'s unscoped `[role=tab]` query is narrowed to `.pg-tabs [role=tab]`. Diagnostics are mapped once per tab through a new pure `mapPerTab` in `circ-diagnostics.ts`; rows for the active tab jump, rows for another file are inert until slice 6, and rows with no tab render disabled as before.
**Files touched:** `site/src/components/Playground.astro`, `site/src/styles/global.css`, `site/src/scripts/circ-diagnostics.ts`, `site/test/circ-diagnostics.test.ts`, `site/test/libcirc.test.ts`, `DOCS/STATUS.md`.
**Tests:** added `a two-tab project lists every diagnostic exactly once` (a tour-step-6 variant with an error injected into **each** file: the flat list has exactly `analysis.diagnostics.length` rows, every row lands on the tab whose file the compiler blamed, and the two per-tab editor lists partition the placed rows) and `no diagnostic is blamed on a builtin source for the shipped content`. Re-pointed `compiles tour step 6 with the last // name.circ file as root` through `fromSource`, adding `toSource(tabs) === step.source` and `requestFor(tabs.files)` equalling `requestFor(splitFiles(source))`. Ran `bun test` (87 pass across 12 files), `bun --bun run build` (pass), `bun run bundle` (ok; `/playground` eager graph 6.4 KB gzip against its 120 KB ceiling), and confirmed the served page carries the strip markup.
**Next slice:** Slice 4 — add, rename and delete.
**Notes:**
1. **`TODO(phase2)-C` is resolved: zero.** Across all 10 examples and all 7 tour steps, no diagnostic is ever blamed on a `<builtin>/…` path, so the list needs no filter and no summary line. The temporary spike was kept as a permanent regression case rather than deleted — Phase 0 made builtin sources appear in `analysis.files`, and this is what would notice if one ever started carrying diagnostics.
2. **One module moved to keep a claim provable.** The plan puts the per-tab row-building inside the island, then asks a `bun test` case to assert the flat list has exactly `analysis.diagnostics.length` rows — which `bun test` cannot import. The logic is therefore a pure `mapPerTab(files, analysis)` in `circ-diagnostics.ts`, returning both the per-tab lists and the flat rows; the island is two lines. This is decision 15's own rule (behaviour a test could reach lives in a module), applied where the plan had not.
3. The per-tab staleness guard is in place: the whole push is skipped when the file count changed in flight, and per tab when that body moved on.

## 2026-09-09 — Phase 2 — Slice 4: add, rename, delete

**What shipped:** `file-tabs.ts` gains `nextFileName` (first free `file<N>.circ`), `seedBody` (one comment line naming the file, with the `.circ` dropped so it is non-blank and is not itself a marker), `addFile` (inserts at `rootIndex`, activates the new tab), `nameError` (marker-shape and uniqueness, skipping the file's own index), `renameFile` (no-op when `nameError` is non-null), `canDelete` and `deleteFile` (refuses the last file; clamps `active` in all three branches). The strip gains a `+` button, a per-tab `×` close control, an inline rename input with `aria-invalid` and a `role="alert"` message, and a two-press delete confirmation armed by `Delete`/`Backspace` or by the close control and disarmed by `Escape`, by any other strip key, or after four seconds. Every operation drives the editor registry (`insertDocument` / `removeDocument` / `showDocument`) and announces its outcome through the existing `aria-live` status line, including the note that a rename does not rewrite `import` paths.
**Files touched:** `site/src/scripts/file-tabs.ts`, `site/test/file-tabs.test.ts`, `site/src/components/Playground.astro`, `site/src/styles/global.css`, `DOCS/STATUS.md`.
**Tests:** added eight cases — `addFile` landing before the root with the root unmoved, the new tab active and `joinConflicts` empty; `nextFileName` skipping taken names including a gap; `seedBody` being non-blank and not a marker; the `nameError` reject/accept table with self-rename allowed and another file's name refused; `renameFile` returning identity on every invalid input and reaching `requestFor`'s keys when valid; `deleteFile` refusing the last file; `deleteFile` promoting a new root and clamping `active` across all three branches plus the at-end case; and every reducer leaving its input untouched. Ran `bun test` (95 pass across 12 files), `bun --bun run build` (pass), `bun run bundle` (ok; `/playground` eager graph 7.4 KB gzip).
**Next slice:** Slice 5 — reorder and the root badge.
**Notes:**
1. The close control is `tabindex="-1"` and pointer-only, and the rename input's label may not be announced either: `role="tab"` is on ARIA's presentational-children list, so a nested control can be pruned from the accessibility tree. Both affordances are therefore carried in the tab's own accessible name and announced through `.pg-status`, and `Delete` on the focused tab is the keyboard path.
2. The rename input stops key propagation, or the strip's own arrow handling would move tabs while the reader is typing a name.
3. Manual checklist — **unrun** (no browser): `+` adds `file1.circ` before the root; `F2` renames with live validation; a second `Delete` removes; the four-second disarm; the last file refusing deletion.

## 2026-09-09 — Phase 2 — Slice 5: reorder and the root badge

**What shipped:** `file-tabs.ts` gains `moveFile(state, from, to)` — both indices clamp, the previously active *file* stays active wherever it lands, and moving a tab past the end is how the root changes, since the root is a position rather than a flag. The strip gains HTML5 drag-and-drop reorder (`draggable`, a `data-drop` hover marker, `drop` calling the same reducer) and `Alt+ArrowLeft` / `Alt+ArrowRight` keyboard reorder with focus following the tab. Every reorder drives `editor.moveDocument`, re-renders, and announces the new root through the `aria-live` status line when the last position changed. The `root` chip shipped in slice 3b and is always on the last tab.
**Files touched:** `site/src/scripts/file-tabs.ts`, `site/test/file-tabs.test.ts`, `site/src/components/Playground.astro`, `site/src/styles/global.css`, `DOCS/STATUS.md`.
**Tests:** added five cases — moving a tab to the end changing both `rootOf(files).name` and `requestFor(files).root`; an exhaustive sweep over every `(from, to)` pair proving the previously active file stays active and the set is only permuted; clamping for out-of-range and identity for a no-op move; `toSource` after a move re-emitting the markers in the new order with bodies untouched and `fromSource` reading the new root back; and `moveFile` leaving its input untouched. Ran `bun test` (100 pass across 12 files), `bun --bun run build` (pass), `bun run bundle` (ok; `/playground` eager graph 7.7 KB gzip).
**Next slice:** Slice 6 — per-tab diagnostic counts and cross-file jumps.
**Notes:**
1. `Alt+Arrow` is checked before the plain arrows and always calls `preventDefault()`. `Alt+ArrowLeft` is the browser's Back on Windows and Linux, and this phase persists nothing, so a single unhandled press would discard the reader's whole session.
2. Manual checklist — **unrun** (no browser): dragging a tab past the root and watching the badge follow; `Alt+ArrowRight` doing the same from the keyboard with focus following the tab.

## 2026-09-09 — Phase 2 — Slice 6: per-tab diagnostic counts and cross-file jumps

**What shipped:** `file-tabs.ts` gains `TabCounts` and `countsByFile(files, analysis)`, which resolves `analysis.files[]` to tab indices and drops every id that is not a current tab — builtin macro sources, and stale paths from a reply that outlived a rename — so a late reply degrades to no badge rather than a wrong one. Each tab renders an error or warning badge and folds the counts into its accessible name (`half_adder.circ, root file, 2 errors`, or `no diagnostics`); a tab that `joinConflicts` names gains a warning marker, and the strip's `role="alert"` span says in one line why that file cannot be written back. The diagnostics list now jumps across files: a row whose file is not the active tab selects that tab, calls `editor.showDocument` and then `editor.select` in the same statement, which works because `showDocument` is synchronous. On the fallback surface the jump only focuses the textarea, since file-local offsets do not address a buffer holding every file at once. Two decision entries landed.
**Files touched:** `site/src/scripts/file-tabs.ts`, `site/test/file-tabs.test.ts`, `site/src/components/Playground.astro`, `site/src/styles/global.css`, `DOCS/decisions/playground.md`, `DOCS/decisions/index.md`, `DOCS/STATUS.md`.
**Tests:** added four cases — errors and warnings split across two tabs; a `<builtin>/…` id, a `/playground/` path naming no current tab, and an id the analysis never listed all contributing nothing; an unrecognised severity counting as a warning; and all-zero rows for a null analysis, an empty diagnostic list and an empty file set. Ran `bun test` (104 pass across 12 files), `bun --bun run build` (pass), `bun run bundle` (ok; `/playground` eager graph 8.3 KB gzip against its 120 KB ceiling).
**Next slice:** none — Phase 2 is complete. Phase 3 (`DOCS/PLANS/PHASE_3_workbench.md`) is the next phase and needs the human's go-ahead.
**Notes:** Manual checklist — **unrun** (no browser): breaking `root.circ` and seeing a red `1` on its tab while `half_adder.circ` is active, then clicking that row and landing on the offending text in the file that just became visible.

## 2026-09-09 — Phase 2 — close-out

**What shipped:** Phase 2 is complete in seven commits (`688aa1a`, `06423d0`, `7c66e39`, `4c69a3f`, `b76b781`, `89d0bb7`, `bafd3aa`). `/playground` edits a multi-file project as tabs over the unchanged `// <name>.circ` marker format: `joinFiles` is its proven inverse, `file-tabs.ts` is the pure model, `doc-registry.ts` plus one `EditorState` per file give per-file undo and selection, and the strip supports click and keyboard selection, add, inline rename, two-press delete, drag and `Alt+Arrow` reorder, per-tab diagnostic badges and cross-file diagnostic jumps. The root is the last tab and reorder is how it changes.
**Files touched:** none beyond the seven slices; this entry is the phase's measurement of record.
**Tests:** `bun test` 104 pass across 12 files, up from 56 at the end of Phase 1. `bun --bun run build` green; `bun run bundle` green with `/playground`'s eager graph at 8.3 KB gzip against its 120 KB ceiling; `zig build test-all` untouched by this phase.
**Next slice:** Phase 3 (`DOCS/PLANS/PHASE_3_workbench.md`) — the app layout, splitters, the `circ.playground.v1` store, the status bar and the two-stage debounce — which needs the human's go-ahead as a new phase.
**Notes:** The whole manual checklist for this phase is **unrun**: no browser has been available in any session on this branch, so nobody has seen a tab render, a rename commit, a drag land or a cross-file jump. Every reducer, the offset mapping and the registry arithmetic are proven under `bun test`; the wiring between them and the DOM is not. The dev server is running at `http://localhost:4321` for exactly that check. Three plan corrections are recorded in the slice entries above: the illegal-name failure mode (slice 1), the `doc-registry` purity regex (slice 3a), and moving the per-tab row-building into a pure module so its stated test could exist at all (slice 3b).

## 2026-09-09 — Phase 3 — Slice 1: the `app` layout variant on `Base.astro` + CSS

**What shipped:** `Base.astro`'s `Props` gains `layout?: 'default' | 'app'`, defaulted to `'default'` and stamped on `<body>` only for `'app'`, so every other page's markup is byte-identical. `playground.astro` opts in and moves its `<h1>` and intro prose into a `<header class="pg-head">` with the paragraph inside a collapsed `<details class="pg-about">`, its text unchanged — the "nothing leaves the page" promise stays on the page. `global.css` gains `--danger` in both token blocks and a `[data-layout='app']` block: the body becomes a `100dvh` grid (with a `100vh` fallback line first) of `auto 1fr auto` with `overflow: hidden`; `main` loses its padding and `max-width` and becomes its own `auto minmax(0, 1fr)` grid with `min-height: 0`; the footer compacts while keeping `.site-footer-row` and its mobile-only theme toggle; `.pg` becomes the workbench grid with `margin: 0`; the panes and the editor's CodeMirror host get `min-height: 0`, with `.cm-editor { height: 100% }` and `.cm-scroller { overflow: auto }` scoped to the app layout so Phase 7's live editors are never matched. Below 800px the lock is released and the panes get `min-height: 60vh` so the page scrolls instead of crushing them. No `.lc-mount` selector is touched.
**Files touched:** `site/src/layouts/Base.astro`, `site/src/pages/playground.astro`, `site/src/styles/global.css`, `site/test/app-layout.test.ts` (new), `DOCS/STATUS.md`.
**Tests:** added seven cases in `site/test/app-layout.test.ts` — the prop's declaration, default and conditional stamping; exactly one page under `src/pages/**` opting in and it being `playground.astro`; the set of comment-stripped selectors mentioning `.lc-mount` equalling the frozen three with none of them carrying `data-layout`; every app-layout rule starting with the attribute selector; the `100vh` fallback preceding `100dvh`; `--danger` in both token blocks; and the no-network promise still in the page inside the `<details>`. Ran `bun test` (111 pass across 13 files), `bun --bun run build` (pass), `bun run bundle` (ok).
**Next slice:** Slice 2 — `site/src/scripts/splitter.ts` and the draggable, keyboard-operable divider.
**Notes:**
1. The frozen `.lc-mount` set is three selectors, not the five the plan names: the mobile block's `.lc-mount` and `.lc-mount canvas` are byte-identical strings to the desktop ones, so the de-duplicated set has three members. The test compares sets, which is the property that matters — no *new* selector may mention it.
2. Headroom for slices 2 to 5, measured now: `/playground`'s eager graph is 8.3 KB gzip against its 120 KB ceiling, so the three new modules have room by a wide margin. That resolves the phase's headroom spike in the affirmative before any of them is written.
3. Manual checklist M1a and M3 (nav and stacking) — **unrun** (no browser).

## 2026-09-09 — Phase 3 — Slice 2: the splitter component and its keyboard contract

**What shipped:** `site/src/scripts/splitter.ts` — `createSplitter` plus the five pure helpers `clampRatio`, `effectiveBounds`, `ratioFromPointer`, `stepRatio` and `ariaValues`. `.pg-panes` becomes a three-track grid driven by the unitless `--pg-split-main`, holding the first pane's share of the space left over after the divider's own 8px, so the tracks always sum to exactly 100%. The divider is a `role="separator"` element with `tabindex="0"`, `aria-orientation`, `aria-controls` naming both pane ids, live `aria-valuenow`/`min`/`max`/`text`, and `touch-action: none`. Pointer drags use pointer capture, so a drag that crosses the simulation canvas is never stolen by the renderer's own pointer handling; `Escape` and `pointercancel` restore the position recorded at `pointerdown`; double-click resets. A `ResizeObserver` re-clamps and refreshes the ARIA values on a container resize but never commits, so a transient narrow window cannot overwrite a preference. The island mounts it at `initial: 0.5`; nothing is persisted yet.
**Files touched:** `site/src/scripts/splitter.ts` (new), `site/test/splitter.test.ts` (new), `site/src/components/Playground.astro`, `site/src/styles/global.css`, `DOCS/STATUS.md`.
**Tests:** added nine cases — `clampRatio` holding bounds, idempotent, and refusing to propagate `NaN` into a custom property; `effectiveBounds` tightening by `minPanePx / sizePx` at 1200px and 800px; the collapse to the midpoint below `2 × minPanePx` with an exhaustive check that the bounds never invert; the zero-size fallback to the configured range; `ratioFromPointer` mapping the container and clamping on both sides with no `NaN` at zero width; the full vertical and horizontal key tables including `Shift`, `Home`/`End`, the wrong-axis arrows and six other keys returning `null`; an exhaustive sweep proving every keyboard result lands inside the bounds; and `ariaValues` producing integer percents and naming both panes. Ran `bun test` (120 pass across 14 files), `bun --bun run build` (pass), `bun run bundle` (ok; `/playground` 9.4 KB gzip).
**Next slice:** Slice 3 — `playground-store.ts`, ratio persistence and the stacked mobile layout.
**Notes:**
1. A `body:not([data-layout='app']) .pg-splitter` guard was written and then removed: my own slice-1 test rejected it, correctly. It is also dead code — `.pg-splitter` exists only inside `.pg`, which only `/playground` renders, and that page is always `layout="app"`. The scoping invariant stays strict.
2. The wrong-axis arrows return `null` rather than being swallowed, so `ArrowUp`/`ArrowDown` on the vertical divider still scroll the page. That is why `stepRatio` returns a nullable number instead of the ratio itself.
3. Manual checklist M2 — **unrun** (no browser).

## 2026-09-09 — Phase 3 — Slice 3: ratio persistence and the stacked mobile layout

**What shipped:** `site/src/utils/playground-store.ts` — the complete `circ.playground.v1` envelope (`version`, `scratch`, `activeId`, `activeFile`, `layout.ratios`, `settings`, `tab`), `defaultEnvelope`, `defaultSettings`, `normalize`, `readEnvelope`, `writeEnvelope` (cap trim by LRU, quota eviction, retry exactly once, session disable), `evictOldest`, `browserStorage` (a probe inside `try/catch`, since access itself throws in a private window), `describeNote` and `createStore` (500 ms debounced writer, buffered notes delivered on subscribe), all pure over an injected `StorageLike` and `TimerLike`. The island creates the store, seeds the splitter from `layout.ratios.main`, commits through `store.update` on a user-driven settle only, flushes on `pagehide` and on `visibilitychange` to hidden, and routes every note into the status line through `describeNote`. A `matchMedia('(max-width: 800px)')` listener hides the divider from the page and the tab order below the breakpoint, where slice 1's CSS already releases the viewport lock and gives each pane `min-height: 60vh`.
**Files touched:** `site/src/utils/playground-store.ts` (new), `site/test/playground-store.test.ts` (new), `site/src/components/Playground.astro`, `DOCS/STATUS.md`.
**Tests:** added 14 cases against a fake storage and a hand-advanced clock — defaults and the seven fields; `normalize` as a fixed point; unknown keys dropped including a snake_case one that must never reach an options object; version and corruption resets through both `normalize` and the real read path; a throwing storage disabling with `unavailable`; every normalisation limit (scratch trimmed newest-first, an oversized source dropping its project, ratios outside `(0,1)` dropped, `truthTableCap` clamped both ways, bad enums and a bad tab falling back, a dangling `activeId` nulled while a content id survives); three updates inside the window producing one write and `flush` cancelling the pending timer; the envelope cap trimming by LRU; a quota error evicting the oldest and retrying exactly once; a permanent quota error disabling the session, emitting one note, attempting no further writes and still tracking state in memory; nothing left to evict disabling rather than looping; a non-quota error disabling as unavailable; `evictOldest` picking correctly; buffered notes delivered on subscribe; and `describeNote` naming the evicted project. Ran `bun test` (134 pass across 15 files), `bun --bun run build` (pass), `bun run bundle` (ok; `/playground` 11.2 KB gzip).
**Next slice:** Slice 4 — the status bar and `pipeline.ts`'s pure half.
**Notes:**
1. `browserStorage()` probes with a write and a remove rather than only reading, because some browsers admit the object and throw on first use.
2. The store keeps mutating its in-memory envelope after persistence is disabled, which a test pins: the page must keep working in a private window, it just does not remember anything.
3. Manual checklist M1b and M3 (divider clause) — **unrun** (no browser).

## 2026-09-09 — Phase 3 — Slice 4: the status bar

**What shipped:** `site/src/scripts/pipeline.ts` with its pure half — `statusFor`, `formatBytes`, `plural`, and (written here, consumed in slice 5) `shouldCompile` and `outputsShouldClear`. The workbench gains a `.pg-statusbar` last row carrying a state dot, a label, a detail span, the compiler identity, an empty `.pg-status-actions` slot for Phase 4's Share button, and the existing `.pg-status` element, moved out of the editor pane **unchanged in class and in `aria-live`** so Phase 2's file-tab announcements and slice 3's store notes keep targeting it. The identity is server-rendered from the committed manifest (`circ 0.0.2 · 67085c7 · CIRF v3`) so the bar is never blank before the handshake, and the island overwrites it from `circ_version()` afterwards. The island's eight pipeline `status.textContent` assignments become one `renderStatus()` over a single `StatusInput` record; the label and the dot are written only when the state *kind* changes, and the per-keystroke text lives in the non-live detail span. `--danger` drives the error dot and the compiling pulse honours `prefers-reduced-motion`.
**Files touched:** `site/src/scripts/pipeline.ts` (new), `site/test/pipeline.test.ts` (new), `site/src/components/Playground.astro`, `site/src/styles/global.css`, `DOCS/STATUS.md`.
**Tests:** added eight cases — a failure outranking loading, in-flight work and an error count all at once; the loading → idle → compiling ladder with work in flight outranking a stale error count; pluralised errors with their warnings; `live` carrying the artifact size and warnings, and staying `live` with no artifact yet; the stale hint appearing in every state that can be stale and nowhere else; `formatBytes` across the KB boundary and for `0`, negative and `NaN`; `shouldCompile` false only for a fresh analysis with errors and true for a stale one; and `outputsShouldClear` false for a body edit but true for an added, removed, renamed or reordered file. Ran `bun test` (142 pass across 16 files), `bun --bun run build` (pass), `bun run bundle` (ok; `/playground` 11.7 KB gzip).
**Next slice:** Slice 5 — the two-debounce rewrite with a sequence guard per stage, `getSharedClient`, and the dimmed last-good outputs.
**Notes:**
1. `pipeline.ts` is one module beyond decision 15's Phase 3 list, added under that decision's own governing rule: the status machine is behaviour a test could reach, and `bun test` cannot import an `.astro` island.
2. `.pg-status` moved rather than being replaced, so the island's existing `querySelector` assertion still resolves. A `!` assertion aimed at a deleted node would have thrown on the first line of `init()` and taken the whole island down, and neither the test suite nor the build executes that code.
3. Manual checklist M4 — **unrun** (no browser).

## 2026-09-09 — Phase 3 — Slice 5: the two-debounce rewrite with both sequence guards

**What shipped:** `pipeline.ts` gains `Stage` (one debounce plus one monotonic counter, claimed at fire time), `ANALYZE_DEBOUNCE_MS = 120` and `BUILD_DEBOUNCE_MS = 350`. `getSharedClient(wasmUrl)` lands on `libcirc-client.ts` and the island adopts it. `DEBOUNCE_MS` and `state.gen` are gone with every reader: `fire()` is split into `runAnalyze` and `runBuild`, each re-checking its own stage after **every** await, and the follow-on `preview` / `truth_table` calls re-check the build stage's sequence — the guard the langlang playground omits on its second stage. A tab switch re-uses `buildStage.seq` instead of claiming, so a compile in flight stays current. The build consults but never waits on the analysis through `shouldCompile`. Outputs now dim rather than vanish: the status-1 and failure paths leave `state.artifact` and the canvas alone and set `data-stale`, `runPreview` and `runTruth` keep their last good text on a refusal and route the reason to the status bar, and `hooks.onArtifact(null, 'files-changed')` is called from exactly one place — the `outputsShouldClear` branch — so the simulator's teardown runs on a file-set change and never on a syntax error. The artifact's size is captured at reply time, before the buffer can be transferred onward. Five decision entries landed.
**Files touched:** `site/src/scripts/pipeline.ts`, `site/src/scripts/libcirc-client.ts`, `site/src/components/Playground.astro`, `site/src/styles/global.css`, `site/test/pipeline.test.ts`, `site/test/shared-client.test.ts` (new), `DOCS/decisions/playground.md`, `DOCS/decisions/index.md`, `DOCS/STATUS.md`.
**Tests:** added seven `Stage` cases under a hand-advanced clock — five schedules inside the window running once; the sequence claimed at fire time and not at schedule time; an in-flight call staying current while a later timer is merely pending and going stale once it fires; a **second-stage** check failing after the first one passed; `flush` cancelling and running now; `cancel` running nothing and `claim` moving the counter with no timer; and the two stages carrying independent counters. Plus three `getSharedClient` cases (memoised per URL, distinct per URL, construction spawning no worker). Ran `bun test` (152 pass across 17 files), `bun --bun run build` (pass), `bun run bundle` (ok; `/playground` 11.7 KB gzip against its 120 KB ceiling).
**Next slice:** none — Phase 3 is complete. Phase 4 (`DOCS/PLANS/PHASE_4_workspace_and_share.md`) is next and needs the human's go-ahead.
**Notes:**
1. `getSharedClient` is Phase 7 slice 1's first half, pulled forward as the plan directs; when Phase 7 begins, its slice 1 reduces to lifting `fnv1a` into its own module. `site/test/shared-client.test.ts` is named exactly as Phase 7 expects, so that phase inherits the file rather than creating a near-duplicate.
2. `hooks.onArtifact` now takes a reason, so the simulate pane says "Compile a circuit first." on a file-set clear and "Fix the errors to simulate." on a build failure. Widening rather than replacing keeps the by-name pin replay untouched.
3. Manual checklist M5 — **unrun** (no browser). Nobody has watched the outputs dim, the divider drag, or the status bar change state.

## 2026-09-09 — Phase 3 — close-out

**What shipped:** Phase 3 is complete in five commits (`a4570f3`, `72bdc48`, `062c0ee`, `f884193`, `dcd2d84`). `/playground` is a viewport-locked workbench: an `app` layout variant on `Base.astro`, a draggable and keyboard-operable divider whose position persists in a schema-versioned `localStorage` envelope, a status bar that is a pure function of one record, and a two-stage pipeline whose stale replies are dropped at every await and whose last good outputs dim instead of vanishing.
**Files touched:** none beyond the five slices; this entry is the phase's measurement of record.
**Tests:** `bun test` 152 pass across 17 files, up from 104 at the end of Phase 2. `bun --bun run build` green; `bun run bundle` green with `/playground`'s eager graph at 11.7 KB gzip against its 120 KB ceiling — no ceiling raised, and the three new modules cost 3.4 KB gzip between them. `zig build test-all` untouched by this phase.
**Next slice:** Phase 4 (`DOCS/PLANS/PHASE_4_workspace_and_share.md`) — the workspace sidebar, scratch projects filling this phase's envelope, `#src=` share links and the Open in playground buttons — which needs the human's go-ahead as a new phase.
**Notes:** The whole manual checklist M1a through M5 is **unrun**. No browser has been available in any session on this branch, so nobody has seen the viewport lock hold, the divider drag, the panes stack below 800px, the status bar change state, or the outputs dim while a source is broken. Every helper, every store path including quota and private-window failure, and both sequence guards are proven under `bun test`; the wiring between them and the DOM is not. The dev server at `http://localhost:4321` is where that check happens. Two of the phase's three spikes were resolved without a browser: the bundle headroom (11.7 KB against 120 KB, answered in slice 1 before the modules were written) and the two "does the host need a poke on resize" questions, both answered no from the installed packages. The remaining two — whether the mobile nav dropdown survives inside the `100dvh` grid, and whether the live region is quiet enough for a screen reader — need a browser and an assistive-technology pass respectively, and are recorded here as open.
