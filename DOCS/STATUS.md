# Status — Native Memories (ROM & RAM)

Append-only log, one entry per shipped slice. Newest at the bottom. See `DOCS/PLANS_PROMPT.md` for the phase index and `DOCS/PLANS/PHASE_<N>_*.md` for each phase's spec.

## 2026-09-07 — Phase 0 — Slice 1: IR variant + resolver + compile-forced arms

**What shipped:** `ir.ComponentKind.memory` (`Memory { mode, data_width, addr_width, arg_count, type_width_given }`); the resolver recognises `rom`/`ram` before primitives and import aliases and resolves both `width_args` through `widthFromSpec` (parametric `[W, A]` binds); every exhaustive `ComponentKind` switch gained its arm — dump text (`kind=rom[W=8,A=4]`), analyze symbol kind + hover (`rom[8,4] code`), port validity, `endpointWidth` via the new `memoryPortWidth` helper, `MemoryNotYetSupported` in both topology serializers (temporary, removed in Phase 2), `MemoryUnsupportedInEmitZig` in emit-zig (permanent). Every artifact mode exits 1 with no output file; `--inspect` and `--analyze` resolve memories with zero diagnostics.
**Files touched:** `lib/ir/types.zig`, `lib/ir/resolver.zig`, `lib/validator/passes/memory_validation.zig` (new, helper only), `lib/validator/passes/port_validation.zig`, `lib/validator/passes/sub_circuit_validation.zig`, `lib/cli/inspect_dump.zig`, `lib/analyze/analyze.zig`, `lib/topology/serializer.zig`, `lib/topology/full_serializer.zig`, `lib/emit/build_fn.zig`, `lib/emit/project.zig`, `build.zig` (memory_validation module wiring), `tests/helpers/ir_dump.zig`, `tests/syntax/translate_test.zig`, `tests/ir/resolver_test.zig`, `tests/validator/structural_passes_test.zig`, `tests/validator/codes_snapshot_test.zig`, fixtures `circuits/{rom,ram}_basic.circ`, `expected-ast/{rom,ram}_basic.txt`, `expected-ir/{rom,ram}_basic.txt`, `projects/memory_parametric/{root,mem_wrap}.circ`, `expected-diagnostics/memory_parametric_clean.txt`
**Tests:** added `rom/ram resolve as memory components before import aliases`, `memory width args bind through parametric width bindings`, `memory records arity and type-position width for the validator` (resolver), `analyze: memory declarations resolve to rom/ram symbols with hover`, `memoryPortWidth contract` (wired into the structural-passes root), AST/IR golden entries `rom-basic`/`ram-basic`, project snapshot `memory_parametric` (clean); ran `zig build test`, result pass (402/402; baseline 397/397, every pre-existing golden byte-identical)
**Next slice:** Slice 2 — `E017`/`E018` in `codes.zig` + `memory_validation.run` wired into `validator/run.zig`, fixtures for each trigger, snapshot rows, and the `E001-E016` → `E001-E018` docs sweep.
**Notes:** The CLI currently prints the raw error names (`topology serialization failed: MemoryNotYetSupported`, `emission failed: MemoryUnsupportedInEmitZig`); the friendly `rom/ram are not yet supported in this mode` line is slice 4. `memory_validation.zig` is imported only by `sub_circuit_validation` for now; slice 2 adds it to `validator/run.zig`, slice 3 to `port_validation`. Zig's exhaustive switches forced the serializer/emit arms into this slice; the else-guarded passes (`required_input`, `combinational_loop`, `name_collision`, `unused_import`) are untouched until slice 3.

## 2026-09-07 — Phase 0 — Slice 2: E017/E018 + code registry

**What shipped:** `E017` (memory parameter list malformed) and `E018` (memory width out of range) appended to `codes.zig` with their `templates` rows; `memory_validation.run` wired into `validator/run.zig` right after `name_collision`. `E017` fires once per component for `arg_count != 2` (0 args carry a `write 'rom m[W, A](...)'` hint; the `rom a, b` identifier-list form reports per name) and for a type-position width (`rom[8] m[8, 4]`); `E018` fires per out-of-range width (`W` in 1..64, `A` in 1..16) and is suppressed when `E017` fired. Every doc that advertised `E001-E016` now says `E001-E018`, and CLAUDE.md's code table gained the two rows.
**Files touched:** `lib/validator/codes.zig`, `lib/validator/passes/memory_validation.zig`, `lib/validator/run.zig`, `build.zig`, `tests/validator/run_test.zig`, `tests/validator/codes_snapshot_test.zig`, `tests/validator/structural_passes_test.zig`, fixtures `circuits/E017_memory_{no_widths,one_width,type_width,ident_list}.circ`, `circuits/E018_memory_width_range.circ` and their `expected-diagnostics/` goldens; docs `README.md`, `CLAUDE.md`, `DOCS/{analyze-api,architecture,circuit-format,index,language,sim-protocol}.md`, `site/src/pages/reference/circuit-format.md`
**Tests:** added `memory_validation: rejects wrong arity`, `memory_validation: rejects type-position width`, `memory_validation: width bounds` (inline), five `run_test` fixture entries and five `codes_snapshot` single-file rows; ran `zig build test`, result pass (405/405; every pre-existing golden byte-identical)
**Next slice:** Slice 3 — reused-code arms: `required_input` (`E004`), `port_validation` per-port `E014` via `memoryPortWidth`, `combinational_loop` policy (`ram` breaks cycles, `rom` does not), `name_collision` + `scan_imports` reservation (`E006`/`E011`), with fixtures for each.
**Notes:** **Zig collects tests only from a test root's own module.** Slice 1's `test { _ = memory_validation; }` in `structural_passes_test.zig` never ran the pass's inline tests (the +1 it produced was the anonymous block itself); it is removed and `memory_validation_mod` is now its own `addTest` root in `build.zig`. This is the same mechanism Phase 1 slice 1 applies to `lib/circuit.zig`. `TODO(phase0)` on the identifier-list form is resolved: one `E017` per name, because the translator already emits one component per name (`translate.zig:622-631`).

## 2026-09-07 — Phase 0 — Slice 3: reused-code arms and name reservation

**What shipped:** `required_input` reports `E004` for a memory's missing ports (rom: `addr`; ram: `addr`, `din`, `we`, `clk`); `port_validation` computes `E014` through a per-endpoint `endpointWidth` helper (primitives keep `Component.width`, memories use `memoryPortWidth`, everything else has no in-module contract) so `addr`/`din`/`we`/`clk`/`out` are checked at their own widths; `combinational_loop` treats `ram` as cycle-breaking and `rom` as combinational; `rom`/`ram` are reserved in `name_collision.isBuiltinName` (`E006`) and `scan_imports.isBuiltinAlias` (`E011`). `memoryPortWidth` returns no contract while the `[W, A]` list is malformed, so `E017` fixtures do not cascade into placeholder-width `E014`s.
**Files touched:** `lib/validator/passes/{port_validation,required_input,combinational_loop,name_collision,memory_validation}.zig`, `lib/resolver/scan_imports.zig`, `build.zig` (`memory_validation_mod` moved ahead of `port_validation_mod`), `tests/validator/{run_test,codes_snapshot_test}.zig`, fixtures `circuits/{E002_memory_unknown_port,E004_memory_missing_ports,E014_memory_port_width,E006_shadows_memory,E008_rom_loop,clean_ram_feedback}.circ`, `projects/E011_memory_alias/{root,rom}.circ`, their `expected-diagnostics/` goldens, and two slice-2 goldens that legitimately gained lines (`E017_memory_ident_list` now also reports `E004 'addr'` per name; `E018_memory_width_range` also reports the real `E014` against the user's width 20)
**Tests:** added seven `run_test` rows, six `codes_snapshot` single-file rows and one project row, and a malformed-list case in `memoryPortWidth contract`; ran `zig build test`, result pass (405/405; every golden outside the two named above byte-identical)
**Next slice:** Slice 4 — `reportBackendError` in `cmd/circ-compile/main.zig` mapping `MemoryNotYetSupported`/`MemoryUnsupportedInEmitZig` to `rom/ram are not yet supported in this mode`, CLI rejection tests for the five artifact modes, `--inspect` AST `width_args` line + `expected-inspect/rom_basic.txt`, `DOCS/language.md` §3.5.
**Notes:** `E008_rom_loop` and `clean_ram_feedback` deliberately loop through a `wire`, not a gate — `and`/`not` are already cycle-breaking (`combinational_loop.zig:15-23`), so a loop through a gate would never reach the memory policy. `E011_memory_alias` shows the full cascade for an existing `import rom`: the alias collides (`E011`), and the body's `rom r(x = …)` is then read as a memory declaration, so the fix for such a file is renaming the alias.

## 2026-09-07 — Phase 0 — Slice 4: CLI rejection surface, inspect polish, docs (phase complete)

**What shipped:** `reportBackendError` in `cmd/circ-compile/main.zig` replaces the twelve `<context>: <ErrorName>` prints at every backend `catch` (topology build, min/full serialization, emission) and maps `MemoryNotYetSupported` / `MemoryUnsupportedInEmitZig` to the single line `rom/ram are not yet supported in this mode`; `--inspect`'s parse-tree dump prints `width_args=[8, 4]` on component lines; `DOCS/language.md` §3.5 documents the declaration shape, port table, width bounds, reserved names, and the not-yet-compilable status.
**Files touched:** `cmd/circ-compile/main.zig`, `lib/cli/inspect_dump.zig`, `tests/cli/integration_test.zig`, `tests/fixtures/expected-inspect/rom_basic.txt`, `DOCS/language.md`, `DOCS/STATUS.md`
**Tests:** added `cli inspect memory fixture exits 0 and matches golden stdout` and `cli memory sources are rejected in every artifact mode` (five spawned invocations: `-o`, `--emit-zig -o`, `--preview`, `--truth-table`, `--sim`; each exits 1, empty stdout, the rejection line on stderr, no output file); ran `zig build test`, result pass (407/407)
**Next slice:** Phase 1 slice 1 — engine test root in `build.zig` plus the latent `lib/circuit.zig:1925` assertion fix (see `DOCS/PLANS/PHASE_1_engine_memories_and_image_codec.md`).
**Notes:** Phase 0 is complete: every row of its Slices table shipped. Both `TODO(phase0)` markers are resolved — `dead_code`/`name_resolution` need no memory arms (`rom_basic` inspects with 0 warnings), and the identifier-list form reports per name. The `--analyze` zero-diagnostics proof lives in the inline `analyze` test rather than a spawned-CLI case, because the CLI test helper cannot feed stdin (`Child.run` uses `.Ignore`). Phase 2 must remove the `MemoryNotYetSupported` arms **and** flip the rejection test in the same slice, or `zig build test` goes red between commits (Phase 2 spec, warning 6).

## 2026-09-07 — Phase 1 — Slice 1: engine test root

**What shipped:** `lib/circuit.zig` is now its own `addTest` root in `build.zig` (`engine_tests`, attached to `test_step` after the interpreter tests), with a `test { _ = transport; }` hook so `transport.zig`'s inline test is collected through the relative import. The latent slice test at `lib/circuit.zig:1925` expected the raw shifted value `0b11`; `Pool.write` canonicalises `value & defined` on commit, so the engine returns `0b01` — the expectation and its explanatory comment now say so.
**Files touched:** `build.zig`, `lib/circuit.zig`
**Tests:** no new tests written; 52 previously never-executed tests now run (50 engine + the hook block + 1 transport). Verified the HEAD version of `circuit.zig` fails that one test when run as a root (standalone `zig test` with a scratch `build_options`), and the fixed version passes; ran `zig build test`, result pass (459/459)
**Next slice:** Phase 1 slice 2 — `lib/memimage.zig` raw image codec (`bytesPerWord`, `maxImageSize`, `validate`, `decode`, `encode`, `MemoryImageError`) with `pub const memimage` re-exported from `circuit.zig`.
**Notes:** `memory.zig`, `transport.zig`, and `log.zig` are relative imports of `circuit.zig`, so they live in the same module and their tests are collectable from this root; the pass modules under `lib/validator/passes/` are separate `createModule`s and are not (see the slice-2 note in Phase 0).

## 2026-09-07 — Phase 1 — Slice 2: raw image codec

**What shipped:** `lib/memimage.zig` — the headerless little-endian raw image codec (decision 2): `bytesPerWord`, `maxImageSize`, `validate` (checks length is a whole number of words → word count fits `2^A` → no bits at or above W; touches no plane), `decode` (validate, then replace-all: loaded words fully defined, the tail undefined; a rejected image leaves both planes untouched), `encode` (writes `value & defined`), and `MemoryImageError { LengthNotWordMultiple, WordExceedsWidth, TooManyWords }`. `lib/circuit.zig` re-exports it as `pub const memimage` and collects its tests through the engine root.
**Files touched:** `lib/memimage.zig` (new), `lib/circuit.zig`
**Tests:** added eight inline tests (`bytesPerWord and maxImageSize`, `round-trip` incl. W=64, `W=64 has no padding bits`, `short image leaves tail undefined` incl. the empty image, `LengthNotWordMultiple leaves planes untouched`, `WordExceedsWidth`, `TooManyWords`, `encode writes value & defined`); `zig test lib/memimage.zig` standalone 8/8; ran `zig build test`, result pass (467/467). `zig-out/lib/circ-runtime.wasm` rebuilt from the wasm module graph with no `build.zig` change, so the relative import resolves on both targets.
**Next slice:** Phase 1 slice 3 — `ComponentType.memory` with asynchronous read (`MemoryMode`, `MemCells`, payload, plane allocation in `createComponent`, `connect` arms, recalc arm without the RAM block, `memoryReadOut`, `memoryCells`, `transport.kindByte` 8/9, `toKind` deleted, doc corrections).
**Notes:** Nothing references the codec yet; Phase 1 slice 5's `memoryLoadImage`/`memoryStoreImage` are its first engine callers, Phase 2's `memBuffer` sizes its staging buffer from `maxImageSize`, and Phase 3 reaches `validate` through `engine.memimage`.
