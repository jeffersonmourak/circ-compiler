# Phase 5 — Decision records and docs close-out

> **Dependencies:** Phases 0–4 complete and committed (`DOCS/PLANS_PROMPT.md` Phase Index), which in turn means PR #79 (native memories) is merged and this branch is rebased on it. This phase adds no compiler, library, or site behaviour — it records the ten locked decisions, sweeps every contributor-facing doc, and closes the plan. Every line number below is cited against the tree PR #79 produces (the `memories` worktree) unless marked `(main)`; re-anchor with the quoted text, because Phases 1–4 shift lines.
> **Warnings:** (1) The proof command in the Phase Index is written with `\|` — in ripgrep that is a *literal* pipe, so `rg -n "CGo\|parser\.a\|CParser\|go build"` matches nothing and would pass vacuously; the real sweep is `rg -n "CGo|parser\.a\b|CParser|go build" --glob '!DOCS/archive/**' --glob '!DOCS/PLANS/**' --glob '!DOCS/STATUS.md' --glob '!DOCS/PLANS_PROMPT.md'` (the `\b` excludes `tools/bench/format-delta-comment.py:147` `parser.add_argument(`, the one argparse false positive; `rg` honours `.gitignore`, so `zig-out/`, `.zig-cache/`, `node_modules/` never count — but so does the *tracked* mirror `site/src/pages/reference/` (ignored by `site/.gitignore`), which needs a second `rg --no-ignore` pass; the generated `lib/parser/parser.zig` contains none of the four strings — verified on a `langlang v0.0.13-zig.2` output). (2) Plan-file collision: PR #79 carries its own `DOCS/PLANS_PROMPT.md`, `DOCS/PLANS/PHASE_*.md`, and `DOCS/STATUS.md` (memories STATUS is 187 lines, last entry "Follow-up — bench regression fixed, ROM fixtures in the bench corpus", `Next slice: none.`); by the time this phase runs, `DOCS/archive/plan-memories.md` must exist and `DOCS/STATUS.md` must be *this* initiative's log — check `head -1 DOCS/STATUS.md` before the first slice. (3) The `.peg` header comment changes the grammar's bytes: whatever Phase 2's `version()` reports as `grammar_sha256` and Phase 4 committed in `site/public/wasm/libcirc.manifest.json` moves with it, so slice 1 refreshes the committed artifact (see Open Questions). (4) `site/src/pages/reference/*.md` are generated by `bun site/scripts/sync-reference.ts` (`syncDoc`, `:16-46`) from `DOCS/{language,getting-started,circuit-format,wasm-api,preview}.md` (the `docs` array, `site/scripts/lib/site-config.ts:15-46`) — edit `DOCS/` and re-run the script; never hand-edit the mirror. (5) Git conduct per `CLAUDE.md`: stage by path, propose every commit message, wait; Conventional Commits titles, no phase prefixes, no trailers. (6) **Phases 1 and 3 already touched several of the files below**: Phase 1 slice 2 (`PHASE_1_vendor_zig_parser.md:17,45-47`) rewrote `CLAUDE.md:7,24,26,38,39,72`, `README.md:40-41`, `DOCS/index.md:85-88` and removed `.gitignore:6-7`, and Phase 1 explicitly deferred `tooling.md`, `decisions/index.md`, `architecture.md`, `circuit-format.md`, the site mirror, the rest of the CLAUDE.md pipeline diagram and the `.peg` header to this phase (`PHASE_1:22`); Phase 3 slice 6 (`PHASE_3_libcirc_wasm.md:57,215`) added the `zig build libcirc-wasm` row and a `-Dwasm-optimize` sentence to the CLAUDE.md/README build tables. Where a row below quotes `main`'s "today" text for one of those lines, the text this phase actually finds is Phase 1's/Phase 3's wording — re-anchor on it and apply the "after" text only where it still differs.

## Goal

A contributor who clones the repo after this phase reads a single, consistent story: the parser is `lib/parser/parser.zig`, generated straight to Zig by the maintainer's langlang fork (`go/v0.0.13-zig.2`) from `lib/grammar/proto-circ.peg`, vendored, and regenerated only when the grammar changes; the build needs Zig (plus Node for the behavioural harnesses) and nothing else; the compiler also builds as `libcirc.a` and `libcirc.wasm` with one ten-export C ABI; and the site's `/playground` runs that module in a Web Worker. `DOCS/decisions/tooling.md` records why Go left and what pins the parser now (the fork's tag; the generated header's `Runtime: … abi=N sha256=…` line as the skew signal), `DOCS/decisions/libcirc.md` records decisions 3–8 and 10 as decision/rationale/alternatives entries, and `compiler-pipeline.md`/`index.md` point at both. `rg -n "CGo|parser\.a\b|CParser|go build" …` over the tree (archives, plans, STATUS excluded) returns only the history entry in `tooling.md`; `zig build test-all` is green; `circ-compile` cross-compiles for the three `cli-tag.yml` matrix targets with Go removed from `PATH`; the grammar file itself says how it is turned into a parser; and `DOCS/STATUS.md` ends with an entry that declares the initiative complete, ready for `DOCS/prompts/ARCHIVE.md` to fold it into `DOCS/archive/plan-libcirc.md`.

## Scope

**In scope:**
- `lib/grammar/proto-circ.peg`: a `//` header comment (langlang's grammar parser accepts `//` line comments — `~/circus/langlang/go/grammar_parser.go:551` `ParseComment <- '//' (!'\n' .)* '\n'`, and the file already carries one at `:29-32`) naming the fork, the `parser:gen` command, `lib/parser/parser.zig`, and the fork's `go/zig/langlang_runtime.zig`; the byte-identity of `parser.zig` under `zig build parser:gen` is the proof that comments never reach the bytecode.
- `DOCS/decisions/tooling.md` rewritten around the Zig parser (decisions 1, 2, 9 plus the pin/skew entry), keeping the retired Go bridge as a history paragraph — the one place the four stale strings legitimately survive.
- New `DOCS/decisions/libcirc.md` with decisions 3, 4, 5, 6, 7, 8, 10 (seven `###` entries), a preamble pointing at `DOCS/libcirc-api.md`, and cross-links from `compiler-pipeline.md`, `runtime-api.md`, `cli.md`, and `index.md`.
- Contributor docs sweep: `CLAUDE.md` (project sentence, toolchain, build table, pipeline diagram, invariant 6 allocator sentence, "Files worth knowing about"), `README.md` (`:3`, Install `:35-41`, Usage, Documentation), `DOCS/index.md` (documents table, repository layout, Build), `DOCS/architecture.md` (pipeline diagram, `### Memory`, Layer 3 bullets, a new Layer 6 for the library, Build targets), `DOCS/circuit-format.md` "Grammar" section and its site mirror via `sync-reference.ts`.
- Optional last slice: migrate the native test wiring in `build.zig` onto Phase 2's `build/frontend_modules.zig`.
- The `DOCS/STATUS.md` close-out entry and the hand-off to `DOCS/prompts/ARCHIVE.md` (plan name `libcirc`).

**Explicitly deferred:**
- Any code change beyond the grammar comment and the optional `build.zig` migration. No new diagnostics, exports, fixtures, or goldens.
- Fixing the parser quirks pinned in Phase 0 (keyword-prefix splitting, trailing-whitespace `BaseRef`, silent drops) — a later grammar initiative, per Architectural Constraints.
- `DOCS/libcirc-api.md` content (Phases 2–3 own it; this phase only links it) and `DOCS/analyze-api.md` (Phase 2 rewrote the overlay/truncation sections).
- The stale "Five invocation modes" heading in `DOCS/decisions/cli.md:3` (`--sim` and `--analyze` make seven) — noted, not this initiative's.
- The `circ-lsp` subprocess → `libcirc.a` switch (the plan prompt calls it "later").
- Running the archive itself: `DOCS/prompts/ARCHIVE.md:14` says archive only when the user explicitly asks; this phase leaves the tree archive-ready and stops.

## File & Module Topology

**New files:**

| Module/Package | File | Responsibility |
|---------------|------|---------------|
| docs | `DOCS/decisions/libcirc.md` | The library's decision records: preamble (one paragraph, same shape as `runtime-api.md:76-78`'s `## Native memories` preamble) + seven `###` entries for decisions 3–8 and 10 (exact slugs in Data & State). Cross-links `DOCS/libcirc-api.md`, `DOCS/analyze-api.md`, `tooling.md`, `runtime-api.md`. |
| plans | `DOCS/PLANS/PHASE_5_decisions_and_docs.md` (this file) | Plan artifact. |

**Modified files:**

| Module/Package | File | Change |
|---------------|------|--------|
| grammar | `lib/grammar/proto-circ.peg` | Prepend the 8-line `//` header in Data & State plus one blank line before `Program <- …` (`:1`). The existing recovery comment at `:29-32` stays (it moves to `:38-41`). Every rule line shifts by +9 — `Program` lands on line 10 (the plan prompt's "grammar line 13"/"line 37" citations are archived with it). |
| site artifacts | `site/public/wasm/libcirc.wasm`, `site/public/wasm/libcirc.manifest.json` | Refreshed by `bun site/scripts/build-libcirc.ts` in the same slice **iff** the manifest's `grammar_sha256` no longer equals `shasum -a 256 lib/grammar/proto-circ.peg` (Open Questions). |
| docs | `DOCS/decisions/tooling.md` | Replace the four sections (`:5` "langlang version pinning", `:13` "Generated parser is vendored in the repo", `:21` "CGo c-archive as the Zig↔Go bridge", `:31` "Build-time check when regenerating") with the five entries in Data & State. Intro sentence `:3` stays. |
| docs | `DOCS/decisions/compiler-pipeline.md` | `:13` "`.circ source → langlang parse tree → resolved IR …`" → "`.circ source → langlang-generated Zig parser (lib/parser/parser.zig) → resolved IR …`". `:29` "Pre-built runtime WASM" decision gains one sentence after the export list: the runtime is built with `-Dwasm-optimize` (default `ReleaseSmall`, stripped) independently of `-Doptimize`, and `libcirc` embeds a wasm-target twin of the same bytes — see [libcirc.md](libcirc.md) "`-Dwasm-optimize` governs both wasm builds". |
| docs | `DOCS/decisions/runtime-api.md` | After `:72` (the "list of *omissions*" paragraph PR #79 added): one sentence — `circ_alloc`/`circ_free`/`circ_version`/`circ_analyze`/`circ_compile`/`circ_preview`/`circ_truth_table`/`circ_result_ptr`/`circ_result_len`/`circ_reset` are exports of the *library* module `libcirc.wasm`, never of a compiled artifact; the artifact's import set stays `env.debugEnabled`/`env.onDebugLog` (`lib/log.zig:7-8`). Points at [libcirc.md](libcirc.md). |
| docs | `DOCS/decisions/cli.md` | End of "Five invocation modes" **Rationale** (`:15`): one sentence — the compile/preview/truth-table/analyze modes are also the library's four entry points (`DOCS/libcirc-api.md`); `--sim`, `--emit-zig`, `--inspect` stay CLI-only per [libcirc.md](libcirc.md) "Not exported through libcirc". |
| docs | `DOCS/decisions/index.md` | `:48-51` tooling bullets → "Parser generated straight to Zig by the maintainer's langlang fork (supersedes the Go c-archive bridge)", "Pin = the fork's tag `go/v0.0.13-zig.2`; skew signal = the generated header's runtime sha256", "Contract pinned before the switch (`Errors (n)` dump, recovery goldens, `expected-analyze`)", "Go retired in one cut-over". New `### [libcirc.md](libcirc.md)` block after `cli.md` with seven bullets (one per entry). `compiler-pipeline.md` bullets gain "`-Dwasm-optimize` (ReleaseSmall, stripped) for both wasm builds". Conventions `:55-58` unchanged. |
| docs | `CLAUDE.md` | (Phase 1 slice 2 already rewrote `:7,24,26,38,39,72` — `:7` reads "(the parser is a langlang-generated Zig file, `lib/parser/parser.zig`, vendored)", the Go bullet `:24` and the `parser:archive` row `:38` are gone, `:72` reads "PEG parser (vendored Zig, generated from" — and Phase 3 slice 6 added the `zig build libcirc-wasm` row; this phase refines that text.) `:7` project sentence: "(the parser is a langlang-generated Zig file, `lib/parser/parser.zig`, vendored)" → "(the parser is `lib/parser/parser.zig`, generated from `lib/grammar/proto-circ.peg` by the maintainer's langlang fork and vendored); it also builds as a library — `zig build libcirc` (`libcirc.a` + `include/libcirc.h`) and `zig build libcirc-wasm` (`libcirc.wasm`, the module behind the site's `/playground`)". `:21-26` toolchain: Zig 0.15.x; Node on `PATH` (harnesses + `tests/e2e/libcirc_wasm_test.zig`); langlang **only** for `parser:gen`, from the fork (`go install github.com/jeffersonmourak/langlang/go/cmd/langlang@v0.0.13-zig.2`, `langlang -version` → `Version: v0.0.13-zig.2 (github.com/jeffersonmourak/langlang/go)`); no Go bullet. `:28-40` build table: the `parser:archive` row (`:38`) is already deleted by Phase 1; `parser:gen` row (`:39`, Phase 1's text "requires the langlang fork (`v0.0.13-zig.2`) on `PATH`; the step refuses any other version") → keep, add "upstream `go/v0.0.12` fails with `Output language \`zig\` not supported`"; ensure rows `zig build libcirc` (→ `zig-out/lib/libcirc.a` + `zig-out/include/libcirc.h`; add if Phase 2 did not) and `zig build libcirc-wasm` (→ `zig-out/lib/libcirc.wasm`; `-Dwasm-optimize=` governs it and `circ-runtime.wasm`, default `ReleaseSmall`; Phase 3 slice 6 added it) exist. `:72-73` pipeline box: Phase 1's "PEG parser (vendored Zig, generated from lib/grammar/proto-circ.peg) → Zig AST" → "langlang-generated Zig parser (lib/parser/parser.zig, vendored; generated from lib/grammar/proto-circ.peg) → Zig AST". `:118` invariant 6 (post-merge text already says "an `ArenaAllocator` over `page_allocator` on every target"): append "`memory.reset()` (`arena.reset(.free_all)`) exists for libcirc and is called only with no `Circuit`/`Session` alive". If the merge left `main`'s older wording ("`std.heap.wasm_allocator` … `GeneralPurposeAllocator`", `main` `:118`), replace it — `lib/memory.zig:64-71` is the arena on every target. `:213-221` "Files worth knowing about" (heading `:213`, seven bullets `:215-221`): add `lib/parser/parser.zig` (generated; never hand-edit), `lib/libcirc.zig` + `lib/libcirc/{frontend,modes,json,c_api,wasm_root}.zig`, `build/frontend_modules.zig`, `DOCS/libcirc-api.md`. |
| docs | `README.md` | `:3` add "It also builds as a library — `libcirc.a` for native hosts and `libcirc.wasm` for the browser, which powers the site's `/playground`." `:37-41` prerequisites: Zig 0.15.x; Node (tests only); the langlang fork sentence (`:41`, `main`'s fork wording, already re-pointed at `lib/parser/parser.zig` by Phase 1 slice 2, `PHASE_1:46`) kept; the Go bullet (`:40`) is already deleted by Phase 1 — verify with `rg -n "Go 1\.21" README.md`. After the flags list (`:78`): a "Library" paragraph with the two build commands and the `DOCS/libcirc-api.md` pointer. `:80` code range already reads `E001`–`E018` in the memories tree (PR #79 did it) — nothing to do unless the merge regressed it (`grep -n "E001" README.md`). Documentation list (`:84-88`) gains `DOCS/libcirc-api.md`. |
| docs | `DOCS/index.md` | Documents table (`:19-33`): add `[libcirc-api.md](libcirc-api.md)` row ("`libcirc.a` / `libcirc.wasm` C ABI: request JSON, status codes, result buffer") if Phases 2–3 did not (`grep -n libcirc-api DOCS/index.md`). Layout block: `:84` "memory.zig Allocator wrapper (wasm_allocator on WASM, GPA on native)" → "Global arena allocator (`ArenaAllocator` over `page_allocator`; `reset()` for libcirc)"; `:86-87` parser lines (Phase 1 slice 2 already collapsed them to "`parser/  Vendored langlang-generated Zig parser (parser.zig; runtime + bytecode tables)`", `PHASE_1:47`) → "`parser/parser.zig` langlang-generated Zig parser (VM runtime + bytecode tables), vendored"; `:89` (Phase 1's "syntax/ Parse tree → AST translation over the generated Tree API") → "Parse tree (`parser.runtime.Tree`) → AST translation"; add `libcirc.zig`, `libcirc/` ("the compiler as a library: frontend, modes, json, c_api, wasm_root"), a `build/` block (`frontend_modules.zig`), and a `site/` line (`public/wasm/libcirc.wasm`, `/playground`). Build section (`:106-118`): add `zig build libcirc` and `zig build libcirc-wasm` lines. |
| docs | `DOCS/architecture.md` | `:12-16` front-end box → "PEG parser generated to Zig by langlang from proto-circ.peg (lib/parser/parser.zig: VM runtime + bytecode tables) → Zig AST (lib/syntax/ast.zig, translate.zig)" keeping the box's shape (every line of `:11-16` is 64 characters: three-space indent + `┌`/`│` + a 59-column interior + `┐`/`│`). `:108` (`### Memory`): "In the WASM target this is `std.heap.wasm_allocator`; on native … `GeneralPurposeAllocator`" → the arena-over-`page_allocator` sentence + `memory.reset()` clause. `:132-134` Layer 3 bullets → grammar (regenerate `parser.zig` with `zig build parser:gen`; fork tag), `lib/parser/parser.zig` (header lines; `pub const runtime` (`:7`) with `Tree`, `Machine`, `Interpreter`, `Bytecode`, `NodeType`; top-level `bytecode` (`:1471`), `Rule` enum (`:1616`), `Parser = runtime.Interpreter(bytecode, Rule, &left_recursive_rules)` (`:1635`) — line numbers from a fresh `v0.0.13-zig.2` generation), `lib/syntax/` (`translate.zig` walks `parser.runtime.Tree` through `typ/name/range/childAt/childrenLen/child`; `span.zig`). New `## Layer 6 — Library (lib/libcirc.zig, lib/libcirc/)` before `## Build targets` (`:149`): frontend → modes → json/c_api, the two build shapes, the ten exports, status codes, overlay-first loading, pointer to `DOCS/libcirc-api.md`. Build targets table (`:153-158`): rows for `libcirc` and `libcirc-wasm`; the runtime row notes `-Dwasm-optimize`. `:160` "There is no longer a TypeScript SDK target …" stays. |
| docs | `DOCS/circuit-format.md` | `:134` Grammar sentence → "The parser is generated from a PEG grammar at `lib/grammar/proto-circ.peg` by the maintainer's langlang fork (`-output-language zig`); the generated file is vendored at `lib/parser/parser.zig` and walked by `lib/syntax/translate.zig`." `:136` lead-in sentence stays; `:138-143` node-type table (rows `:140-143`) → `parser.runtime.NodeType` values `.sequence`, `.node`, `.string`, `.err` (same four meanings; `langlang_runtime.zig:213-218` numbers them `string=0, sequence=1, node=2, err=3`). `:145` "`lib/syntax/nodes/declaration.zig` handles `Declaration` rule nodes …" deleted (Phase 1 removed the dead file). Then `bun site/scripts/sync-reference.ts` regenerates `site/src/pages/reference/circuit-format.md` (`:136` today). |
| build (optional slice 5) | `build.zig` | Replace the hand-built native front-end module chain (`translate_mod` `:96-100` (main) … through `validator_run_project_mod`, `resolver_*_mod`, `topology_*_mod`, `runtime_embed`) with one `frontend_modules` call and route the test roots' `addImport` calls at its handles. Today's counts (main, `grep -c`): 60 `addTest`, 122 `createModule`, and per-name imports `translate` 18, `scan_imports` 16, `import_cycle` 13, `resolve_bodies` 12, `validator_run_project` 11, `ir_types` 29, `file_loader` 5, `runtime_embed` 5, `full_serializer` 5, `serializer` 4, `section_writer` 3, `engine_session` 3, `truth_table_builder` 2 — Phases 1–3 add artifacts (`parser_tests`, `createParserModule`, the libcirc modules), so recount before the slice. Phase 1 already deleted `fn linkParserArchive` (`:9-12`) and its 27 call sites (28 lines matched `linkParserArchive` on main), 25 of the 27 `.linkLibC()` calls (the two on `wasm_run.zig` consumers stay for `std.c.getpid`, `PHASE_1:27`), and the 28 `addIncludePath(b.path("."))`/`addIncludePath(b.path("./lib"))` pairs (56 calls). `golden_tests` (`:79-89`) is not one of the 27 — it sets `.link_libc = true` on its root module because `tests/helpers/golden_test.zig:5` `@cImport`s `setenv`/`unsetenv` (`tests/helpers/golden.zig` itself does not); keep it. |
| status | `DOCS/STATUS.md` | One entry per slice; the last one declares the initiative complete (text in Data & State). |

**New dependencies:** None. `bun` (already required by `site/`) runs `sync-reference.ts` and, if needed, `build-libcirc.ts`; `langlang v0.0.13-zig.2` (already required by `parser:gen`) proves the grammar-comment no-op. No Zig module is added; the optional slice removes `createModule` calls rather than adding any.

## Data & State

This phase introduces no runtime types. Its "data" is the exact text it commits.

**The grammar header** (`lib/grammar/proto-circ.peg`, lines 1–8 after the change; line 9 is blank, `Program` moves to line 10):

```peg
// proto-circ.peg — the .circ grammar. `zig build parser:gen` feeds this file to the
// maintainer's langlang fork (github.com/jeffersonmourak/langlang, branch
// zig-parser-gen, tag go/v0.0.13-zig.2) as
//   langlang -grammar lib/grammar/proto-circ.peg -disable-capture-spaces -output-language zig -output-path lib/parser/parser.zig
// and vendors the result: the fork's go/zig/langlang_runtime.zig (the PEG VM, all 33
// opcodes, wasm32-freestanding clean) followed by this grammar's bytecode as comptime
// tables. The generated header's `Runtime: … abi=N sha256=…` line is the skew signal
// between fork and vendored file. Edit here → parser:gen → review tests/fixtures/expected-ast.
```

Facts the header quotes, verified on this machine: `langlang -version` prints `Version: v0.0.13-zig.2 (github.com/jeffersonmourak/langlang/go)`; the fork's tags are `go/v0.0.13-zig.1` and `go/v0.0.13-zig.2` (installed as `@v0.0.13-zig.2`, `go/zig/README.md:11,16-19`); a fresh generation of this grammar is 1,639 lines whose first three lines are `// Code generated by langlang (unknown commit hash), DO NOT EDIT.` / `// Source File: lib/grammar/proto-circ.peg` / `// Runtime: langlang_runtime.zig abi=1 sha256=007bc7864beb8c39960a00e87f52b19f4f3e7c334516168cc78285b3b094ec06 (Zig 0.15.1, native + wasm32-freestanding, no libc)`, and `shasum -a 256 ~/circus/langlang/go/zig/langlang_runtime.zig` is that same digest; the pasted runtime declares `abi_version: u32 = 1` and `langlang_version = "go/v0.0.12"`. The "unknown commit hash" is what a `go install`ed binary prints — the sha256 line, not the hash, is the signal.

**`DOCS/decisions/tooling.md` after the rewrite** (house style `validation.md:5-13`: `###` slug, **Decision.**/**Rationale.**/**Alternatives.**, ~15 lines; each entry names its decision number from `DOCS/PLANS_PROMPT.md` in the rationale):

| Entry (`###` slug) | Replaces | Locked decision / content |
|--------------------|----------|---------------------------|
| Parser generated straight to Zig by the langlang fork | "CGo c-archive as the Zig↔Go bridge" (`:21-29`) | Decision 1. Decision: `-output-language zig` on the fork; one self-contained `parser.zig` = pasted runtime (33 opcodes incl. left recursion) + comptime bytecode tables + `Rule` enum; circ vendors it as `lib/parser/parser.zig`; `translate.zig` walks `parser.runtime.Tree`. Rationale: the runtime is complete and lives with the generator, so grammar growth never needs parser work in circ; parity was proven on the langlang side (`TestGenZigDifferential/grammars/circ`, `TestGenZigTablesMatchVendoredGo`, `go/zig/scripts/diff-circ.sh` at 0 mismatches — `go/zig/CIRC.md:8-27`). Alternatives (all rejected): a circ-local bytecode VM (tracks langlang's instruction set by hand), recursive descent (the grammar becomes prose), a Go `js/wasm`/`wasip1` sidecar (~2–2.5 MB of Go runtime, two linear memories), wrapping `parser.a` (impossible: Go refuses `c-archive` for wasm, `$GOROOT/src/internal/platform/supported.go:146-158`; `parser.h:66` static-asserted 64-bit pointers). **History** paragraph (the only surviving mention of the retired bridge, and the reason the sweep tolerates this file): until Phase 1 the parser was `lib/parser/parser.go` + `shim/shim.go` built by `go build -buildmode=c-archive` into `parser.a`/`parser.h` and linked through `lib/syntax/CParser.zig`, which cost ~2 MB of Go runtime per binary, a Go 1.21+ prerequisite at every build, and no Windows cross-compile (`build.zig:43-53` (main) panicked for non-native targets). |
| The fork's tag is the pin; the generated header is the skew signal | "langlang version pinning" (`:5-11`) and "Build-time check when regenerating" (`:31-37`) | Decision: pin `go install github.com/jeffersonmourak/langlang/go/cmd/langlang@v0.0.13-zig.2` (fork of upstream `go/v0.0.12`, the exact generator that produced the last `parser.go` byte for byte); the vendored file's `Runtime: langlang_runtime.zig abi=1 sha256=…` line and the runtime's `abi_version`/`langlang_version` constants are what a reviewer compares against the fork. Rationale: a grammar change never needs runtime work, but a langlang *version* bump that changes the bytecode ABI does, and that work lives in the fork — the header makes drift visible in the diff. The build-time check the old entry recorded was never implemented on `main` (`build.zig:19-36` ran `langlang` unconditionally); Phase 1 implemented it as `check_langlang = b.addSystemCommand(&.{ "langlang", "-version" })` with `addCheck(.{ .expect_stdout_match = "v0.0.13-zig.2" })` and `has_side_effects = true` ahead of `generate_parser_cmd` (`PHASE_1_vendor_zig_parser.md:42,187-190`), so `parser:gen` refuses any other langlang — upstream `go/v0.0.12` would otherwise fail on `-output-language zig` with "Output language `zig` not supported". A tag bump changes that one literal in `build.zig` together with `CLAUDE.md:26`, `README.md:41`, and the `.peg` header (`PHASE_1:277`). Alternatives: tracking the fork's branch head (moving target); trusting whatever `langlang` is on `PATH` (the header's sha256 would still catch drift, but only after a wrong regeneration lands in a diff). |
| Generated parser is vendored in the repo | same slug (`:13-19`) | Kept; `parser.go` → `parser.zig`; the sentence "the standard build path needs only Zig and Go (Go is required at every build …)" (`:17`) → "needs only Zig (Node for the behavioural harnesses)". |
| The contract was pinned before the switch | new | Decision 2: `ast_dump` gained `Errors (n)`; the `recovery_*` fixtures and `expected-analyze/*.json` goldens were generated by the Go parser and hand-reviewed; circ's fixtures and inline inputs feed the fork's corpus-mode differential (`diff-circ.sh`) so recovery parity is checked with node ids and spans. Rationale: every `expected-*` golden stayed byte-identical through Phases 1–5 because the oracle was fixed first. Alternatives: corpus/mutation/fuzz machinery in circ (dropped — it de-risked a circ-local VM that was never built). |
| Go retired in one cut-over | new | Decision 9: Phase 1 vendored `parser.zig` and deleted `lib/parser/{go.mod,parser.go,parser.a,parser.h,shim/}`, `CParser.zig`, `nodes/declaration.zig`, `fn linkParserArchive`, `parser:archive`, the GOOS/GOARCH/CC block, the `.gitignore` lines, in the same slice; no dual-parser build option. Rationale: the differential proof lives on the langlang side and circ's goldens are the acceptance; `pr-tests.yml` gained a `Remove Go from PATH` step between `Install Node` (`:36-38`) and `Run all tests` (`:40-53`) — it prunes every `PATH` entry holding an executable `go` and fails if `command -v go` still resolves — because `ubuntu-latest` ships Go and could not otherwise prove independence; the existing `test-all` step then builds `circ-compile` Go-free (`test_step` depends on `circ_compile_exe`, `build.zig:986` (main); `tests/cli/integration_test.zig:46` spawns `zig build circ-compile`), which subsumes the plan prompt's separate "`zig build circ-compile` step" (`PHASE_1:48`). Alternatives: a `-Dparser=go|zig` switch (two parsers to keep in lock-step for no consumer). |

**`DOCS/decisions/libcirc.md`** — preamble: "The entries below record the library half of the libcirc initiative (`lib/libcirc.zig`, `libcirc.a`, `libcirc.wasm`, the site's `/playground`). The numbered decisions they cite are the ten locked in `DOCS/PLANS_PROMPT.md`; the parser half is in [tooling.md](tooling.md). The host-facing reference is `DOCS/libcirc-api.md`; the JSON the status-1 result shares with `--analyze` is `DOCS/analyze-api.md`." Then exactly seven entries:

| `###` slug | Decision | Load-bearing content the entry must carry |
|------------|----------|-------------------------------------------|
| One wasm module, ten exports, JSON in / bytes-or-JSON out | 3 | The ten names in order (`circ_alloc(len)`, `circ_free(ptr, len)`, `circ_version()`, `circ_analyze(req, len)`, `circ_compile(req, len)`, `circ_preview(req, len)`, `circ_truth_table(req, len)`, `circ_result_ptr()`, `circ_result_len()`, `circ_reset()`); request shape `{"root": "/playground/main.circ", "files": {...}, "options": {...}}`; status table `0` ok / `1` front-end diagnostics in analyze-api shape / `2` bad request / `3` mode refusal (`{"error": "..."}`: input-bit cap, RAM-stateful truth table, layout failure) / `4` OOM / `5` internal; request buffers from `circ_alloc`, freed by the caller; result buffer library-owned, valid until the next `circ_*` call; identical surface for `libcirc.a` (`export fn`, `callconv(.c)`, `u32` ptr/len). Alternatives: one export per mode with out-pointers; a WASI shim; per-call instantiation. |
| Overlay-first virtual file loading | 4 | `file_loader.resolveImportPath` resolves lexically with `std.fs.path.resolvePosix`, returns the key when the overlay holds it, else `realpath` on native, else `FileNotFound` (keeps `E009`); disk branches compiled out on `.freestanding`; keys are absolute-looking `/playground/<name>.circ`, matched byte-for-byte. Rationale includes the latent native bug it fixed (overlay-only sibling imports failed with `E009` because `:72` (main) realpathed unconditionally) and the analyze inline test Phase 2 added as its tripwire. Alternatives: a VFS trait object; rewriting imports client-side. |
| Library memory model | 5 | Per-call `ArenaAllocator` for compiler allocations; library-owned result buffer; `memory.reset()` (`arena.reset(.free_all)`) after `truthTable` tears down its `Session` — the single addition to the engine's global allocator (CLAUDE.md invariant 6, `lib/memory.zig:64-71`); on wasm the arena sits over `std.heap.wasm_allocator`; no re-instantiation per call (Phase 3's 50-compile bound test is the tripwire). Alternatives: an allocator parameter on the engine (rejected by invariant 6); fresh `WebAssembly.instantiate` per request. |
| `-Dwasm-optimize` governs both wasm builds | 6 | Default `ReleaseSmall`, stripped; decoupled from `-Doptimize` (`build.zig:708-712` (main) tied the runtime to the global option, which is why every committed site artifact was a ~921 KB Debug build, ~600 KB DWARF); `expected-wasm` fixtures are behaviour vectors so runtime bytes may change freely; quote the sizes STATUS recorded in Phase 3 (planning measurement: 19,628 B `circ-runtime.wasm` under ReleaseSmall vs 933,072 B Debug; budget `libcirc.wasm` ≤ 600 KB raw / ≤ 200 KB gzip). `wasm-opt` is not a build dependency. Alternatives: `-Doptimize=ReleaseSmall` for everything (slows native tests); post-processing with `wasm-opt`/`wasm-strip`. |
| The renderer follows the compiler, once, for v03 | 7 | libcirc sequenced after PR #79 so the `circ-renderer` sync (kinds `rom=8`/`ram=9`, ports `addr/din/we/clk`, `Aux.memory`, exported `SUPPORTED_TOPOLOGY_VERSIONS`) happened a single time; one commit bumps the pin via `bun install` against the new ref, regenerates `site/public/wasm/*.wasm`, and refreshes `libcirc.wasm` from one compiler revision; the playground handshakes `circ_version().full_version` against the set and shows a banner instead of a decode throw. Alternatives: a compatibility shim in the compiler (rejected by `compiler-pipeline.md`: pre-1.0 the version byte is freely revvable). |
| Playground artifacts are committed; the module runs in a Web Worker | 8 | `site/public/wasm/libcirc.wasm` + `libcirc.manifest.json` (version, revision, topology versions, grammar sha256, byte size), written by `site/scripts/build-libcirc.ts` which fails over budget; refreshed on tagged releases; `deploy-site.yml` runs bun only (`:43,57,65`); the Worker keeps `2^bits` truth-table enumeration off the main thread (UI cap 10 bits, ≤ 12 with a warning; the library's 24-bit maximum unreachable from the page). Alternatives: building the wasm in CI (needs Zig in `deploy-site.yml`); running on the main thread. |
| Not exported through libcirc | 10 | `--sim` (stdio-shaped; reads image files after PR #79), `--emit-zig` (needs a Zig toolchain on the consumer; rejects memories), `--inspect` (single-module debug output; `circ_inspect` is a cheap later addition); browser interactivity is `circ_compile`'s bytes fed to `renderCircuit({ bytes })`. Alternatives: a `circ_sim` line-protocol export (would re-implement stdio over JSON for no consumer). |

**Sentences the sweep replaces** (post-merge line numbers; `main` numbers where they differ). "Today" quotes the memories tree; rows marked † were already rewritten by Phase 1 slice 2 (`PHASE_1:45-47`), so the text found at that line is Phase 1's (quoted in parentheses) and the "After" column applies only where it still differs:

| File:line | Today | After |
|-----------|-------|-------|
| `CLAUDE.md:72-73` † | `[lib/syntax + lib/parser]   PEG parser (vendored C, generated from` / `lib/grammar/proto-circ.peg) → Zig AST` (Phase 1: `PEG parser (vendored Zig, generated from`) | `[lib/syntax + lib/parser]   langlang-generated Zig parser (lib/parser/parser.zig,` / `vendored; generated from lib/grammar/proto-circ.peg) → Zig AST` |
| `CLAUDE.md:118` (`main:118` differs) | "an `ArenaAllocator` over `page_allocator` on every target, so `free` only rewinds …" (`main`: "On WASM that is `std.heap.wasm_allocator`; on native … `GeneralPurposeAllocator`") | post-merge sentence + "`memory.reset()` (`arena.reset(.free_all)`) exists for libcirc and is called only with no `Circuit`/`Session` alive." |
| `README.md:40` † | "- [Go](https://go.dev/) 1.21+ — used to compile the langlang-generated parser into a CGo c-archive …" | already deleted by Phase 1 slice 2 (`PHASE_1:46`); verify with `rg -n "Go 1\.21" README.md` |
| `DOCS/index.md:84` | `memory.zig               Allocator wrapper (wasm_allocator on WASM, GPA on native)` | `memory.zig               Global arena (ArenaAllocator over page_allocator; reset() for libcirc)` |
| `DOCS/index.md:86-87` † | `parser/                  Vendored langlang-generated Go parser + CGo shim` / `(parser.go, shim/shim.go → parser.a + parser.h)` (Phase 1: one line, `parser/  Vendored langlang-generated Zig parser (parser.zig; runtime + bytecode tables)`) | `parser/parser.zig        langlang-generated Zig parser (VM runtime + bytecode tables), vendored` |
| `DOCS/index.md:89` † | `syntax/                  Parse tree → AST translation, FFI to the c-archive` (Phase 1: `Parse tree → AST translation over the generated Tree API`) | `syntax/                  Parse tree (parser.runtime.Tree) → AST translation` |
| `DOCS/architecture.md:13-14` | `PEG parser (langlang Go output + CGo c-archive shim,` / `generated from proto-circ.peg) → Zig AST` | `PEG parser generated to Zig by langlang from` / `proto-circ.peg (lib/parser/parser.zig) → Zig AST` |
| `DOCS/architecture.md:108` | "In the WASM target this is `std.heap.wasm_allocator`; on native (`zig build test`) it's a `GeneralPurposeAllocator`." | "It is an `ArenaAllocator` over `page_allocator` on every target (`lib/memory.zig:65`); `free` is a no-op, and `memory.reset()` exists only for libcirc's per-call teardown." |
| `DOCS/architecture.md:133` | "**`lib/parser/`** — `parser.go` (langlang-generated) plus `shim/shim.go` (CGo bridge …) … linked via `lib/syntax/CParser.zig`." | "**`lib/parser/parser.zig`** — the generated parser: header (`Code generated by langlang`, `Source File`, `Runtime: … abi=1 sha256=…`), `pub const runtime` (the VM: `Tree`, `Machine`, `Interpreter`, `Bytecode`), the `bytecode` comptime tables, the `Rule` enum, and `Parser = runtime.Interpreter(bytecode, Rule, …)`. Never hand-edited; `zig build parser:gen` rewrites it." |
| `DOCS/architecture.md:134` | "**`lib/syntax/`** — `CParser.zig` is the FFI wrapper, `translate.zig` lowers the parse tree …" | "**`lib/syntax/`** — `translate.zig` walks `parser.runtime.Tree` (`typ`/`name`/`range`/`childAt`/`childrenLen`/`child`) into `ast.zig`; `span.zig` carries source spans." |
| `DOCS/circuit-format.md:134` | "The generated Go parser is at `lib/parser/parser.go`; a hand-written CGo shim … `lib/syntax/CParser.zig`." | "… by the maintainer's langlang fork (`-output-language zig`); the generated file is vendored at `lib/parser/parser.zig` and walked by `lib/syntax/translate.zig`." |
| `DOCS/circuit-format.md:140-143` (`main:138-141`) | `NodeType_Sequence` / `NodeType_Node` / `NodeType_String` / `NodeType_Error` | `.sequence` / `.node` / `.string` / `.err` (`parser.runtime.NodeType`) |
| `DOCS/circuit-format.md:145` | "`lib/syntax/nodes/declaration.zig` handles `Declaration` rule nodes …" | deleted |

**The sweep command and its expected output** (run from the repo root; `rg` honours `.gitignore`):

```sh
rg -n "CGo|parser\.a\b|CParser|go build" \
   --glob '!DOCS/archive/**' --glob '!DOCS/PLANS/**' \
   --glob '!DOCS/STATUS.md' --glob '!DOCS/PLANS_PROMPT.md'
# expected: only DOCS/decisions/tooling.md:<n> lines, all inside the "History" paragraph
# of "Parser generated straight to Zig by the langlang fork"
```

Today (main, before Phases 1–4) the same command returns 24 lines across 11 files: `CLAUDE.md` (3), `README.md` (1), `build.zig` (6), `DOCS/index.md` (2), `DOCS/circuit-format.md` (1), `DOCS/decisions/index.md` (1), `DOCS/decisions/tooling.md` (4), `DOCS/architecture.md` (3), `lib/parser/shim/shim.go` (1), `lib/syntax/nodes/declaration.zig` (1), `lib/syntax/translate.zig` (1) — the `\b` matters: without it `tools/bench/format-delta-comment.py:147` (`parser.add_argument(`) is a 25th line. Phase 1 slice 2 removes the code hits *and* the `CLAUDE.md`, `README.md`, `build.zig`, `DOCS/index.md` lines (its own proof greps those files, `PHASE_1:240`), so what this phase inherits is 9 lines in 4 files — `DOCS/architecture.md` (3: `:13`, `:132`, `:133` memories), `DOCS/circuit-format.md` (1: `:134`), `DOCS/decisions/index.md` (1: `:51`), `DOCS/decisions/tooling.md` (4: `:17`, `:21`, `:23`, `:25`) — plus the site mirror below. One tracked file is invisible to that command: `site/src/pages/reference/circuit-format.md:136` carries the same CGo sentence, but `site/.gitignore` lists `src/pages/reference/` (the four mirrors were committed before the ignore rule, `git ls-files site/src/pages/reference` prints them) and `rg` obeys ignore files even for tracked paths. The sweep therefore has a second half:

```sh
rg -n --no-ignore "CGo|parser\.a\b|CParser|go build" site/src/pages/reference/
# expected: no output (the mirror is regenerated from DOCS/circuit-format.md by sync-reference.ts)
```

**The STATUS close-out entry** (appended by slice 6; `DOCS/STATUS.md`'s first line is whatever header Phase 0's first entry created — `PHASE_0_parser_contract_pins.md` does not dictate it; the memories precedent is `# Status — Native Memories (ROM & RAM)` followed by the "Append-only log …" sentence — never edit prior entries):

```
## YYYY-MM-DD — Phase 5 — Slice 6: close-out (initiative complete)

**What shipped:** The libcirc initiative is complete: the parser is `lib/parser/parser.zig` generated by the langlang fork (`go/v0.0.13-zig.2`), Go and the c-archive are gone, `zig build libcirc` / `zig build libcirc-wasm` share one ten-export ABI, the site's `/playground` runs `libcirc.wasm` in a Web Worker, and the decision records (`tooling.md`, `libcirc.md`), CLAUDE.md, README, and DOCS agree with the code.
**Files touched:** `DOCS/STATUS.md`
**Tests:** ran `zig build test-all`, result pass (<n>/<n>); ran the Go-less cross-compiles for `x86_64-linux-gnu`, `aarch64-macos`, `x86_64-windows-gnu` (`cli-tag.yml:22-30` matrix), all three linked; the rg sweep returns only `DOCS/decisions/tooling.md`.
**Next slice:** none — Phase 5 is the last phase. Archive with `DOCS/prompts/ARCHIVE.md` as `plan-libcirc` when asked.
**Notes:** <sizes from Phase 3 STATUS, whether slice 5 landed or was skipped, the Linux Node init() hang status, anything the archive should carry as a papercut>
```

**Archive hand-off** (not a slice; `DOCS/prompts/ARCHIVE.md:7-16` gates it on the user asking): plan name `libcirc` → `DOCS/archive/plan-libcirc.md` with the header at `ARCHIVE.md:54-62` (full SHA from `git rev-parse HEAD`, short + subject from `git log -1 --pretty='%h %s'`, duration = first → last STATUS date); the body sections at `:64-73`; then delete `DOCS/PLANS_PROMPT.md`, `DOCS/PLANS/`, `DOCS/STATUS.md`, add a row to `DOCS/archive/index.md` (six rows today, `plan-memories.md` should be the seventh before this one), and confirm `DOCS/index.md:32-33` (memories tree; `:31` is `decisions/`) still point at `archive/` and `prompts/ARCHIVE.md` only. After the deletion the `--glob '!DOCS/PLANS_PROMPT.md'` exclusion in the sweep becomes moot.

## Execution & Concurrency Model

This phase is fully synchronous. No background threads, workers, or build steps are introduced. The only concurrency it *describes* is the Web Worker Phase 4 shipped (`site/src/workers/libcirc.worker.ts`, decision 8), recorded in `libcirc.md` as-is. `zig build parser:gen` and `bun site/scripts/sync-reference.ts` are one-shot commands run by the execution agent in the foreground; the optional slice 5 changes how `build.zig` constructs its module graph but not what runs or in what order (`test_step` `:967` (main) keeps its `dependOn` list).

## Persistence & I/O

Writes, all in the working tree and staged by path: `lib/grammar/proto-circ.peg` (header), `lib/parser/parser.zig` (rewritten by `zig build parser:gen`, expected `git diff` empty), `site/public/wasm/libcirc.{wasm,manifest.json}` (only if `grammar_sha256` moved; `bun site/scripts/build-libcirc.ts`), the seven `DOCS/decisions/*.md` files and the new `libcirc.md`, `CLAUDE.md`, `README.md`, `DOCS/{index,architecture,circuit-format}.md`, `site/src/pages/reference/circuit-format.md` (generated by `sync-reference.ts`, which also rewrites the other four mirrors — their diffs must be empty), optionally `build.zig`, and `DOCS/STATUS.md` (append-only). Reads: the files above plus `~/circus/langlang/go/zig/{CIRC.md,README.md,langlang_runtime.zig}` for the quoted facts. No network I/O; `bun install` is not run (the renderer pin is Phase 4's). The archive step, when the user asks for it, deletes three plan artefacts and adds one file under `DOCS/archive/`.

## Slices

The execution agent implements this phase one slice at a time, stopping for review after each.

| # | Slice Title | Deliverable | Test Proof |
|---|-------------|-------------|-----------|
| 1 | Grammar header comment | The 8-line `//` header prepended to `lib/grammar/proto-circ.peg`; `zig build parser:gen` run afterwards; if `jq -r .grammar_sha256 site/public/wasm/libcirc.manifest.json` ≠ `shasum -a 256 lib/grammar/proto-circ.peg`, run `bun site/scripts/build-libcirc.ts` and stage the refreshed `libcirc.wasm` + manifest too. Commit: `docs(grammar): describe the langlang fork pipeline in proto-circ.peg`. | `git diff --stat lib/parser/parser.zig` is empty after `parser:gen` (comments never reach the bytecode; `langlang -version` reports `v0.0.13-zig.2`); `zig build test` green with every `expected-ast`/`expected-analyze`/recovery golden byte-identical; `head -8 lib/grammar/proto-circ.peg` is the header verbatim and `sed -n 10p` is `Program     <- …`; the manifest's `grammar_sha256` equals the file's sha256; `bun test` in `site/` (Phase 4's `site/test/libcirc.test.ts`) still passes against the committed wasm. |
| 2 | Decision records | `tooling.md` rewritten (five entries per the table, history paragraph inside the first); new `libcirc.md` (preamble + seven entries); `compiler-pipeline.md:13` wording and `:29` sentence; `runtime-api.md` sentence after `:72`; `cli.md:15` sentence; `index.md` tooling bullets, new `libcirc.md` block, compiler-pipeline bullet. Commit: `docs(decisions): record the libcirc and Zig-parser decisions`. | Review: `grep -c "^### " DOCS/decisions/libcirc.md` = 7 and `grep -c "^### " DOCS/decisions/tooling.md` = 5; every entry names its decision number in the rationale; `rg -n "CGo|parser\.a\b|CParser|go build" DOCS/decisions` returns only `tooling.md` lines inside the history paragraph; `grep -n "libcirc.md" DOCS/decisions/{index,compiler-pipeline,runtime-api,cli}.md` finds each cross-link; `index.md` conventions `:55-58` hold (unnumbered `###`). |
| 3 | CLAUDE.md and README | The `CLAUDE.md` edits (`:7`, `:21-26`, `:28-40` table, `:72-73`, `:118`, `:213-220`) and `README.md` edits (`:3`, `:37-41`, Library paragraph, `:80` code range, Documentation list) from Modified files. Commit: `docs: describe the Zig parser and libcirc in CLAUDE.md and README`. | `rg -n "CGo|parser\.a\b|CParser|go build|Go 1\.21|parser:archive|parser\.go|wasm_allocator|GeneralPurposeAllocator" CLAUDE.md README.md` is empty; `grep -n "zig build libcirc" CLAUDE.md README.md` finds the two build commands in both; the CLAUDE.md build table has exactly one row each for `libcirc` and `libcirc-wasm` and none for `parser:archive` (Phase 1 removed it; Phase 3 slice 6 added `libcirc-wasm`, so the count relative to `main`'s nine rows is −1 +2 = 10 once `libcirc` is present); `zig build` unaffected (docs only). |
| 4 | DOCS index, architecture, grammar reference + site mirror | `DOCS/index.md` (table row, layout lines `:84,86-87,89`, `libcirc`/`build/`/`site/` lines, Build section), `DOCS/architecture.md` (`:13-14` box, `:108`, `:132-134`, new Layer 6, Build targets rows), `DOCS/circuit-format.md` (`:134`, `:140-143` table rows, `:145` deleted), then `bun site/scripts/sync-reference.ts`. Commit: `docs: sync DOCS index, architecture, and grammar reference with the Zig parser and libcirc`. | Both sweep commands in Data & State pass — the tree-wide one returns only `DOCS/decisions/tooling.md` lines and the `--no-ignore` one over `site/src/pages/reference/` returns nothing (the definition-of-done proof); `rg -n "wasm_allocator|GeneralPurposeAllocator" DOCS/index.md DOCS/architecture.md` empty; `git status --short site/src/pages/reference/` lists only `circuit-format.md`; `diff DOCS/circuit-format.md site/src/pages/reference/circuit-format.md` shows only front-matter and `/reference/...` link hunks; `rg -n "libcirc-api" DOCS/index.md README.md` finds the pointer in both; the architecture box at `:11-16` keeps its shape: `sed -n '11,16p' DOCS/architecture.md \| python3 -c "import sys; print([len(l.rstrip('\n')) for l in sys.stdin])"` prints `[64, 64, 64, 64, 64, 64]` (macOS `awk length` counts bytes and prints `186/68/68/70/68/186` for the same box, so do not use it). |
| 5 | (optional) Native test wiring through `build/frontend_modules.zig` | Replace the hand-built front-end module chain in `build.zig` with the Phase 2 helper's handles; every `addTest` root (60 on `main`; recount after Phases 1–3) keeps its name and `test_step.dependOn` order; `golden_tests` keeps `.link_libc = true` (`:79-89`, for `tests/helpers/golden_test.zig:5`'s `@cImport`) and the two `.linkLibC()` calls Phase 1 left for `wasm_run.zig`'s `std.c.getpid` stay unless Phase 1's open question retired them. Skip (and record the reason in STATUS) if the helper does not expose per-module handles. Commit: `build: route the native test module graph through frontend_modules.zig`. | `zig build test-all` green; `zig build test --summary all` reports the same number of test steps as before the slice; `git status --short tests/fixtures` empty (every golden byte-identical); `grep -c createModule build.zig` drops by at least the count of front-end modules the helper now owns (record before/after in STATUS); `zig build circ-compile`, `zig build libcirc`, `zig build libcirc-wasm` all still build; `zig build bench` green (no engine change, but the module graph moved). |
| 6 | Close-out | The STATUS entry in Data & State, written after the proofs below have run. Commit: `docs(status): close out the libcirc initiative`. | `zig build test-all` green (`SKIP_WASM_E2E` unset on macOS/Node 24); with Go removed from `PATH` — prune every entry that holds an executable `go`, the same rule Phase 1's CI step uses: `PATH="$(echo "$PATH" \| tr ':' '\n' \| while IFS= read -r d; do [ -x "$d/go" ] \|\| printf '%s\n' "$d"; done \| paste -sd: -)"`, confirmed by `command -v go` failing (on this machine `go` is `/usr/local/bin/go`, so a pattern filter such as `grep -v -E '/go/bin\|/Go/'` leaves it on `PATH`; `zig` is an asdf shim and `node` an fnm shim, both survive the prune), `zig build -Doptimize=ReleaseFast -Dtarget=<t> circ-compile` succeeds for `x86_64-linux-gnu`, `aarch64-macos`, `x86_64-windows-gnu` (`cli-tag.yml:22-30`; the Windows one was impossible before Phase 1); both sweeps pass (tree-wide → only `tooling.md`; `--no-ignore` over the site mirror → nothing); `git log --oneline` contains every slice STATUS lists (`ARCHIVE.md:11-12` precondition); `git status --porcelain` shows nothing outside `DOCS/STATUS.md` before staging. |

Slices are ordered by dependency. Each slice must be fully reviewable on its own. Slice 1 first because its `parser:gen` byte-identity check also proves the toolchain the later docs describe is the one on the machine; slice 2 before 3–4 so the prose can link to entries that exist; slice 5 is optional and independent; slice 6 last because its entry quotes the other slices' outcomes.

## Tests

No new Zig `test` blocks are added — this phase's proofs are the commands below, each run by the execution agent and its outcome recorded in STATUS.

**Unit tests:**

| Test Name | Module | What It Asserts |
|-----------|--------|----------------|
| `parser:gen` byte-identity | `lib/grammar/proto-circ.peg` → `lib/parser/parser.zig` | After the header comment, `zig build parser:gen && git diff --stat lib/parser/parser.zig` prints nothing: langlang's `Comment` rule (`grammar_parser.go:551`) drops comments before bytecode generation, so the vendored parser, its `Runtime: … sha256=` line, and every golden are untouched. |
| grammar sha ↔ manifest | `site/public/wasm/libcirc.manifest.json` | `jq -r .grammar_sha256 site/public/wasm/libcirc.manifest.json` equals the digest column of `shasum -a 256 lib/grammar/proto-circ.peg` (after the slice-1 refresh if it was needed). |
| stale-string sweep | whole tree + the ignored-but-tracked site mirror | The tree-wide `rg` command in Data & State prints only `DOCS/decisions/tooling.md:<n>` lines (the same command with `--glob '!DOCS/decisions/tooling.md'` prints nothing), and `rg -n --no-ignore "CGo|parser\.a\b|CParser|go build" site/src/pages/reference/` prints nothing (plain `|` alternation — never `\|`, see Warnings). |
| allocator sentence sweep | `CLAUDE.md`, `DOCS/architecture.md`, `DOCS/index.md` | `rg -n "wasm_allocator|GeneralPurposeAllocator"` over the three files is empty (`lib/memory.zig:65` is an arena over `page_allocator`; `wasm_allocator` legitimately appears only in `libcirc.md`/`libcirc-api.md` for the library's per-call arena). |
| site mirror is generated, not edited | `site/src/pages/reference/*.md` | After `bun site/scripts/sync-reference.ts`, `git status --short site/src/pages/reference/` lists only `circuit-format.md`, and its diff against `DOCS/circuit-format.md` is front-matter (`sync-reference.ts:33-41`) plus `](.md)` → `](/reference/…)` rewrites (`:24-27`). |
| existing tripwires | `tests/syntax/translate_test.zig`, `tests/analyze/analyze_golden_test.zig`, `tests/libcirc/{driver,c_api}_test.zig`, `tests/e2e/libcirc_wasm_test.zig`, `site/test/libcirc.test.ts` | All unchanged and green: the grammar comment did not move a span, the library still emits the analyze-api JSON, and the committed `libcirc.wasm` still compiles the inverter and the multi-file full adder. Per their specs neither `libcirc_wasm_test.zig` (`PHASE_3:236`, key set + native byte-equality) nor `site/test/libcirc.test.ts` (`PHASE_4:231`, equality with the manifest) pins a literal `grammar_sha256`, so slice 1 only has to refresh the wasm and the manifest *together* or not at all (Open Questions). |

**Integration tests:**

| Test Name | Scope | What It Asserts |
|-----------|-------|----------------|
| `zig build test-all` | whole repo, Node 24 on macOS | Green after every slice; the definition-of-done gate (`pr-tests.yml:53`). |
| Go-less matrix cross-compile | `build.zig` × `cli-tag.yml:22-30` targets (build line `:40`) | With `command -v go` failing under the executable-based `PATH` prune from slice 6, `zig build -Doptimize=ReleaseFast -Dtarget=x86_64-linux-gnu circ-compile`, `… -Dtarget=aarch64-macos …`, and `… -Dtarget=x86_64-windows-gnu …` each produce `zig-out/bin/circ-compile[.exe]`; proves the tag workflow (which cannot be run without pushing a tag) would build. |
| `zig build bench` (slice 5 only) | engine counters vs `tests/fixtures/bench/engine.bench.golden` | Unchanged counters after the module-graph migration (`bench-golden-gate.yml` hard-fails drift; `lib/circuit.zig`/`lib/memory.zig` are untouched by this phase, so a mismatch means the bench's own module wiring moved). |
| `bun run build` + `bun test` in `site/` (slice 1 only if the wasm was refreshed) | site | The playground still builds against the refreshed `libcirc.wasm`; `site/test/libcirc.test.ts` passes. |

Run command: `zig build test-all` (macOS, Node 24 on `PATH`; on Linux export `SKIP_WASM_E2E=1` as `pr-tests.yml:47-48` does — the `libcirc.wasm` harness honours it and reports SKIPPED). Docs-only slices (2, 3, 4) need no rebuild but the agent still runs `zig build test` before the STATUS entry, per the working loop.

## Open Questions / Spikes

- `TODO(phase5)`: does Phase 2's `version()` compute `grammar_sha256` over the raw `.peg` bytes (`@embedFile` + comptime/build-time sha)? Phase 2 has no spec at planning time (`DOCS/PLANS/` holds `PHASE_0,1,3,4,5` only); Phase 3 lists the key (`PHASE_3_libcirc_wasm.md:104`) and Phase 4's manifest copies it from `circ_version()` (`PHASE_4_renderer_sync_and_playground.md:104-108`) without saying what it hashes. If it is the raw bytes, slice 1 must refresh `site/public/wasm/libcirc.{wasm,manifest.json}` (a ~600 KB binary churn for a comment) — decide whether to fold that refresh into the next tagged release instead and, if so, record the mismatch in STATUS and keep the manifest check in slice 6. If the sha is over a comment-stripped form, nothing moves.
- Resolved from the Phase 3/4 specs (re-confirm against the shipped tests in slice 1): neither test pins a literal `grammar_sha256`. `tests/e2e/libcirc_wasm_test.zig` asserts the key set and byte-equality with the native `c_api.circ_version()` (`PHASE_3:236`); `site/test/libcirc.test.ts` asserts `circ_version()`'s `version/revision/topology_version/full_version/grammar_sha256` equal `libcirc.manifest.json` (`PHASE_4:231`) — so refreshing the wasm and the manifest together keeps it green, and refreshing neither keeps it green too; only a half refresh fails.
- `TODO(phase5)`: does `build/frontend_modules.zig` (Phase 2, unspecified at planning time — Phase 1 only provides `createParserModule(b, target, optimize)` for it, `PHASE_1:14,42`) return per-module handles (`translate`, `scan_imports`, `import_cycle`, `resolve_bodies`, `validator_run_project`, `ir_types`, `file_loader`, `full_serializer`, `serializer`, `section_writer`, `engine_session`, `truth_table_builder`, `runtime_embed`) that test roots can `addImport`, and does it accept the already-materialised `build_options` module (`build.zig:720-748` (main): `addOptions` exactly once)? If either is no, slice 5 is skipped and STATUS says why.
- `TODO(phase5)`: after the rebase onto the merged PR #79, confirm the three-way merge kept `main`'s fork sentences (`CLAUDE.md:26`, `README.md:41`, `DOCS/architecture.md:131` on `main` at `3b84bcb`; the `memories` tree still carries the older `clarete/langlang@v0.0.12` wording at `CLAUDE.md:26`, `README.md:41`, `DOCS/architecture.md:132`, and both trees at `DOCS/decisions/tooling.md:7` until slice 2 rewrites it) — the hunks are disjoint from PR #79's, so git should auto-resolve, but the sweep (`rg -n "clarete/langlang"` outside `DOCS/archive`, expected empty after slice 2) is the check.
- `TODO(phase5)`: which of the Phase 1–4 STATUS notes become "papercuts carried forward" in the archive (`ARCHIVE.md:72`): at minimum the Linux Node `init()` hang (`pr-tests.yml:41-48`), the `parser:gen` version gate being a substring match on the single literal `v0.0.13-zig.2` (`PHASE_1:277` — a tag bump touches `build.zig`, `CLAUDE.md:26`, `README.md:41`, and the `.peg` header together), the two `.linkLibC()` calls kept for `wasm_run.zig`'s `std.c.getpid`, the parser quirks pinned in Phase 0, and the deferred `circ_inspect`/CodeMirror/mem* UI items. Slice 6's Notes line should list them so the archive step does not re-read 100+ STATUS lines for them.
- None otherwise — the ten decisions in `DOCS/PLANS_PROMPT.md` cover every choice this phase records; their placement (1, 2, 9 → `tooling.md`; 3–8, 10 → `libcirc.md`) is fixed by the plan prompt's "Locked design decisions" preamble and Phase Index row 5.
