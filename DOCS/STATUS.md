# Status — libcirc (compiler front end as a library)

Append-only log, one entry per shipped slice. Newest at the bottom. See `DOCS/PLANS_PROMPT.md` for the phase index and `DOCS/PLANS/PHASE_<N>_*.md` for each phase's spec.

## 2026-09-08 — Phase 0 — Slice 1: `Errors (n)` section in the AST dump

**What shipped:** `tests/helpers/ast_dump.zig::dumpFile` prints a trailing `Errors (n)` block, one `  Error "<message>" [f<id>:<sl>:<sc>-<el>:<ec>]` line per `ast.File.errors` entry with the raw span (zero-width marks stay `[f0:2:9-2:9]`; the `--analyze` widening is not applied). The 27 `expected-ast` goldens were regenerated; every diff is the single new last line `Errors (0)`.
**Files touched:** `tests/helpers/ast_dump.zig`, `tests/syntax/translate_test.zig`, `tests/fixtures/expected-ast/*.txt` (27), `DOCS/STATUS.md` (new).
**Tests:** added `errors: clean source has no marks` and `errors: truncated bus carries busvalue then busclose marks` (the latter pins the unwidened `2:9-2:9` spans on `input a\nand g(a=` without a trailing newline). `zig build test` green; `git diff --stat tests/fixtures/expected-ast` = 27 files, 27 insertions; `git diff … | grep '^+' | sort -u` = exactly `+Errors (0)`; nothing else under `tests/fixtures` moved; `expected-inspect/*` untouched (`lib/cli/inspect_dump.zig` is the CLI copy and does not gain the section).
**Next slice:** Slice 2 — the five label-recovery fixtures (`recovery_busvalue_eof` with no trailing newline, `recovery_busvalue_newline`, `recovery_busclose_midline`, `recovery_busclose_newline`, `recovery_empty_ports`) with goldens reviewed against the blocks in `PHASE_0`, plus the whitespace-only `InvalidProgram` test and the no-trailing-newline guard.
**Notes:** None.

## 2026-09-08 — Phase 0 — Slice 2: label-recovery fixtures

**What shipped:** Five `tests/fixtures/circuits/recovery_*.circ` sources with `expected-ast` goldens generated by the Go parser and checked byte for byte against the blocks in `PHASE_0` *Data & State*: `recovery_busvalue_eof` (16 B, no trailing newline; 2 marks, both `[f0:2:9-2:9]`), `recovery_busvalue_newline` (2 marks at `[f0:3:1-3:1]` — Spacing eats the newline before `PortRef`), `recovery_busclose_midline` (2 marks: zero-width `busclose` at `2:11`, then `RecoverLine` `[f0:2:11-2:15]`; `g` survives with `NamedRef a.out [f0:2:9-2:11]` absorbing the trailing space), `recovery_busclose_newline` (1 mark at `[f0:3:1-3:1]`; `NamedRef a.out [f0:2:9-3:1]` absorbs the newline and the following `output` still parses), `recovery_empty_ports` (4 marks: `busname` `[f0:2:10-2:11]`, `busassign` eats the entire next line `[f0:3:1-3:8]`, `busvalue`/`busclose` at `[f0:4:1-4:1]`; both declarations vanish).
**Files touched:** `tests/fixtures/circuits/recovery_{busvalue_eof,busvalue_newline,busclose_midline,busclose_newline,empty_ports}.circ`, `tests/fixtures/expected-ast/recovery_*.txt` (5), `tests/syntax/translate_test.zig`, `DOCS/STATUS.md`.
**Tests:** five fixture-table rows; added `edge: whitespace-only .circ is InvalidProgram` and `recovery: busvalue_eof fixture has no trailing newline` (guards the last byte `=`). `zig build test` green; `git status --short tests/fixtures` listed exactly the 10 new files.
**Next slice:** Slice 3 — the seven `RecoverLine`/quirk fixtures (`junk_line`, `keyword_prefix`, `connection_line`, `import_empty_path`, `width_overflow`, `multibyte_columns`, `trailing_ws_baseref`) and the corrected `#`-operator comment at the `subscript: name [i] with space` test.
**Notes:** None — every golden equalled its spec block on the first regeneration.

## 2026-09-08 — Phase 0 — Slice 3: RecoverLine and quirk fixtures + comment fix

**What shipped:** Seven more `recovery_*.circ` fixtures with Go-parser goldens, each byte-equal to its `PHASE_0` block: `recovery_junk_line` (one `RecoverLine` mark `[f0:2:1-2:13]`, all declarations kept), `recovery_keyword_prefix` (`andx g(...)` → `and` + instance `x`, then `RecoverLine` `[f0:2:6-2:17]`), `recovery_connection_line` (line 2 is a `Connection` dropped without a mark; line 3's inner `busclose` throw is discarded by backtracking, leaving exactly one `RecoverLine` mark `[f0:3:1-3:11]`), `recovery_import_empty_path` (`import x ""` is **kept** with `path="`, `Errors (0)`), `recovery_width_overflow` (every declaration with a 300 literal drops silently; `Input b width=[255]` survives, `Errors (0)`), `recovery_multibyte_columns` (byte columns: the mark ends at `2:15` on a 12-code-point line), `recovery_trailing_ws_baseref` (`NamedRef` spans absorb trailing whitespace; `a [2]` is `Indexed bit=2 [f0:3:16-3:19]`). The `subscript: name [i] with space` test is renamed and its comment now states the real cause (Spacing consumed by `BaseRef`'s failed `('.' Identifier)?`, not an ignored `#`).
**Files touched:** `tests/fixtures/circuits/recovery_{junk_line,keyword_prefix,connection_line,import_empty_path,width_overflow,multibyte_columns,trailing_ws_baseref}.circ`, `tests/fixtures/expected-ast/recovery_*.txt` (7), `tests/syntax/translate_test.zig`, `DOCS/STATUS.md`.
**Tests:** seven fixture-table rows (table now 39 entries). `zig build test` green; `git status --short tests/fixtures` listed exactly the 14 new files.
**Next slice:** Slice 4 — `tests/analyze/analyze_golden_test.zig` + `expected-analyze/{clean,truncated,junk_line,empty}.json`, the `analyze_golden_tests` block in `build.zig`, the `tests/README.md` line, and the fork's `diff-circ.sh` cross-check over this checkout.
**Notes:** None — all seven goldens equalled their spec blocks on the first regeneration.

## 2026-09-08 — Phase 0 — Slice 4: `--analyze` JSON goldens + build wiring + fork cross-check

**What shipped:** `tests/analyze/analyze_golden_test.zig` renders `analyze.renderJson(analyze.analyze(...))` for the four LSP overlays (`clean`, `truncated`, `junk_line`, `empty`) and compares byte for byte against `tests/fixtures/expected-analyze/*.json` (1320 / 959 / 1387 / 101 bytes, each equal to its `PHASE_0` block). `build.zig` gains the `analyze_golden_tests` artifact (named, so it shows as `run test analyze_golden_tests` in `--summary all`) wired into `test_step` after `analyze_tests`. `tests/README.md` lists `expected-analyze/`.
**Files touched:** `tests/analyze/analyze_golden_test.zig` (new), `tests/fixtures/expected-analyze/{clean,truncated,junk_line,empty}.json` (new), `build.zig`, `tests/README.md`, `DOCS/STATUS.md`.
**Tests:** `analyze json goldens for the lsp overlays`. `zig build test` green with the new step in the run. Fork cross-check: `CIRC_ROOT=<this checkout> langlang/go/zig/scripts/diff-circ.sh -v` → `TestGenZigDifferentialCorpus` 156/156 PASS (144 pre-existing + 12 `recovery_*`), `TestGenZigTablesMatchVendoredGo` PASS, 0 mismatches.
**Next slice:** Phase 0 is complete after the `zig build test-all` gate. Next is Phase 1 (`DOCS/PLANS/PHASE_1_vendor_zig_parser.md`): vendor the langlang-generated `lib/parser/parser.zig` and retire the Go/cgo archive; acceptance is `zig build test` green with `git diff --stat tests/fixtures` empty.
**Notes:** `build.zig` on this branch is not `zig fmt`-clean (pre-existing trailing whitespace around `:703-800`); the slice-4 hunk was applied without reformatting the file so the diff stays 22 insertions. For Phase 1: `grep -c 'linkParserArchive(b, ' build.zig` = 28 and `grep -c 'tests/helpers/golden.zig' build.zig` = 14 now.

## 2026-09-08 — Phase 1 — Slice 1: cut over to the generated Zig parser

**What shipped:** `lib/parser/parser.zig` vendored from `langlang v0.0.13-zig.2` (`-grammar lib/grammar/proto-circ.peg -disable-capture-spaces -output-language zig`; 1,639 lines / 75,915 B, sha256 prefix `61daddd228c39aeb`, header `Runtime: langlang_runtime.zig abi=1 sha256=007bc786…`; `zig fmt --check` clean). `build.zig`: `createParserModule(b, target, optimize)` replaces `linkParserArchive`; `parser:gen` re-pointed at the Zig backend behind a `langlang -version` gate (`expect_stdout_match = "v0.0.13-zig.2"`, `has_side_effects`); `parser_mod` imported into `translate_mod` as `parser`; a named `parser_tests` artifact (the generated `langlang tables` test) in `test`; the `parser:archive` step, the `GOOS/GOARCH/CGO_ENABLED/CC` block, 28 archive-link calls, 26 `.linkLibC()` calls and all 28 `addIncludePath` pairs deleted (`linkLibC()` survives only on `emit_behavior_tests` and `project_behavior_tests` for `wasm_run.zig:12` `std.c.getpid`). `lib/syntax/translate.zig`: `TranslationContext.tree: *const parser.runtime.Tree`, the six accessors are one-liners over the tree, `Range` is the runtime's `usize` range (the `@intCast`s went), `NodeType_*` → `.string/.sequence/.node/.err`, `pub const circ_label_messages` + `labelMessage` loop + comptime guards (every bound label exists in the grammar; every recovery label has a message; `entry_rule == .Program`; no LR rules), `translateTree` uses `tree.root() orelse error.ParsingFailed`, `parseSourceCapturing` runs a per-call `parser.Parser` bound to the message table, and the dead `translate()` is gone.
**Files touched:** `lib/parser/parser.zig` (new, generated), `build.zig`, `lib/syntax/translate.zig`, `DOCS/STATUS.md`. `lib/parser/{go.mod,parser.go,shim/}`, `lib/syntax/CParser.zig` and `lib/syntax/nodes/declaration.zig` stay on disk, unreferenced, until slice 2.
**Tests:** `zig build test` and `zig build test-all` green with `git status --porcelain tests/fixtures` empty (all 39 `expected-ast`, the 4 `expected-analyze`, and every ir/zig/wasm/diagnostics/preview/truth-table golden byte-identical through the Zig parser). Comptime guard proof: misspelling `busclose` fails the build with `translate.zig:641:13: error: translate.zig binds a label the grammar does not define: buscloze`. Go-free proof: with the Go directory pruned from `PATH`, `rm -rf .zig-cache zig-out && zig build circ-compile --verbose` succeeds and spawns no `go build`. `zig build parser:gen && git diff --exit-code lib/parser/parser.zig` → 0 (idempotent); with a fake `langlang` printing `Version: v0.0.12` the gate fails with `expected to find in stdout` and leaves `parser.zig` untouched. Fork differential: `TestGenZigDifferentialCorpus` 156/156 PASS, `TestGenZigTablesMatchVendoredGo` PASS (last run possible against `parser.go`; slice 2 deletes it).
**Next slice:** Slice 2 — `git rm` the Go sources and `CParser.zig`/`nodes/declaration.zig`, drop `.gitignore:6-7`, rewrite the prerequisite in `CLAUDE.md`/`README.md`/`DOCS/index.md`, add the `Remove Go from PATH` step to `pr-tests.yml`, and replay the `cli-tag.yml` matrix locally.
**Notes:** Sizes on this machine (aarch64-macos): `circ-compile` Debug 4,880,400 B (the `main` checkout's Debug binary with the Go archive is 6,605,840 B), ReleaseFast 1,572,128 B. The `2,186,432 B` figure in `PHASE_1` was not reproducible here. `build.zig` still carries the pre-existing trailing whitespace at `:703-800`; the edits were applied without reformatting so the diff is reviewable.

## 2026-09-08 — Phase 1 — Slice 2: delete the Go sources, drop the prerequisite from docs, prove it in CI

**What shipped:** `git rm` of `lib/parser/{go.mod,parser.go,shim/shim.go}`, `lib/syntax/CParser.zig` and the dead `lib/syntax/nodes/declaration.zig`; the untracked `parser.a`/`parser.h` removed and their `.gitignore` lines dropped (`git ls-files lib/parser` = `parser.zig`; `lib/syntax` = `ast.zig span.zig translate.zig`). `CLAUDE.md` (project sentence, toolchain list without Go, `parser:archive` row gone, `parser:gen` row names the version gate, pipeline diagram says "vendored Zig"), `README.md` (Go bullet gone, langlang bullet re-pointed at `parser.zig`), `DOCS/index.md` (`parser/` and `syntax/` rows). `.github/workflows/pr-tests.yml` gains a `Remove Go from PATH` step between the Node install and `Run all tests` that prunes every `PATH` directory holding an executable `go`, exports the pruned `PATH`, and fails the job if `go` still resolves.
**Files touched:** `.gitignore`, `.github/workflows/pr-tests.yml`, `CLAUDE.md`, `README.md`, `DOCS/index.md`, `DOCS/STATUS.md`; deleted `lib/parser/go.mod`, `lib/parser/parser.go`, `lib/parser/shim/shim.go`, `lib/syntax/CParser.zig`, `lib/syntax/nodes/declaration.zig`.
**Tests:** `grep -rn "CGo\|parser\.a\|CParser\|go build\|parser:archive\|Go 1\.21" CLAUDE.md README.md build.zig .gitignore DOCS/index.md .github` is empty. Local replay of the CI recipe with the Go directory pruned from `PATH`: `! command -v go && zig build test-all --summary all` green (62 test artifacts run), `git status --porcelain tests/fixtures` empty. Tag-matrix cross-builds from this one Zig install, ReleaseFast: `x86_64-linux-gnu` 6,877,232 B, `aarch64-macos` 1,572,128 B, `x86_64-windows-gnu` 2,024,448 B (`.exe`, plus a 4,415,488 B `.pdb`). `cli-tag.yml` needed no edit.
**Next slice:** Phase 1 complete. Next is Phase 2 (`DOCS/PLANS/PHASE_2_host_free_front_end_and_libcirc.md`): host-free front end, `lib/libcirc.zig` driver, native static library. Its spec assumes PR #79 (native memories) is merged; if it is not yet, the phase runs against this branch's pre-#79 tree and the rebase notes in `PHASE_1` Warning 3 apply later.
**Notes:** The fork's `diff-circ.sh` can no longer run `TestGenZigTablesMatchVendoredGo` against this checkout (it reads `lib/parser/parser.go`); reruns need `git show 4ce729b:lib/parser/parser.go > <tmp>/parser.go` and `CIRC_PARSER_GO` pointed at it — a langlang-side env override is the tidy fix. The first real run of the `Remove Go from PATH` step happens on the PR.

## 2026-09-08 — Phase 2 — Slice 1: overlay-first file loader

**What shipped:** `lib/resolver/file_loader.zig` consults the overlay by a normalised key (`normalizeKey` = `std.fs.path.resolvePosix` over the path) *before* `realpath`; `resolveImportPath` gains an `overlay` parameter, joins with `resolvePosix`, and returns the key when the overlay holds it, so an overlay-only sibling import resolves with no disk access; disk branches are compiled out on `.freestanding` (`has_disk`) and fall through to `error.FileNotFound` → `E009`. `scan_imports.zig` passes its overlay through. `analyze.zig` exposes `convertDiagnostic`/`convertDiagnostics` (split out of `appendDiag`) and `writeJsonString` as `pub` for the libcirc response codec.
**Files touched:** `lib/resolver/file_loader.zig`, `lib/resolver/scan_imports.zig`, `lib/analyze/analyze.zig`, `tests/resolver/file_loader_test.zig`, `tests/resolver/scan_imports_test.zig`, `build.zig` (the scan_imports test module imports `file_loader`), `DOCS/STATUS.md`.
**Tests:** added `resolveImportPath overlay-only sibling returns the key`, `resolveImportPath normalises dot segments`, `resolveImportPath disk sibling still realpaths`, `loadFileWithOverlay hits the overlay for a never-saved key`, `scan imports resolves an overlay-only sibling without E009` (7 files, `import_table[0].target_file == 1`), `analyze: overlay-only sibling import resolves without E009`. `zig build test` green; no file under `tests/fixtures` moved (the Phase 0 `expected-analyze` goldens and every diagnostics snapshot are byte-identical).
**Next slice:** Slice 2 — freestanding guards (`color.zig` `.auto`, `builder.zig` `drive_ns`, `resolve_bodies.zig` `std.debug.print`, `log.zig` arena) and `memory.reset()`.
**Notes:** This branch is pre-PR #79, so the memories-specific parts of `PHASE_2` (`Preload`, `writeImageError`, `stateful_ram` refusal, `rom` preloads in requests) do not exist here and are deferred to the post-#79 rebase; every other row is executed as written. One observable change on native: `files[].path` for an overlay-keyed file is now the normalised key, not its realpath (documented in slice 8).

## 2026-09-08 — Phase 2 — Slice 2: freestanding guards and `memory.reset`

**What shipped:** `lib/preview/render/color.zig` — `.auto` is `false` on `.freestanding` before the `isatty` call; `lib/truth_table/builder.zig` — `drive_ns` is measured only when `has_clock` (0 on freestanding; the `Table.drive_ns` doc says so); `lib/resolver/resolve_bodies.zig` — the specialization-failure `std.debug.print` is now `std.log.scoped(.resolver).warn` (no stderr on freestanding; the CLI's log filter formats it as `warning(resolver): …`); `lib/log.zig` — the wasm log arena is backed by `std.heap.page_allocator` instead of `memory.allocator` so it cannot dangle across a reset; `lib/memory.zig` — `pub fn reset()` (`arena.reset(.free_all)`, counters untouched) and the test-only `arenaCapacityForTest()`.
**Files touched:** `lib/preview/render/color.zig`, `lib/truth_table/builder.zig`, `lib/resolver/resolve_bodies.zig`, `lib/log.zig`, `lib/memory.zig`, `lib/circuit.zig` (test only), `DOCS/STATUS.md`.
**Tests:** added `memory.reset frees the arena and the allocator stays usable` (`lib/circuit.zig`: build, deinit, reset → capacity 0, rebuild and propagate `1 & 1 == 1`) and `truth_table_build_records_drive_ns_on_native` (`builder.zig`). `zig build test` green, no fixture moved; `zig build bench` → `golden matches` (56 fixtures, counters byte-identical).
**Next slice:** Slice 3 — `build/frontend_modules.zig` sharing the front-end module graph between the CLI and the future `libcirc` module; `build_info` materialised once with `grammar_sha256` and `parser_runtime_sha256`.
**Notes:** The `engine_session`/`sim/loop.zig` image-error move in the spec is memories-only (`writeImageError`, `Preload`, `ImageError` do not exist pre-#79) and is deferred to the post-#79 rebase.
