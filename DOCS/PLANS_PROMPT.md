# Plan Prompt — (archived) Prebuilt inprocess / FFI initiative

**Status:** **Superseded (2026-05)** as an active roadmap; defaults unchanged: **`zig build wasm` subprocess**. **`zig build inprocess-lib`** remains available **without vendoring**: **`ZIG_COMPILER_SRC`** points at a ziglang/zig checkout; installed Zig **`lib/`** is documented for callers — see `DOCS/decisions/compiler-pipeline.md` and `DOCS/getting-started.md`.

Historical phase specs remain under `DOCS/PLANS/PHASE_*.md` for reference only.
