# Status — Native Memories (ROM & RAM)

Append-only log, one entry per shipped slice. Newest at the bottom. See `DOCS/PLANS_PROMPT.md` for the phase index and `DOCS/PLANS/PHASE_<N>_*.md` for each phase's spec.

## 2026-09-07 — Phase 0 — Slice 1: IR variant + resolver + compile-forced arms

**What shipped:** `ir.ComponentKind.memory` (`Memory { mode, data_width, addr_width, arg_count, type_width_given }`); the resolver recognises `rom`/`ram` before primitives and import aliases and resolves both `width_args` through `widthFromSpec` (parametric `[W, A]` binds); every exhaustive `ComponentKind` switch gained its arm — dump text (`kind=rom[W=8,A=4]`), analyze symbol kind + hover (`rom[8,4] code`), port validity, `endpointWidth` via the new `memoryPortWidth` helper, `MemoryNotYetSupported` in both topology serializers (temporary, removed in Phase 2), `MemoryUnsupportedInEmitZig` in emit-zig (permanent). Every artifact mode exits 1 with no output file; `--inspect` and `--analyze` resolve memories with zero diagnostics.
**Files touched:** `lib/ir/types.zig`, `lib/ir/resolver.zig`, `lib/validator/passes/memory_validation.zig` (new, helper only), `lib/validator/passes/port_validation.zig`, `lib/validator/passes/sub_circuit_validation.zig`, `lib/cli/inspect_dump.zig`, `lib/analyze/analyze.zig`, `lib/topology/serializer.zig`, `lib/topology/full_serializer.zig`, `lib/emit/build_fn.zig`, `lib/emit/project.zig`, `build.zig` (memory_validation module wiring), `tests/helpers/ir_dump.zig`, `tests/syntax/translate_test.zig`, `tests/ir/resolver_test.zig`, `tests/validator/structural_passes_test.zig`, `tests/validator/codes_snapshot_test.zig`, fixtures `circuits/{rom,ram}_basic.circ`, `expected-ast/{rom,ram}_basic.txt`, `expected-ir/{rom,ram}_basic.txt`, `projects/memory_parametric/{root,mem_wrap}.circ`, `expected-diagnostics/memory_parametric_clean.txt`
**Tests:** added `rom/ram resolve as memory components before import aliases`, `memory width args bind through parametric width bindings`, `memory records arity and type-position width for the validator` (resolver), `analyze: memory declarations resolve to rom/ram symbols with hover`, `memoryPortWidth contract` (wired into the structural-passes root), AST/IR golden entries `rom-basic`/`ram-basic`, project snapshot `memory_parametric` (clean); ran `zig build test`, result pass (402/402; baseline 397/397, every pre-existing golden byte-identical)
**Next slice:** Slice 2 — `E017`/`E018` in `codes.zig` + `memory_validation.run` wired into `validator/run.zig`, fixtures for each trigger, snapshot rows, and the `E001-E016` → `E001-E018` docs sweep.
**Notes:** The CLI currently prints the raw error names (`topology serialization failed: MemoryNotYetSupported`, `emission failed: MemoryUnsupportedInEmitZig`); the friendly `rom/ram are not yet supported in this mode` line is slice 4. `memory_validation.zig` is imported only by `sub_circuit_validation` for now; slice 2 adds it to `validator/run.zig`, slice 3 to `port_validation`. Zig's exhaustive switches forced the serializer/emit arms into this slice; the else-guarded passes (`required_input`, `combinational_loop`, `name_collision`, `unused_import`) are untouched until slice 3.

## 2026-09-07 — Phase 0 — Slice 2: E017/E018 + code registry

**What shipped:** `E017` (memory parameter list malformed) and `E018` (memory width out of range) appended to `codes.zig` with their `templates` rows; `memory_validation.run` wired into `validator/run.zig` right after `name_collision`. `E017` fires once per component for `arg_count != 2` (0 args carry a `write 'rom m[W, A](...)'` hint; the `rom a, b` identifier-list form reports per name) and for a type-position width (`rom[8] m[8, 4]`); `E018` fires per out-of-range width (`W` in 1..64, `A` in 1..16) and is suppressed when `E017` fired. Every doc that advertised `E001-E016` now says `E001-E018`, and CLAUDE.md's code table gained the two rows.
**Files touched:** `lib/validator/codes.zig`, `lib/validator/passes/memory_validation.zig`, `lib/validator/run.zig`, `build.zig`, `tests/validator/run_test.zig`, `tests/validator/codes_snapshot_test.zig`, `tests/validator/structural_passes_test.zig`, fixtures `circuits/E017_memory_{no_widths,one_width,type_width,ident_list}.circ`, `circuits/E018_memory_width_range.circ` and their `expected-diagnostics/` goldens; docs `README.md`, `CLAUDE.md`, `DOCS/{analyze-api,architecture,circuit-format,index,language,sim-protocol}.md`, `site/src/pages/reference/circuit-format.md`
**Tests:** added `memory_validation: rejects wrong arity`, `memory_validation: rejects type-position width`, `memory_validation: width bounds` (inline), five `run_test` fixture entries and five `codes_snapshot` single-file rows; ran `zig build test`, result pass (405/405; every pre-existing golden byte-identical)
**Next slice:** Slice 3 — reused-code arms: `required_input` (`E004`), `port_validation` per-port `E014` via `memoryPortWidth`, `combinational_loop` policy (`ram` breaks cycles, `rom` does not), `name_collision` + `scan_imports` reservation (`E006`/`E011`), with fixtures for each.
**Notes:** **Zig collects tests only from a test root's own module.** Slice 1's `test { _ = memory_validation; }` in `structural_passes_test.zig` never ran the pass's inline tests (the +1 it produced was the anonymous block itself); it is removed and `memory_validation_mod` is now its own `addTest` root in `build.zig`. This is the same mechanism Phase 1 slice 1 applies to `lib/circuit.zig`. `TODO(phase0)` on the identifier-list form is resolved: one `E017` per name, because the translator already emits one component per name (`translate.zig:622-631`).

## 2026-09-07 — Phase 0 — Slice 3: reused-code arms and name reservation

**What shipped:** `required_input` reports `E004` for a memory's missing ports (rom: `addr`; ram: `addr`, `din`, `we`, `clk`); `port_validation` computes `E014` through a per-endpoint `endpointWidth` helper (primitives keep `Component.width`, memories use `memoryPortWidth`, everything else has no in-module contract) so `addr`/`din`/`we`/`clk`/`out` are checked at their own widths; `combinational_loop` treats `ram` as cycle-breaking and `rom` as combinational; `rom`/`ram` are reserved in `name_collision.isBuiltinName` (`E006`) and `scan_imports.isBuiltinAlias` (`E011`). `memoryPortWidth` returns no contract while the `[W, A]` list is malformed, so `E017` fixtures do not cascade into placeholder-width `E014`s.
**Files touched:** `lib/validator/passes/{port_validation,required_input,combinational_loop,name_collision,memory_validation}.zig`, `lib/resolver/scan_imports.zig`, `build.zig` (`memory_validation_mod` moved ahead of `port_validation_mod`), `tests/validator/{run_test,codes_snapshot_test}.zig`, fixtures `circuits/{E002_memory_unknown_port,E004_memory_missing_ports,E014_memory_port_width,E006_shadows_memory,E008_rom_loop,clean_ram_feedback}.circ`, `projects/E011_memory_alias/{root,rom}.circ`, their `expected-diagnostics/` goldens, and two slice-2 goldens that legitimately gained lines (`E017_memory_ident_list` now also reports `E004 'addr'` per name; `E018_memory_width_range` also reports the real `E014` against the user's width 20)
**Tests:** added seven `run_test` rows, six `codes_snapshot` single-file rows and one project row, and a malformed-list case in `memoryPortWidth contract`; ran `zig build test`, result pass (405/405; every golden outside the two named above byte-identical)
**Next slice:** Slice 4 — `reportBackendError` in `cmd/circ-compile/main.zig` mapping `MemoryNotYetSupported`/`MemoryUnsupportedInEmitZig` to `rom/ram are not yet supported in this mode`, CLI rejection tests for the five artifact modes, `--inspect` AST `width_args` line + `expected-inspect/rom_basic.txt`, `DOCS/language.md` §3.5.
**Notes:** `E008_rom_loop` and `clean_ram_feedback` deliberately loop through a `wire`, not a gate — `and`/`not` are already cycle-breaking (`combinational_loop.zig:15-23`), so a loop through a gate would never reach the memory policy. `E011_memory_alias` shows the full cascade for an existing `import rom`: the alias collides (`E011`), and the body's `rom r(x = …)` is then read as a memory declaration, so the fix for such a file is renaming the alias.

## 2026-09-07 — Phase 0 — Slice 4: CLI rejection surface, inspect polish, docs (phase complete)

**What shipped:** `reportBackendError` in `cmd/circ-compile/main.zig` replaces the twelve `<context>: <ErrorName>` prints at every backend `catch` (topology build, min/full serialization, emission) and maps `MemoryNotYetSupported` / `MemoryUnsupportedInEmitZig` to the single line `rom/ram are not yet supported in this mode`; `--inspect`'s parse-tree dump prints `width_args=[8, 4]` on component lines; `DOCS/language.md` §3.5 documents the declaration shape, port table, width bounds, reserved names, and the not-yet-compilable status.
**Files touched:** `cmd/circ-compile/main.zig`, `lib/cli/inspect_dump.zig`, `tests/cli/integration_test.zig`, `tests/fixtures/expected-inspect/rom_basic.txt`, `DOCS/language.md`, `DOCS/STATUS.md`
**Tests:** added `cli inspect memory fixture exits 0 and matches golden stdout` and `cli memory sources are rejected in every artifact mode` (five spawned invocations: `-o`, `--emit-zig -o`, `--preview`, `--truth-table`, `--sim`; each exits 1, empty stdout, the rejection line on stderr, no output file); ran `zig build test`, result pass (407/407)
**Next slice:** Phase 1 slice 1 — engine test root in `build.zig` plus the latent `lib/circuit.zig:1925` assertion fix (see `DOCS/PLANS/PHASE_1_engine_memories_and_image_codec.md`).
**Notes:** Phase 0 is complete: every row of its Slices table shipped. Both `TODO(phase0)` markers are resolved — `dead_code`/`name_resolution` need no memory arms (`rom_basic` inspects with 0 warnings), and the identifier-list form reports per name. The `--analyze` zero-diagnostics proof lives in the inline `analyze` test rather than a spawned-CLI case, because the CLI test helper cannot feed stdin (`Child.run` uses `.Ignore`). Phase 2 must remove the `MemoryNotYetSupported` arms **and** flip the rejection test in the same slice, or `zig build test` goes red between commits (Phase 2 spec, warning 6).

## 2026-09-07 — Phase 1 — Slice 1: engine test root

**What shipped:** `lib/circuit.zig` is now its own `addTest` root in `build.zig` (`engine_tests`, attached to `test_step` after the interpreter tests), with a `test { _ = transport; }` hook so `transport.zig`'s inline test is collected through the relative import. The latent slice test at `lib/circuit.zig:1925` expected the raw shifted value `0b11`; `Pool.write` canonicalises `value & defined` on commit, so the engine returns `0b01` — the expectation and its explanatory comment now say so.
**Files touched:** `build.zig`, `lib/circuit.zig`
**Tests:** no new tests written; 52 previously never-executed tests now run (50 engine + the hook block + 1 transport). Verified the HEAD version of `circuit.zig` fails that one test when run as a root (standalone `zig test` with a scratch `build_options`), and the fixed version passes; ran `zig build test`, result pass (459/459)
**Next slice:** Phase 1 slice 2 — `lib/memimage.zig` raw image codec (`bytesPerWord`, `maxImageSize`, `validate`, `decode`, `encode`, `MemoryImageError`) with `pub const memimage` re-exported from `circuit.zig`.
**Notes:** `memory.zig`, `transport.zig`, and `log.zig` are relative imports of `circuit.zig`, so they live in the same module and their tests are collectable from this root; the pass modules under `lib/validator/passes/` are separate `createModule`s and are not (see the slice-2 note in Phase 0).

## 2026-09-07 — Phase 1 — Slice 2: raw image codec

**What shipped:** `lib/memimage.zig` — the headerless little-endian raw image codec (decision 2): `bytesPerWord`, `maxImageSize`, `validate` (checks length is a whole number of words → word count fits `2^A` → no bits at or above W; touches no plane), `decode` (validate, then replace-all: loaded words fully defined, the tail undefined; a rejected image leaves both planes untouched), `encode` (writes `value & defined`), and `MemoryImageError { LengthNotWordMultiple, WordExceedsWidth, TooManyWords }`. `lib/circuit.zig` re-exports it as `pub const memimage` and collects its tests through the engine root.
**Files touched:** `lib/memimage.zig` (new), `lib/circuit.zig`
**Tests:** added eight inline tests (`bytesPerWord and maxImageSize`, `round-trip` incl. W=64, `W=64 has no padding bits`, `short image leaves tail undefined` incl. the empty image, `LengthNotWordMultiple leaves planes untouched`, `WordExceedsWidth`, `TooManyWords`, `encode writes value & defined`); `zig test lib/memimage.zig` standalone 8/8; ran `zig build test`, result pass (467/467). `zig-out/lib/circ-runtime.wasm` rebuilt from the wasm module graph with no `build.zig` change, so the relative import resolves on both targets.
**Next slice:** Phase 1 slice 3 — `ComponentType.memory` with asynchronous read (`MemoryMode`, `MemCells`, payload, plane allocation in `createComponent`, `connect` arms, recalc arm without the RAM block, `memoryReadOut`, `memoryCells`, `transport.kindByte` 8/9, `toKind` deleted, doc corrections).
**Notes:** Nothing references the codec yet; Phase 1 slice 5's `memoryLoadImage`/`memoryStoreImage` are its first engine callers, Phase 2's `memBuffer` sizes its staging buffer from `maxImageSize`, and Phase 3 reaches `validate` through `engine.memimage`.

## 2026-09-07 — Phase 1 — Slice 3: memory kind with asynchronous read

**What shipped:** `ComponentType.memory` — one engine kind carrying `mode: MemoryMode { rom, ram }` (decision 1) with the payload `{ mode, cells: MemCells, addr, din, we, clk, prev_clk }`; `pub const MemCells { addr_width, values, defined }` planes allocated (zeroed, `1 << A` words each) inside `createComponent`, which returns `error.InvalidAddrWidth` for `A ∉ 1..16` and asserts callers left the slices empty; `deinit` frees the planes; `connect` accepts `addr` on both modes and `din`/`we`/`clk` only on a ram; the recalc arm evaluates `memoryReadOut` (`cells[addr]` when every address bit is defined, else fully undefined) at gate delay through the existing `else`; `pub fn memoryCells` exposes the planes read-only; `pub const MAX_ADDR_WIDTH = 16`, `pub const MemoryError`. `transport.kindByte` maps `mode` to the wire bytes 8/9. Dead `toKind` deleted (its numbering contradicted the wire format); `CLAUDE.md`, `DOCS/simulation-engine.md`, and `DOCS/architecture.md` now describe nine kinds and cite `format.zig` as the encoding truth.
**Files touched:** `lib/circuit.zig`, `lib/transport.zig`, `CLAUDE.md`, `DOCS/simulation-engine.md`, `DOCS/architecture.md`
**Tests:** added `memory: unloaded cells read undefined`, `memory: async read through undefined addr`, `memory: connect policy per mode`, `memory: createComponent rejects addr_width out of range`, `memory: delay class is gate delay` (engine), `transport: memory kind bytes`; ran `zig build test`, result pass (473/473); `grep -rn toKind` over `.zig`/`.md` is empty outside the plan files
**Next slice:** Phase 1 slice 4 — the RAM rising-edge write inside the recalc arm (`prev_clk` stored before acting, width-agnostic `we`/`clk` bit tests, write only with `we` defined-high and `addr` fully defined, partial-X `din` stored canonically).
**Notes:** The recalc arm switches on `component.kind` by value today (`.memory => { … }`); slice 4 changes that arm to a `|*m|` pointer capture so it can update `prev_clk` and the planes through the mutable `*Component` while `circuit` stays `*const`. Tests fill cells directly through the payload for now; slice 5's host hooks replace that in later tests.

## 2026-09-07 — Phase 1 — Slice 4: RAM rising-edge write

**What shipped:** The `.memory` recalc arm now captures the payload by pointer and, for a ram, commits `din` into `cells[addr]` on a defined-low → defined-high `clk` transition while `we` is defined-high and `addr` is fully defined (decision 5). `prev_clk` is stored before acting on every recalc, so Phase 2's repeated recalcs within one timestep cannot double-fire and a `we`/`din` change while `clk` stays high never writes; X→1 is not an edge. `we`/`clk` are read with width-agnostic bit-0 tests (`bit0High`/`bit0Low`) instead of `isHigh`/`isLow`, which assert `width == 1`. Partial-X `din` is stored canonically (`value & defined`).
**Files touched:** `lib/circuit.zig`
**Tests:** added `ram: writes only on defined-low to defined-high`, `ram: requires we high and fully defined addr`, `ram: same-step din is captured`, `ram: no double fire when clk and din change together` (one input fans out to both `clk` and `din`, so the ram is recalculated twice in the step; `prev_clk` equals the committed clk after the first), `ram: level changes while clk high do not write`, `ram: partial-X din stored as-is`, `ram: width-agnostic we/clk bit test` (control port driven by a width-4 pin); ran `zig build test`, result pass (480/480)
**Next slice:** Phase 1 slice 5 — host hooks `memoryWriteWord`/`memoryClear`/`memoryLoadImage`/`memoryStoreImage` with the change-guarded `memoryRefresh` (`BitVecState.equals` before `propagateEvent`, so an untouched presented word does not bump `current_time`); closes the phase.
**Notes:** The write lands at time T (the cell array changes inside Phase 2); the word appears on `out` at T+5 through the ordinary enqueue when `addr` presents it. A `RamHarness` test helper (one input pin per port, optional wider `we`) lives with the tests for slice 5 to reuse.

## 2026-09-07 — Phase 1 — Slice 5: host memory hooks (phase complete)

**What shipped:** `Circuit.memoryWriteWord` (one word, canonicalised to W), `memoryClear`, `memoryLoadImage` (replace-all through `memimage.decode`; a rejected image leaves cells and `out` untouched and returns the codec error), `memoryStoreImage` (`value & defined` into a full-image buffer, `BufferTooSmall` otherwise), all returning `NotAMemory` for any other kind and `AddressOutOfRange` past `2^A`. Every mutator ends in the private `memoryRefresh`: it asserts the queue is empty, recomputes `out` with the same `memoryReadOut` the recalc arm uses, and calls `propagateEvent` only when the word differs under `BitVecState.equals` — so a load that does not touch the presented address neither enqueues nor advances `current_time`.
**Files touched:** `lib/circuit.zig`, `tests/helpers/wasm_run.zig`
**Tests:** added `memory: host write to presented address resyncs out` (downstream output pin follows; time advances by exactly gate + wire delay), `memory: no-op refresh does not bump current_time` (unpresented cell, identical image, identical word), `memory: load image replaces all cells and resyncs` (short image undefines the tail, empty image ≡ clear), `memory: failed load leaves cells untouched`, `memory: store writes value & defined`, `memory: clear undefines every cell`, `memory: hooks reject non-memory and out-of-range`; ran `zig build test`, result pass (487/487); ran `zig build test-all` (the phase gate), result pass (491/491) **after** adding `memimage.zig` to the emit-zig smoke's engine-file copy list in `tests/helpers/wasm_run.zig` — the smoke copies `circuit.zig` and its relative-import siblings into a scratch workspace, and the first `test-all` run failed with `unable to load 'memimage.zig'` because slice 2 had not extended that list (the fast suite never exercises it).
**Next slice:** Phase 2 slice 1 — wire format v03 (`format.ComponentKind.rom/ram`, `PortName.addr..clk`, `Aux.memory`), every compile-forced arm in serializers/decoders/interpreter/session/preview, deletion of Phase 0's `MemoryNotYetSupported` with the CLI rejection test flipped **in the same commit** (see `DOCS/PLANS/PHASE_2_topology_v03_runtime_wasm_exports.md`).
**Notes:** Phase 1 is complete: every row of its Slices table shipped and the engine is still user-invisible (Phase 0's rejection stays until Phase 2). The hooks' error sets are inferred (`!`) rather than the spec's spelled-out unions; they contain exactly `MemoryError`, `memimage.MemoryImageError`, and the allocator/queue errors, so Phase 3's `applyImage` `catch |err| switch` with `else => |e| return e` works unchanged. `TODO(phase1)` (guarding the exports against a mutator called mid-propagate) is deferred to Phase 2's export design; the engine keeps the Debug assert.

## 2026-09-07 — Phase 2 — Slice 1: wire format v03 + every compile-forced arm + CLI test flip

**What shipped:** `format.VERSION`/`full_format.FULL_VERSION` → `0x03`; `ComponentKind.rom = 8`, `.ram = 9`; `PortName.addr = 4`, `.din = 5`, `.we = 6`, `.clk = 7`; `Aux.memory { addr_width }`. `.min` memory records are 7 bytes (`aux_lo` = address width, dispatched by kind like slice's suffix); `.full` records carry one trailing `addr_width` byte. Both serializers emit memory records (module and project paths, Pass 1a/Pass 2/`resolveSignalGlobalId`), `full_decoder` reads the aux, the runtime `interpreter` materialises one engine node per record (`InvalidMemoryRecord` for `A ∉ 1..16`, `TruncatedPayload` for a missing aux byte) and exposes `memKindByte`, `engine_session.Session.build` does the same from the full topology; all five port-byte mappers plus `preview/layout/route.portByteOf` know the four new ports. Preview: `VirtualNode.addr_width` (from `Aux.memory`), `sizing.memorySize`/`memoryLabelLen`, labels `rom code[8,4]` / `ram data[8,4]`, rom = 1-input 3-row box, ram = 4-input 9-row box on the odd rows, macro colour. `serializer.ProjectTopology.memory_ids` lists root-module memories by declared name (freed by `deinit`/`serializeProject`). Phase 0's `MemoryNotYetSupported` arms are gone; `reportBackendError` keeps only the permanent emit-zig line (`--emit-zig does not support rom/ram; compile to .wasm instead`), and the CLI test flipped in the same commit: `-o`, `--preview`, `--truth-table`, `--sim` succeed on the memory fixtures, `--emit-zig` still rejects.
**Files touched:** `lib/topology/{format,full_format,serializer,full_serializer,full_decoder}.zig`, `templates/interpreter.zig`, `lib/engine_session.zig`, `lib/preview/layout/{types,collapse,sizing,place,route}.zig`, `lib/preview/render/glyphs.zig`, `lib/preview/dump.zig`, `cmd/circ-compile/main.zig`, `tests/cli/integration_test.zig`, `tests/topology/full_emit_integration_test.zig`, docs `DOCS/{circuit-format,preview}.md`, `DOCS/decisions/{compiler-pipeline,runtime-api}.md`, `site/src/pages/reference/{circuit-format,preview}.md`
**Tests:** stability tests extended (kinds 8/9, ports 4..7, version 0x03); `serialize: rom/ram records carry addr_width suffix`, `serialize: project path assigns dense ids across memories` (leak-checked `memory_ids`), `full_encode: memory record carries one addr_width aux byte`, `full_walk: buildFromModule emits memory records with aux and name`, `full_decode_rejects_v02`, `full_decode: memory aux round-trips`, `interpreter: rejects v02 payload`, `interpreter: decodes rom and ram records and wires their ports`, `interpreter: rejects truncated or out-of-range memory record`, `session builds rom and ram nodes from memory records`, `session rejects a memory record without its aux`, `sizing: memorySize …`; the 6-byte-stride walks in `serializer.zig` and `full_emit_integration_test.zig` now use a kind-aware record length; hardcoded `0x02` literals in the topology tests updated. Ran `zig build test`, result pass (499/499); every pre-existing golden byte-identical.
**Next slice:** Phase 2 slice 2 — preview render goldens for `rom_basic`/`ram_basic`, `BuildError.StatefulComponent` + the origin-agnostic `firstRamName` pre-flight so `--truth-table` refuses a ram, `rom_basic.truth.golden`.
**Notes:** Everything compiled on the first attempt except one byte-count typo in a new test, confirming the compile-forced-arms inventory in the Phase 2 spec was complete. `--truth-table` on a **ram** currently enumerates its `clk`/`we`/`din`/`addr` as inputs and produces an order-dependent table — slice 2 closes that. The committed `site/public/wasm/*.wasm` (v01) were deliberately not recompiled: `site/node_modules/circ-renderer` is pinned to full-format v01.

## 2026-09-07 — Phase 2 — Slice 2: preview goldens, truth-table policy

**What shipped:** `BuildError.StatefulComponent` and `pub fn firstRamName(topology)` in `lib/truth_table/builder.zig` — an origin-agnostic walk, so a ram nested in an imported sub-circuit (still driven from the enumerated root inputs) is refused too; `build()` returns the error before enumerating anything. The CLI pre-flights the same walk and prints `truth-table: ram '<name>' is stateful (its clk/we would be enumerated as inputs and rows would depend on visiting order); use --sim to drive it`, exit 1. Unloaded roms tabulate as `?` (existing renderer story; `--strict` exits 1). Render goldens `rom_basic.render.golden` / `ram_basic.render.golden` and the truth-table golden `rom_basic.truth.golden` lock the slice-1 glyphs and policy.
**Files touched:** `lib/truth_table/builder.zig`, `cmd/circ-compile/main.zig`, `tests/cli/integration_test.zig`, `tests/fixtures/preview/renders/{rom,ram}_basic.render.golden`, `tests/fixtures/truth_table/rom_basic.truth.golden`
**Tests:** added `truth_table_build_rejects_ram_as_stateful`, `truth_table_build_rejects_nested_ram`, `truth_table_build_rom_reads_undefined_until_loaded` (builder), `preview: rom box render golden`, `preview: ram box render golden`, `truth_table_rom_unloaded_renders_undefined_golden`, `truth_table_rejects_ram_as_stateful` (CLI in-process), and the `ram_basic --truth-table` case in the spawned CLI memory test; ran `zig build test`, result pass (506/506)
**Next slice:** Phase 2 slice 3 — the eight WASM memory exports in `templates/main.zig` with the `0 / -1..-7` status convention and per-memory staging buffers, plus the `topology_protocol_test` host-side coverage over a hand-built v03 payload.
**Notes:** `--truth-table --strict` on an unloaded rom exits 1 with `undefined output` rows, as designed; Phase 3's `--mem` preload is what makes a rom tabulate real values. The ram rejection deliberately lives in `build()` as well as the CLI pre-flight so library callers (bench, future tools) cannot produce an order-dependent table.

## 2026-09-08 — Phase 2 — Slice 3: WASM memory exports

**What shipped:** `templates/main.zig` exports `getMemInfo(id) -> (kind<<16)|(W<<8)|A`, `memBuffer(id)` (a per-memory staging buffer of `maxImageSize(W, A)` bytes, allocated once and reused because `memory.allocator` is an arena), `memLoad(id, len)`, `memStore(id)` (bytes written), `memClear(id)`, `setMemWord(id, addr, value, defined)`, and the paired `getMemValue`/`getMemDefined(id, addr)` — decision 4 verbatim, with the status convention `0` ok, `-1` uninitialised/bad id/not a memory, `-2` length not a word multiple, `-3` word exceeds width, `-4` too many words, `-5` `len` exceeds staging, `-6` `memBuffer` never called, `-7` address out of range. Ids address nodes by position exactly like `setPin`; every mutator goes through Phase 1's hooks, so `out` resyncs without a `run()`. `init()` allocates the staging table after the topology is materialised.
**Files touched:** `templates/main.zig`, `tests/e2e/topology_protocol_test.zig`
**Tests:** `topology host protocol: memory exports over a hand-built v03 payload` — five records (`rom[8,4]`, `rom[12,2]` so `-2` is reachable, `rom[4,2]` so `-3` is reachable) driven from Node: every status code, `memBuffer` idempotence, load → `setPin` → `getOutputValue` read-through (`0x33`), `setMemWord` resync without `run()`, `getMem*` bounds, `memClear`, and `memStore` writing `value & defined` (`0xab` masked `0x0f` → `0x0b`); the existing inverter test now shares the Node/skip/splice plumbing (`nodeAvailable`, `runHostScript`, a common script prologue). Ran `zig build test`, result pass (507/507). A compiled `rom_basic.wasm` lists all eight names in `WebAssembly.Module.exports`.
**Next slice:** Phase 2 slice 4 — the `expected-wasm` behavioral harness gains a `mem <name> <hexbytes>` preamble in `section_writer_fixtures_test.zig` (multi-bit bit-string tokens already exist there), plus `rom_lookup` / `ram_write_read` fixtures and `tests/README.md` grammar.
**Notes:** `-4` (`TooManyWords`) is unreachable through `memLoad`: the staging buffer is exactly one full image, so an oversize `len` is `-5` before the codec runs; the code exists for parity with `MemoryImageError` and becomes reachable through Phase 3's file-backed `load`. `TODO(phase1)` (a mutator called from inside a host callback mid-propagate) stays a Debug assert in the engine — no host callback runs during `propagate` in this runtime, so there is no path to it.

## 2026-09-08 — Phase 2 — Slice 4: behavioral harness `mem` preamble + memory fixtures

**What shipped:** `tests/e2e/section_writer_fixtures_test.zig` (the multi-bit-capable harness) parses a `Spec { preloads, steps }`: `mem <name> [<hexbytes>]` lines before the first vector load a raw image into the named root memory through the artifact's own `memBuffer`/`memLoad` exports right after `init()` (an empty token is a zero-length image = clear; odd or non-hex text is `InvalidHexImage`; a preamble after a vector is `PreloadAfterVector`; a name absent from `ProjectTopology.memory_ids` is `UnknownMemory`; a non-zero `memLoad` status fails the run). Fixtures `rom_lookup` (16-word image, read by address) and `ram_write_read` (clock pulsed as an ordinary pin: X→1 is not an edge, a defined rising edge with `we=1` writes, `we=0` pulses do not, the value persists across an address change). `tests/README.md` documents the bit-string tokens and the preamble.
**Files touched:** `tests/e2e/section_writer_fixtures_test.zig`, `tests/README.md`, fixtures `circuits/{rom_lookup,ram_write_read}.circ`, `expected-wasm/{rom_lookup,ram_write_read}.txt`
**Tests:** added `section_writer: mem preamble parsing` and the two fixture rows (run inside the existing `circuits fixtures behavioral correctness` and `combined wasm is structurally valid` loops); ran `zig build test`, result pass (508/508); all pre-existing behavioral fixtures unchanged.
**Next slice:** Phase 2 slice 5 — docs: `DOCS/wasm-api.md` memory section (exports, staging protocol, status codes incl. the `-4`-unreachable note) and the stale "setPin only enqueues" sentence, the export-list sentences in `DOCS/architecture.md`, `CLAUDE.md`, `README.md`, and the site mirror of `wasm-api.md`; closes Phase 2.
**Notes:** The phase-index amendment from the plan review holds: no `pin=0xNN/0xMM` notation was introduced — the harness's existing MSB-first bit strings carry multi-bit values, and `serializer_fixtures_test.zig` (scalar-only) is untouched. Ratified here for the record.

## 2026-09-08 — Phase 2 — Slice 5: docs (phase complete)

**What shipped:** `DOCS/wasm-api.md` (and its site mirror) gains the eight memory exports in the export block and a "Memory exports" section — the id mapping via the `.full` section, the asynchronous `out` port, the ram edge rule with the host-pulsed clock, the headerless image format, the staging protocol (allocate-once `memBuffer`, re-view `memory.buffer` after it), the `0 / -1..-7` status table with the `-4`-unreachable note, the no-`run()`-needed resync guarantee, and a Node example. The stale "`setPin` only enqueues — call `run()`" sentence now says `setPin` settles before returning. The export-list sentences in `DOCS/architecture.md`, `CLAUDE.md`, and `README.md` name the memory family, and CLAUDE.md's allocator paragraph now describes the real `ArenaAllocator`-over-`page_allocator` behaviour (it claimed `wasm_allocator`/GPA).
**Files touched:** `DOCS/wasm-api.md`, `site/src/pages/reference/wasm-api.md`, `DOCS/architecture.md`, `CLAUDE.md`, `README.md`
**Tests:** docs only; `zig build test` unaffected (508/508 at the previous slice)
**Next slice:** Phase 3 slice 1 — `engine_session.zig` gains `MemRef`, `Preload`, `collectMemories`, `validateImage`, `Session.memories`, `findMemory`, `applyImage` (see `DOCS/PLANS/PHASE_3_sim_and_truth_table_ergonomics.md`).
**Notes:** Phase 2 is complete: `circ-compile x.circ -o x.wasm` ships a v03 artifact whose host can load, mutate, and export memory contents, and every CLI mode handles memories (`--emit-zig` rejects by design). `DOCS/getting-started.md:153` and `DOCS/index.md:3` still enumerate the six original exports as "the full export list"; Phase 4 owns those files and rewrites them with the ROM walkthrough. The committed `site/public/wasm/*.wasm` remain v01 until `circ-renderer` is updated.
