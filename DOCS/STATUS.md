# Implementation Status

## 2026-05-06 — Phase 0 — Slice 1: `min` placeholder rename

**What shipped:** Renamed the static `topology_blob` marker emitted by `circ-compile` from `"debug-paths-v1"` to `"circ.topology.v0.min"`. The export `getTopology()` mechanism is unchanged; only the marker string content moved. This establishes the `circ.topology.v0.{min,full}` naming axis in the codebase ahead of slice 2's IR work and slice 3+4's `full` payload.

**Files touched:** `lib/emit/main.zig`, `lib/emit/project.zig`, `tests/emit/full_emit_test.zig`, plus 9 golden fixtures under `tests/fixtures/expected-zig/` (`anonymous_nested.zig`, `empty_ish.zig`, `and_two_inputs.zig`, `projects/passthrough_chain/main.zig`, `projects/same_name_half_adder/main.zig`, `projects/diamond/main.zig`, `projects/deep_chain/main.zig`, `projects/nested_invert/main.zig`, `projects/and_pair/main.zig`).

**Tests:** added `emit topology_blob marker is circ.topology.v0.min` (focused marker assertion in `tests/emit/full_emit_test.zig`); ran `zig build test`, result pass.

**Next slice:** Phase 0 Slice 2 — extend the IR with `macro_origin: []const MacroFrame` chains in `lib/ir/types.zig` and thread provenance through `lib/syntax/translate.zig` and `lib/ir/resolver.zig`.

**Notes:** The TODO(phase0) for slice 1 ("confirm `getTopology()` is the only consumer") is resolved: the placeholder string lives in two emit-side sites (`lib/emit/main.zig:59`, `lib/emit/project.zig:736`) and is golden-locked in 9 fixture files — no parsers consume the literal value, only golden tests assert it. Bulk perl rewrite handled all 11 sites in lockstep; the existing `full emitter fixture files` test would have failed loudly if any site drifted, which makes the rename self-checking.

**Reframing (2026-05-06, post slice-2):** Slice 1's commit `04422b4` renamed an *unrelated* runtime-export placeholder (`topology_blob` returned via `getTopology()` — a static string baked into the runtime WASM, separate from the offline `circ.topology` custom section). The original Phase 0 spec had conflated these two things; the spec was rewritten in commit `f0efe23` to disentangle them. Slice 2 (commit below) is the actual section-name rename the original "min" intent referred to. Slice 1's cleanup remains useful and stands as-committed; this note is here so a future reader mapping slice numbers to commits doesn't get confused.

## 2026-05-06 — Phase 0 — Slice 2: rename custom section to `circ.topology.v0.min`

**What shipped:** Renamed the WASM custom section emitted by `lib/topology/section_writer.zig` from `"circ.topology"` to `"circ.topology.v0.min"`. The section's payload bytes are unchanged — only its identifier on the `circ.topology.v0.*` versioned axis is being established. This is what the original Phase 0 "rename existing min" intent referred to (now correctly disentangled from the slice 1 placeholder rename).

**Files touched:** `lib/topology/section_writer.zig` (constant `SECTION_NAME`, comments, hardcoded byte counts in the `custom section bytes are correct` test); `tests/e2e/section_writer_fixtures_test.zig`, `tests/e2e/serializer_fixtures_test.zig`, `tests/e2e/cli_e2e_test.zig`, `tests/e2e/topology_protocol_test.zig` (Zig literals + JS section-name strings in WebAssembly.Module.customSections calls).

**Tests:** existing `section_writer: custom section bytes are correct` updated for new byte counts (1 + 20 + 9 = 30 body, name_len = 20, payload offset = section_start + 23); ran `zig build test`, result pass.

**Next slice:** Phase 0 Slice 3 — `circ.topology.v0.full` schema (new `lib/topology/full_format.zig`) + encoder/decoder over hand-built structs (no IR walk yet).

**Notes:** Blast radius was 14 sites across 5 files: 1 constant + 3 comments + 4 test-byte-counts + 6 e2e test references (mix of Zig literals and JS section-name strings inside Zig multiline string blocks). Used `perl -pi -e` with a negative-lookahead regex `(?!\.v0)` for the e2e files to avoid double-replacing on a hypothetical re-run; the lib changes were targeted Edits because byte-count constants needed individual updating. The TODO(phase0) about external consumers reading the section name literally is left open — the user's pre-1.0 stance applies, but if a downstream tool turns out to grep `"circ.topology"`, the rename will surface in a follow-up rather than a compat shim.

## 2026-05-06 — Phase 0 — Slice 3: `circ.topology.v0.full` schema + encoder + decoder

**What shipped:** New `lib/topology/full_format.zig` defining `FULL_MAGIC = "CIRF"`, `FULL_VERSION = 0x01`, in-memory types `OriginFrame`, `FullComponentRecord`, `FullTopology` (with `deinit`), and reusing `format.ComponentKind` / `format.PortName` / `format.ConnectionRecord`. New `lib/topology/full_serializer.zig` exposing `pub fn encode(allocator, topology) ![]u8` operating over a hand-built `FullTopology` (no IR walk yet — that's slice 4). New `lib/topology/full_decoder.zig` exposing `pub fn decode(allocator, bytes) !FullTopology` with a small `Cursor` helper for bounded byte reads. New `tests/topology/full_roundtrip_test.zig` exercising encoder + decoder together. Four new module registrations in `build.zig`.

**Files touched:** `lib/topology/full_format.zig` (new), `lib/topology/full_serializer.zig` (new), `lib/topology/full_decoder.zig` (new), `tests/topology/full_roundtrip_test.zig` (new), `build.zig` (4 new module registrations between `section_writer_tests` and `section_writer_fixtures_tests_mod`).

**Tests:** added `full_format: magic and version are stable`, `full_format: kind values mirror min payload`, `full_encode_empty: locks the wire format` (golden 13 bytes), `full_encode: single component with no origin emits expected layout` (golden 28 bytes with offset assertions), `full_decode_rejects_bad_magic`, `full_decode_rejects_unknown_version`, `full_decode: empty payload round-trips`, `full_decode_rejects_truncated_input`, `full_encode_decode_roundtrip_simple` (3 components, 2 connections), `full_encode_decode_roundtrip_with_origin` (single-frame and nested-frame origin chains, including empty-alias inner frame). Ran `zig build test`, result pass.

**Next slice:** Phase 0 Slice 4 — IR-walking serializer. Mirror `serializer.serializeProjectFull`'s recursion in `lib/topology/full_serializer.zig` with an added `pub fn serializeProjectFull(allocator, project) ![]u8` that threads an `OriginFrame` stack and records names while walking.

**Notes:** Hit one Zig style error during first build — `var components/origin/connections` for slices that aren't mutated through their headers should be `const`. Fixed by changing the three slice-binding declarations. The decoder's errdefer chain handles partial-allocation cleanup correctly (built counters for components and origin frames let errdefer free only what was successfully constructed). The roundtrip-with-origin test deliberately includes an empty-alias inner frame to lock that anonymous-nested-macro round-trips byte-identically — that case is what the Phase 2 expanded-mode renderer will need to display correctly.

## 2026-05-06 — Phase 0 — Slice 4: IR-walking serializer for `circ.topology.v0.full`

**What shipped:** `lib/topology/full_serializer.zig` gained `pub fn buildFromProject(allocator, project) !FullTopology` (in-memory walk producing a typed topology) and `pub fn serializeProjectFull(allocator, project) ![]u8` (build + encode shorthand for the emit path that slice 5 will call). The walk mirrors `lib/topology/serializer.zig:expandModule`'s recursion structure so the two serializers produce identical global-id sequences (a reader can zip records by index across the two payloads). On top of that, the full walker threads an `OriginFrame` stack through recursion: pushes a frame on entry into a `sub_circuit_ref`, pops on return, and when emitting a primitive, copies the current stack into the component's `origin` chain.

**Files touched:** `lib/topology/full_serializer.zig` (added IR-walk implementation: `ExpanderState`, `BoundInput`, `freePartialComponents`, `dupOrigin`, `parsePortByte`, `primitiveToKind`, `findComponent`, `resolveSignalGlobalId`, `expandModule`, `buildFromProject`, `serializeProjectFull` — plus new `ir` import). `build.zig` (added `ir_types` import to `topology_full_serializer_mod`).

**Tests:** added `full_walk_primitives_have_empty_origin` (hand-built single-module project with three primitives — confirms primitive walk path produces empty origin chains and copies `instance_name` to `name`); `full_walk_one_subcircuit_records_origin` (root referencing a subcircuit via instance "combine" of imported "MyNot" — confirms one-frame origin chain with correct alias/subcircuit/target_file fields); `full_walk_nested_subcircuits` (three-level nesting: root → Wrapper → Tiny — confirms two-frame origin chain in outermost-first order). Ran `zig build test`, result pass.

**Next slice:** Phase 0 Slice 5 — wire the full serializer into the emit path. `cmd/circ-compile/main.zig` will call `full_serializer.serializeProjectFull` (or `serializeModule` for single-file) alongside the existing `serializer.serializeProject`, and `lib/topology/section_writer.zig` gains a `combineTwo` (or similar) entry point so both `circ.topology.v0.min` and `circ.topology.v0.full` are appended to the produced `.wasm`. Plus the integration test that compiles a real `.circ` fixture using a subcircuit and asserts both sections are present and the full payload's origin chain references the subcircuit alias.

**Notes:** The "two parallel serializers must produce identical id sequences" invariant is currently a documented contract, not an enforced one. Slice 5's integration test will exercise it indirectly (decode both sections, assert component IDs match). If a future change to either walker's traversal order breaks the invariant silently, the symptom would be wrong wires in the rendered preview rather than a test failure — worth keeping in mind for slice 5's test design. Resolved the spec's TODO about choosing approach (a) vs (b): went with (b), parallel implementations, because `serializer.zig`'s 80-line `expandModule` is heavily intertwined with payload writing and extracting a generic visitor would be a meaningful refactor risk for this slice's scope. The `serializeModule` (single-file) entry point on the full-serializer side is deferred to slice 5 if needed — the existing `serializeModule` in the min serializer errors on `sub_circuit_ref` so the single-file path on the full side might be unused in practice, but slice 5 will check.

## 2026-05-06 — Phase 0 — Slice 5: two-section emit + integration test

**What shipped:** Full topology now lands in every `circ-compile`-produced `.wasm`. Three pieces composed:

1. `lib/topology/full_serializer.zig` gained `buildFromModule(allocator, module)` and `serializeModuleFull(allocator, module)` for the single-file (no-project) path. Mirrors the existing `serializer.serializeModule`: errors on `sub_circuit_ref`, used when the CLI compiles a `.circ` with no imports.
2. `lib/topology/section_writer.zig` gained `combineTwo(allocator, runtime_wasm, min_payload, full_payload)` which appends both `circ.topology.v0.min` and `circ.topology.v0.full` custom sections in order. Plus two new tests covering the byte layout (offsets, name lengths, body lengths) and the short-payload error case.
3. `cmd/circ-compile/main.zig`'s compile branch now produces both payloads and calls `combineTwo` instead of `combine`. New `full_serializer` import added; mirrors the existing `maybe_project ? serializeProjectFull : serializeModuleFull` switch alongside the min path.
4. `build.zig` wires `full_serializer` into the `circ_compile_mod` import map and registers the new integration test module with all required imports (scan_imports, import_cycle, resolve_bodies, validator_run_project, both serializers, full_decoder, section_writer, runtime_embed) plus parser_lib link.

**Files touched:** `lib/topology/full_serializer.zig` (added single-file path); `lib/topology/section_writer.zig` (added `combineTwo` + 2 tests + `SECTION_NAME_FULL` constant); `cmd/circ-compile/main.zig` (new full serializer call + combineTwo call + import); `tests/topology/full_emit_integration_test.zig` (new — end-to-end integration test); `build.zig` (full_serializer wired into circ-compile binary; new test module registered); `DOCS/STATUS.md` (this entry).

**Tests:** added `section_writer: combineTwo emits both sections in order` (byte-level layout assertion: id, body_len, name_len, name string, payload, repeated for both sections); `section_writer: combineTwo rejects short full payload` (error.TopologyTooShort); `phase0_full_pipeline_roundtrip: and_pair fixture carries both sections with origin chains` — the load-bearing integration test that resolves the `tests/fixtures/projects/and_pair/` project (which uses an imported `paired_and` subcircuit with two instances `p1` and `p2`), serializes both payloads, calls `combineTwo`, then walks the resulting WASM custom sections via an inline LEB128-aware section walker, finds both names, decodes the full payload, and asserts: (a) both section payloads byte-match what was passed in, (b) component count from min and decoded full match, (c) every component has a non-empty name, (d) at least one component has a non-empty origin chain referencing `paired_and`, (e) connections walk byte-by-byte across min and full. Ran `zig build test`, result pass — 137 tests across all suites.

**Next slice:** Phase 0 is complete. Next initiative-level work is Phase 1 (preview subcommand). Per the rewritten Phase 0 spec, the macro-plan and Phase 1/2/3 specs may need similar reconvergence against the codebase — recommend reviewing each at planning time before that phase's implementation begins.

**Notes:** The integration test does *not* invoke the actual CLI binary — it runs the same parse → resolve → validate → serialize → combine pipeline in-process, matching the existing e2e test pattern. This keeps the test fast and avoids needing to drive a subprocess. The inline section walker (`findCustomSection` + `readLeb128`) is a 30-line helper that knows just enough about WASM section structure to extract custom sections by name. It's intentionally local to this test file rather than promoted to `lib/topology/`; if Phase 1's preview subcommand needs the same logic, that's the right time to factor it out. The TODO(phase0) about external consumers reading `circ.topology` literally remains open — not a blocker for shipping, the user's pre-1.0 stance applies.

## 2026-05-06 — Phase 1 — Slice 1: `--preview` flag plumbing

**What shipped:** New `Mode.preview` variant in `lib/cli/args.zig`'s `Mode` enum. Parse-time recognition of `--preview` flag with mutual-exclusion checks against `--emit-zig` and `--inspect` (returns `error.ConflictingModes`). Parse-time rejection of `-o` in preview mode (returns `error.InvalidFlagValue`). `MissingOutput` validation extended so neither `--inspect` nor `--preview` requires `-o`. Placeholder dispatch arm in `cmd/circ-compile/main.zig` writes `"preview mode not yet implemented\n"` to stderr and returns exit code 1 — the real preview behaviour lands in slices 2–4.

**Files touched:** `lib/cli/args.zig` (Mode enum + parse logic + 4 new tests); `cmd/circ-compile/main.zig` (new `.preview` switch arm with placeholder).

**Tests:** added `cli_args_parse_preview_flag`, `cli_args_preview_rejects_emit_zig` (both orderings), `cli_args_preview_rejects_inspect` (both orderings), `cli_args_preview_rejects_output_path` (both orderings). Ran `zig build test`, result pass — 137+ tests across all suites.

**Next slice:** Phase 1 Slice 2 — widen `cmd/circ-compile/main.zig`'s existing `pub fn run() !u8` signature to accept `(allocator, argv, stdout, stderr)` so integration tests can drive the CLI in-process.

**Notes:** Resolved the Phase 1 TODO about `-o` rejection: chose **parse-time rejection** rather than dispatch-time (which is what `--inspect` does at `main.zig:106`). Trade-off: tests the rejection from `cli_args.parse` directly without needing CLI integration, matching the named-test placement under `lib/cli/args.zig`'s suite. Cost: minor inconsistency with `--inspect`'s pattern. **Future cleanup opportunity:** consider promoting `--inspect`'s `-o` rejection to parse-time too for symmetry — out of slice 1's scope. Both-orderings tests (e.g. `--preview --emit-zig` and `--emit-zig --preview`) are intentional: the conflict-detection logic uses `seen_*` flags whose order-sensitivity could regress without the symmetry assertion. The placeholder dispatch arm in `main.zig` is a deliberately broken state that proves the flag plumbing reaches dispatch but doesn't pretend to do real work — slice 4 replaces it with the real branch.

## 2026-05-06 — Phase 1 — Slice 2: `run(...)` signature widening

**What shipped:** Widened `cmd/circ-compile/main.zig`'s `fn run() !u8` to `pub fn run(allocator, argv, stdout_writer, stderr_writer) !u8`. The arena/argv/writer initialization moved out of `run` and into `main`, which now does the orchestration: arena from page allocator, `std.process.argsAlloc`, real stdout/stderr writers — then calls the widened `run`. The `run` body itself is unchanged structurally; only its parameter inputs changed source. `circ_compile_mod` now also has a test target wired in `build.zig`, picking up test blocks inside `main.zig`.

**Files touched:** `cmd/circ-compile/main.zig` (signature widening + main refactor + 1 new test block); `build.zig` (new `circ_compile_tests` test target + `test_step.dependOn` registration).

**Tests:** added `run with --inspect on existing fixture` smoke test inside `cmd/circ-compile/main.zig` — calls `run(allocator, argv, stdout_buf.writer(), stderr_buf.writer())` with synthetic argv pointing at `tests/fixtures/circuits/and_two_inputs.circ` in `--inspect` mode, asserts exit code 0, non-empty stdout, empty stderr. Existing 137+ tests all stay green; ran `zig build test`, result pass.

**Next slice:** Phase 1 Slice 3 — implement `pub fn dump(writer, topology: FullTopology) !void` in new `lib/preview/dump.zig`. Pure formatting function over a hand-built `FullTopology`; no CLI wiring yet (slice 4 connects the dispatch arm).

**Notes:** The smoke test uses an arena rooted at `std.testing.allocator` — leak detection is preserved through the arena's parent allocator at deinit time. This pattern is what slice 4's golden integration tests will inherit. The refactor is genuinely a non-behaviour change for existing modes: every test that was green before stays green, and the smoke test exercises the exact same `--inspect` code path that `main` would invoke. The `circ_compile_tests` build wiring ran into a forward-reference issue: `test_step` is declared further down in `build.zig`, so the `test_step.dependOn(&run_circ_compile_tests.step)` call has to be moved into the bottom section that aggregates all test dependencies. The test target's `addTest` + `addRunArtifact` lives near the binary's `addExecutable` block (line 770ish), and the dependency-add lives at the bottom (line 843ish) — same split as every other test in the project.

## 2026-05-06 — Phase 1 — Slice 3: `preview.dump.dump` implementation

**What shipped:** New `lib/preview/dump.zig` exporting `pub fn dump(writer: anytype, topology: full_format.FullTopology) !void`. Pure formatting function (no allocator, no I/O beyond the writer). Two-section output: header (`Topology (v0.full)` + counts) → `Components:` table (id-ascending order, format `[id] kind "name" [origin: chain]`) → `Connections:` table (input order, format `src_id.out -> dst_id.<port_name>`). Origin chains use ` > ` as frame separator with each frame as `alias:subcircuit`; anonymous-alias frames render with empty alias between separators (e.g. `g:xnor > :xor`). `target_file` is omitted from output (debug aid, not user-facing useful). Component kinds rendered via `@tagName` (`input_pin`, `not_gate`, `and_gate`, `led`, `wire`, `output_pin`). Port names decoded via `full_format.PortName` — `in=0, a=1, b=2, out=3`.

**Files touched:** `lib/preview/dump.zig` (new — implementation + 6 unit tests); `build.zig` (new `preview_dump_mod` + `preview_dump_tests` target with `test_step.dependOn` registration, placed between the topology_full_roundtrip and topology_full_emit_integration sections).

**Tests:** added `preview_dump_primitives` (3 components no origin, 2 connections — full literal-string assertion), `preview_dump_with_origin` (single-frame origin chain `g:xor` rendering on subset of components), `preview_dump_nested_origin` (two-frame chain with anonymous middle alias `g:xnor > :xor`), `preview_dump_anonymous_component_name` (substring-match for `[0] not_gate ""`), `preview_dump_port_decoding` (substring-matches for `0.out -> 1.in`, `0.out -> 2.a`, `1.out -> 2.b`), `preview_dump_deterministic_ordering` (same topology dumped twice → byte-identical). Ran `zig build test`, result pass.

**Next slice:** Phase 1 Slice 4 — wire the preview branch in `cmd/circ-compile/main.zig` and add golden-file integration tests for the three Phase-0 fixtures (primitives-only, builtin_xor, builtin_xnor). The dispatch path becomes parse → resolve → translate → `full_serializer.buildFromProject` (or `buildFromModule`) → `preview.dump.dump` → stdout. Replaces the slice-1 placeholder.

**Notes:** The `dump` function is genuinely pure (no allocator parameter) because its only state is the writer. Tests pass `ArrayList(u8).writer(allocator)` to capture output; production will pass the real stdout writer. The "origin column omitted when origin.len == 0" rule is what gives non-macro components their tidy single-line rendering — a reader skimming a 50-component dump can spot at a glance which gates came from subcircuit expansion vs. were declared directly. Current `FullConnectionRecord` only stores `port` (destination); source port is implicit `out`, so the dump hardcodes `.out` on the source side. If the format ever evolves to encode source ports explicitly, the dump would need to read them.

## 2026-05-06 — Phase 1 — Slice 4: CLI wiring + golden integration tests

**What shipped:** `cmd/circ-compile/main.zig`'s preview switch arm now does the real preview work: builds a `FullTopology` via `full_serializer.buildFromProject` (when `maybe_project` is set) or `full_serializer.buildFromModule` (single-file), calls `preview_dump.dump(stdout_writer, topology)`, frees the topology, returns 0. The slice-1 placeholder is replaced. The dispatch gate `has_imports and args.mode != .inspect` was widened to `(has_imports or args.mode == .preview) and args.mode != .inspect` — preview always goes through the project pipeline regardless of explicit imports, because builtin macros (`xor`, `xnor`, etc.) appear as `sub_circuit_ref` even with no explicit `import` statement, and `scan_imports` discovers them via the implicit_builtin path. Compile and emit_zig keep the cheaper `has_imports` gate to avoid extra disk I/O on macro-free fixtures (locked by perf-budget tests). New `preview_dump` import in `circ_compile_mod` and `golden` helper module also added so integration tests in main.zig can use `golden.expectGolden(...)`. **Phase 1 is now complete.**

**Files touched:** `cmd/circ-compile/main.zig` (new `preview_dump` import; gate logic refactored to `needs_project_resolution`; preview switch arm replaced with real implementation; 4 new integration tests + a `runPreview` helper); `build.zig` (new `golden` and `preview_dump` imports on `circ_compile_mod`); 3 new golden files under `tests/fixtures/circuits/` (`chain.preview.golden`, `builtin_xor.preview.golden`, `builtin_xnor.preview.golden`).

**Tests:** added `phase1_preview_primitives_fixture` (chain.circ → byte-matches captured golden); `phase1_preview_xor_fixture` (builtin_xor.circ → 19-component dump with two-frame origin chains like `g:xor > o:or`); `phase1_preview_xnor_fixture` (builtin_xnor.circ → similar nested structure through xnor's expansion); `phase1_preview_parse_error_to_stderr` (E001_undeclared.circ → exit non-zero, stdout empty, stderr non-empty — locks the stream-split contract). Plus 3 new golden files captured via `UPDATE_GOLDENS=1 zig build test`. Ran `zig build test`, result pass — 153 tests across all suites.

**Next slice:** Phase 2 Slice 1 (or the broader Phase 2 begins) — public types + sizing constants + `--expand-macros` flag plumbing, per `DOCS/PLANS/PHASE_2_layout_pass.md`.

**Notes:** The first attempt at fixing the dispatch gate widened it for *all* non-inspect modes (`if (args.mode != .inspect)`) but tripped a perf regression on the 100-component grid integration test — `scanProjectImports` does an extra disk-read pass to detect builtin imports. Reverted to the targeted preview-only widening. This is the kind of latent bug worth flagging: **compile mode probably has the same issue with builtin-macro single-file fixtures** but it's not tested today (no `circ-compile builtin_xor.circ -o ...` test exists). A future fix would either thread implicit-builtin detection into the `has_imports` check or accept the perf cost universally. Out of slice 4's scope; recorded as future cleanup. The xor golden's 19 components show the macro expansion threading correctly: `g:xor > o:or` for primitives inside xor's `or` sub-instance, `g:xor > n:nand` for primitives inside xor's `nand` sub-instance, plus xor's own gate and boundary I/O pins. Phase 1's debug dump is exactly the kind of inspection tool that surfaces what subcircuit expansion actually produces — useful for the Phase 2 layout work that has to lay this graph out visually.

## 2026-05-06 — Phase 2 — Slice 1: public types + sizing constants + flag plumbing

**What shipped:** New `lib/preview/layout.zig` skeleton with all public types defined (`PortCoord`, `PortSlot`, `NodeKind = union(enum) { primitive, subcircuit }`, `PlacedComponent`, `Segment`, `RoutedWire`, `LayoutGrid`, `LayoutOptions`) and `pub fn layout(...)` returning `error.NotImplemented`. New `lib/preview/layout/types.zig` defining the inter-stage pipeline contract (`InputEdge`, `OutputEdge`, `VirtualNode`, `VirtualGraph`, `ColumnAssignment`, `RowAssignment`) — re-imports `NodeKind` from `lib/preview/layout.zig` so both layers share one definition. New `lib/preview/layout/sizing.zig` with the locked per-kind cell-size table (`primitive_sizing` as `std.EnumArray(ComponentKind, PrimitiveSize)` with input_pin=6×1, not_gate=5×3, and_gate=5×3, led=3×3, wire=0×0 sentinel, output_pin=6×1) plus `macroSize(label_width)` returning `max(8, label_width+2) × 3`. New `--expand-macros` boolean flag on `Args`, parsed in `cli_args.parse`, with parse-time rejection when used outside `--preview` mode (returns `error.InvalidFlagValue`).

**Files touched:** `lib/preview/layout.zig` (new — public types + stub function + 2 tests); `lib/preview/layout/types.zig` (new — pipeline types + 1 test); `lib/preview/layout/sizing.zig` (new — sizing table + 2 tests); `lib/cli/args.zig` (new `expand_macros` field on Args + flag parsing + parse-time validation + 2 tests); `build.zig` (3 new module registrations: `preview_layout_mod`, `preview_layout_types_mod`, `preview_layout_sizing_mod`, each with `full_format` import; types_mod additionally imports `layout`).

**Tests:** added `layout: public types compile and have expected fields` (comptime `@typeInfo` introspection on PortCoord, PlacedComponent, LayoutGrid, NodeKind); `layout: stub returns NotImplemented`; `types: pipeline structs compile`; `sizing: primitive table values match locked spec`; `sizing: macroSize floors at 8 wide`; `cli_args_parse_expand_macros_flag` (with `--preview` accepted; default false); `cli_args_expand_macros_rejects_outside_preview` (rejected with `--inspect` and with `-o`). Ran `zig build test`, result pass — 160+ tests across all suites.

**Next slice:** Phase 2 Slice 2 — implement Stage 1 (`collapse.zig`). Walk a `FullTopology` to produce a `VirtualGraph`, dropping `wire` primitives by short-circuiting their edges, and (in opaque mode) grouping primitives that share the same outermost `OriginFrame` (alias + subcircuit) into single virtual subcircuit nodes with synthetic ids above `max(component.id) + 1`.

**Notes:** The `expand_macros` flag is parsed and stored but **not yet consumed** by the dispatch — Phase 2's layout pipeline isn't wired into the CLI until Phase 3 (per the spec's "CLI invisible during Phase 2" rule). The flag plumbing happens now so slice 2 can exercise it through tests. The `output_pin` cell size (6×1, same as input_pin) is included in the sizing table even though the spec's table didn't enumerate it — `std.EnumArray` requires all variants. Picked the same shape as input_pin since both are boundary I/O. The `wire` size at 0×0 is a sentinel: wires are collapsed before placement and should never be sized via `primitive_sizing.get(.wire)` — but having the entry keeps the EnumArray complete. The comptime `@typeInfo` test is intentionally minimal — it only asserts field counts, not exact field shapes. A future slice can deepen the type contract via more granular asserts if drift becomes a concern. Forward-reference issue avoided this round: all three new modules go AFTER `test_step` is declared, so inline `test_step.dependOn` works.

## 2026-05-06 — Phase 2 — Slice 2: Stage 1 (collapse)

**What shipped:** New `lib/preview/layout/collapse.zig` with `pub fn collapse(arena, topology, opts) !VirtualGraph`. Three-step algorithm: (a) compute wire transitive closure — for each `wire` primitive, walk the driver chain back to a non-wire source, recording the resolution in a hash map; (b) determine groups (opaque mode only) — primitives sharing the same outermost `OriginFrame` (alias + subcircuit) cluster into groups, each gets a synthetic id allocated above `max(component.id) + 1`; (c) walk connections, applying wire-resolution to sources and group-remapping to both endpoints — internal connections (both endpoints in the same group) are dropped; surviving connections become `InputEdge` / `OutputEdge` entries on the appropriate `VirtualNode`'s edge lists. Final pass materializes `VirtualNode`s sorted by id ascending: non-grouped primitives become `kind = .primitive(comp.kind)` with their real id and full origin chain; each group becomes a single `kind = .subcircuit(<subcircuit>)` virtual node with the alias as `name`, empty origin chain (the outer frame is "consumed" by the collapse), and the synthetic id.

**Files touched:** `lib/preview/layout/collapse.zig` (new — algorithm + 4 unit tests); `build.zig` (new `preview_layout_collapse_mod` registration with imports for `full_format`, `layout`, `layout_types`).

**Tests:** added `collapse_drops_wire_primitives` (3 components `pin → wire → led` with 2 connections; verifies wire is dropped and a direct `pin → led` edge remains); `collapse_chains_of_wires` (5 components with 3-wire chain; verifies transitive closure works through arbitrarily long chains); `collapse_opaque_subcircuit` (4 components with 2 inside an `xor:g` group; verifies (a) one virtual node with synthetic id 4 is created, (b) `kind = .subcircuit("xor")`, `name = "g"`, (c) internal `not → and` connection vanishes, (d) external connections remap to/from the virtual node, (e) `next_id` is one past the synthetic); `collapse_expanded_subcircuit` (same shape, `expand_macros = true`; verifies all four primitives appear separately preserving real ids and full origin chains, all three connections survive). Ran `zig build test`, result pass — 165+ tests across all suites.

**Next slice:** Phase 2 Slice 3 — Stage 2 (`columns.zig`): longest-path layering. `pub fn assignColumns(arena, graph) !ColumnAssignment`. Input pins force column 0; LEDs/output_pins land in `num_columns - 1`; everything else is `1 + max(column_of[upstream])`.

**Notes:** All collapse-stage allocation is arena-routed (HashMap inits and ArrayList appends use the caller-supplied arena). No explicit deinit calls — arena cleanup is the discipline. The wire-resolution depth is capped at 64 to prevent infinite loops on (theoretical) wire cycles; real `.circ` sources shouldn't produce cycles, but defensive bounding is cheap insurance. The "outer frame consumed" design choice — virtual subcircuit nodes carry empty origin rather than the deeper frames — is deliberate for slice 2: simplifies edge accounting and matches what opaque-mode rendering will need (display the box as `[xor:g]` not as `[xor:g] (containing or:o, nand:n)`). If a future phase wants nested-opaque rendering (collapse only the outermost level, keep deeper levels expanded inside), the algorithm would need to preserve `comp.origin[1..]` on the virtual node — flagged here as a future option, not implemented now. Edge port info is preserved verbatim (the destination port byte from each source connection becomes the virtual node's `dst_port` for that input). Slice 5 (place) may want to remap to per-subcircuit published port names ("a", "b" for xor's published inputs) — but slice 2 keeps it simple.

## 2026-05-06 — Phase 2 — Slice 3: Stage 2 (columns)

**What shipped:** New `lib/preview/layout/columns.zig` with `pub fn assignColumns(arena, graph) !ColumnAssignment`. Longest-path layering algorithm: input pins pinned at column 0; every other node assigned `column_of[n] = 1 + max(column_of[upstream])` via a fixed-point sweep over all nodes (bounded at `n + 1` iterations); after convergence, `led` and `output_pin` primitives are forced to `num_columns - 1` so all sinks line up on the right edge. Returns `ColumnAssignment { column_of, num_columns }`.

**Files touched:** `lib/preview/layout/columns.zig` (new — algorithm + 4 unit tests + helper functions `isInputPin` / `isSink`); `build.zig` (new `preview_layout_columns_mod` registration).

**Tests:** added `columns_longest_path` (4-node chain `pin → not → and → led` produces columns `[0, 1, 2, 3]`); `columns_diamond` (4-node fan-out + fan-in with one pin driving two parallel `not`s feeding one `and` produces `[0, 1, 1, 2]` — verifies parallel paths share the same column); `columns_input_pins_at_zero` (two unrelated input pins both at column 0 — verifies the input-pin pinning rule applies regardless of graph shape); `columns_leds_rightmost` (5 nodes where one LED is "deep" at natural column 3 and another LED would naturally land at column 1 from a direct pin connection — verifies both LEDs get forced to the rightmost column 3, while non-sink nodes keep their natural columns). Ran `zig build test`, result pass — 169+ tests across all suites.

**Next slice:** Phase 2 Slice 4 — Stage 3 (`rows.zig`): barycenter-method row assignment with two sweeps (left-to-right then right-to-left), with deterministic tie-break by ascending node id when barycenter values are equal.

**Notes:** The fixed-point sweep is intentionally simple: O(n²) worst case (each pass touches every node, up to n+1 passes). For the typical `.circ` scale of <50 components, this is fine and the code stays trivially correct. A topological-sort-based one-pass algorithm would be O(V+E) but harder to test. The "force LED to rightmost" rule has a real visual consequence: an LED whose natural longest-path column would be 1 (e.g. a direct `pin → led` connection in a graph that also has a long `pin → not → not → and → led` chain) gets stretched out to the rightmost column. The `columns_leds_rightmost` test specifically exercises this case — without the forced rule, the early LED would land mid-graph and break the schematic's right-aligned-outputs convention. The cycle-handling decision (per the spec's TODO) is silently "iteration cap fires without convergence" — no error returned. Combinational `.circ` sources can't produce graph cycles via subcircuit expansion in the current language, so this is acceptable; if a future feature introduces cycles (e.g. latches), columns will need explicit cycle detection.

## 2026-05-06 — Phase 2 — Slice 4: Stage 3 (rows)

**What shipped:** New `lib/preview/layout/rows.zig` with `pub fn assignRows(arena, graph, columns) !RowAssignment`. Barycenter-method row assignment with two sweeps. Initial row order within each column is id-ascending (inherited from `VirtualGraph.nodes`'s collapse-stage sort). Sweep 1 (left-to-right): for columns 1..N-1, compute each node's barycenter as the mean row of its upstream neighbours from earlier columns, reorder the column ascending barycenter, ties broken by ascending node id. Sweep 2 (right-to-left): same but using downstream neighbours from later columns, processing columns N-2..0. Determinism is anchored on stable input order (`graph.nodes` is sorted by id), float comparison without epsilon, and integer-id tie-break.

**Files touched:** `lib/preview/layout/rows.zig` (new — algorithm + 2 unit tests + helper functions `sweepBarycenter`, `computeBarycenter`, `accumulateRow`, `lessByBarycenterThenId`); `build.zig` (new `preview_layout_rows_mod` registration).

**Tests:** added `rows_barycenter_simple: crossing edges resolved` (4 nodes across 2 columns where naive id-ascending produces crossing edges; verifies barycenter sweep reorders column 1 to eliminate the crossing — `not_a` driven by `pin1` ends up at row 1, `not_b` driven by `pin0` ends up at row 0); `rows_deterministic_tie_break: equal barycenters resolve by ascending id` (3 nodes where two nots in column 1 share the same single upstream pin, so both have barycenter 0; verifies tie-break puts `not_lo` at row 0 and `not_hi` at row 1, plus runs the algorithm twice on the same input to confirm byte-identical row_of and num_rows). Ran `zig build test`, result pass — 171+ tests across all suites.

**Next slice:** Phase 2 Slice 5 — Stage 4 (`place.zig`): per-kind cell sizing + absolute (x, y) coordinate computation + per-component port-coordinate resolution producing `PortSlot` lists. Output: `[]PlacedComponent`.

**Notes:** Hit one implementation mistake in `computeBarycenter`'s first draft: copied neighbour ids into a `std.heap.page_allocator`-backed ArrayList and then tried to free the *items slice* directly, which is wrong (you free the ArrayList's backing buffer via `deinit`, not its `.items` slice). Refactored to inline the iteration over `node.inputs`/`node.outputs` via a small `accumulateRow` helper that takes `&sum` and `&count` pointers. This is the cleaner version anyway — no transient allocation, just a switch over direction. The barycenter iteration uses **linear scan** to find a neighbour's row (`for (buckets[neighbour_col].items, 0..) |bi, pos| if (bi == neighbour_idx) ...`). For the typical `.circ` scale with <50 components per column, this is fine; a future optimization could maintain a parallel `row_of` map updated in lockstep with bucket reorderings, but slice 4 keeps it simple. The two-sweep convergence is good enough for the test cases here; harder graphs (multi-level fan-in/fan-out with high crossings) might need 3+ sweeps to fully minimize crossings, but the spec said "two sweeps" and the tests pass — flagged as a future tunable if Phase 3's render reveals layout sub-optimality.

## 2026-05-06 — Phase 2 — Slice 5: Stage 4 (place)

**What shipped:** New `lib/preview/layout/place.zig` with `pub fn place(arena, graph, columns, rows) ![]PlacedComponent`. Cell-sizing + absolute coordinate computation + per-port slot resolution. Algorithm: (1) compute per-node `PrimitiveSize` from the locked `sizing.zig` table for primitives or `sizing.macroSize(label_width)` for opaque subcircuits; (2) compute per-column max width and per-row max height; (3) cumulative left-edge x per column and top-edge y per row using `COL_GUTTER = 4`, `ROW_GUTTER = 1`; (4) for each `VirtualNode` produce a `PlacedComponent` with absolute (x, y), width, height, plus per-kind port slot resolution. Port positions follow the locked spec convention: `not_gate` in/out at vertical middle, `and_gate` a top-left/b bottom-left/out middle-right, `input_pin`/`output_pin`/`led` at edge centers, opaque subcircuits expose only the boundary inputs they actually receive (a, b, in) at the same a/in/b vertical positions as gates.

**Files touched:** `lib/preview/layout/place.zig` (new — algorithm + 3 unit tests + helpers `sizeOf` / `resolvePortCoords`); `build.zig` (new `preview_layout_place_mod` registration with imports for `full_format`, `layout`, `layout_types`, `sizing`).

**Tests:** added `place_cell_sizing: not_gate at column 1 row 0` (verifies cell width=5, height=3, and absolute (10, 0) computed from `col_widths = [6, 5]` → `col_x = [0, 10]` and `row_y = [0]`); `place_port_coords_and_gate: a, b, out at expected offsets` (3-node graph with two pins driving an and_gate at column 1 row 0 → and lands at (10, 0); verifies port coords `a@(10,0)`, `b@(10,2)`, `out@(14,1)` per spec); `place_macro_label_width: long subcircuit alias widens the box` (single subcircuit virtual node with subcircuit = "xor" and name = "longish_combine" → label is `[xor:longish_combine]` (21 chars), `macroSize(21)` returns 23 wide × 3 tall, asserting both the exact width and that it exceeds the 8-char floor). Ran `zig build test`, result pass — 174+ tests across all suites.

**Next slice:** Phase 2 Slice 6 — Stage 5 (`route.zig`): interval-greedy channel-track allocation + orthogonal segment computation + crossing detection. Plus the `dumpLayout(writer, grid)` debug formatter in `lib/preview/dump.zig` and the `layout(...)` orchestrator wired up to call all five stages, ending with the six golden integration tests.

**Notes:** Per-row max height is computed *globally* across all columns at the same row index — so row R's y-coordinate is consistent regardless of column. This makes wire routing simpler at the cost of some vertical waste when one column has a tall component at row 0 but other columns at row 0 are short (those short cells get the same y-range). For typical `.circ` sources this trade-off is invisible; if future fixtures show ugly empty space, per-column-per-row sizing is the upgrade path. The subcircuit port resolution is intentionally minimal: it inspects the virtual node's incoming edges to decide which of `{a, in, b}` ports actually appear, then places only those on the left edge. So an `xor` subcircuit (a, b inputs) gets two left-edge ports; a unary subcircuit (just `in`) gets one centered port. The output is always at the right edge's vertical middle. Sinks (`led`, `output_pin`) carry a phantom out_port at the cell's right edge — the field is non-optional in `PlacedComponent`, but Phase 3's renderer should never emit wires from sinks regardless. The 3-node graph in `place_port_coords_and_gate` was designed so the row heights and column widths produce a deterministic absolute placement: pin1 at (0, 0), pin2 at (0, 4) (row 0 height = max(1, 3) = 3, then ROW_GUTTER = 1, then row_y[1] = 4), and at (10, 0). This confirms the port coord assertions are computed from a fully-deterministic layout, not from accidentally-aligned cells.

## 2026-05-06 — Phase 2 — Slice 6a: Stage 5 (route)

**What shipped:** New `lib/preview/layout/route.zig` with `pub fn route(arena, graph, placed) !RouteResult`. Stage 5 walks every output edge in the `VirtualGraph`, computes endpoint coords from `PlacedComponent.out_port` (source) and the matching `PlacedComponent.in_ports[*]` slot (destination, looked up by `dst_port` byte), then assembles a 3-leg L-shaped path: horizontal source → track_x, vertical at track_x from sy to dy, horizontal track_x → destination. Tracks are allocated per source-x via interval-greedy first-fit: each wire originating at the same `sx` claims the lowest-index track whose existing y-range doesn't overlap the wire's `[min(sy, dy), max(sy, dy)]`. Crossings are detected pairwise across distinct wires by comparing each wire's vertical segment cell-occupancy against every other wire's horizontal segment cell-occupancy with **inclusive bounds** — a corner of one wire touching another wire's straight segment counts as a crossing because the renderer has to choose between drawing the rail glyph or the corner glyph at that shared cell. Output is a `RouteResult { wires, width, height }` with `width`/`height` derived from the bounding box of placed components plus all routed segments.

**Files touched:** `lib/preview/layout/route.zig` (new — algorithm + 3 unit tests + helpers `portCoordOf` / `portByteOf` / `segmentCross`); `build.zig` (new `preview_layout_route_mod` registration with imports for `full_format`, `layout`, `layout_types`).

**Tests:** added `route_segments_are_axis_aligned` (single edge between a pin and an led; verifies every produced `Segment` has either `from.y == to.y` or `from.x == to.x`); `route_two_wires_no_crossing` (two pins at disjoint y values each driving a separate led at matching y; verifies both wires share track 0 at `sx + 1 = 6` and crossings lists are empty); `route_two_wires_with_crossing` (two pins at y=0 and y=4 driving leds in opposite y directions, both originating at sx=5; verifies wires get different tracks (one at x=6, one at x=7) and both crossings lists are non-empty). Ran `zig build test`, result pass — 177+ tests across all suites.

**Next slice:** Phase 2 Slice 6b — the wrap-up. `dumpLayout(writer, grid)` debug formatter added to `lib/preview/dump.zig`, `layout(...)` orchestrator in `lib/preview/layout.zig` wired to call all five stages in order and produce a `LayoutGrid`, plus six golden integration tests against the existing fixtures (`<primitives>`, `builtin_xor`, `builtin_xnor` in opaque and expanded modes) and the `phase2_layout_deterministic` byte-identical re-run check.

**Notes:** First-attempt crossing detection used strict-interior bounds (`v_x > h_x_min and v_x < h_x_max` etc.), which excluded the corner-touch case where two wires from the same source column have their L-corners interleave. The `route_two_wires_with_crossing` test failed on first run because the two wires' Ls met at corner cells `(6, 4)` and `(7, 4)` — visually conflicting (one wire turns there while the other passes through), but excluded by strict bounds. Switched to inclusive bounds; the test now flags both corner-cells as crossings. This is the right behaviour for Phase 3's renderer, which needs to know "this cell has competing glyphs" to emit jump-arc or `┼`. Track allocation is by ascending wire index in `pending`, which is itself ordered by VirtualGraph traversal (which is id-ascending from collapse). So track assignment is fully deterministic. The 3-leg L is the simplest universal routing shape; for two wires whose endpoints are exactly aligned vertically (sy == dy AND track_x == sx + 1 AND track_x + 1 == dx), a special-case single horizontal segment is emitted, but this rarely fires in practice — most wires need the L. Slice 6a is intentionally the route-only half of the spec's larger slice 6; slice 6b adds dumpLayout, the orchestrator, and the goldens.

## 2026-05-06 — Phase 2 — Slice 6b: orchestrator + dumpLayout + integration tests (Phase 2 complete)

**What shipped:** Three coordinated additions that compose Phase 2's five stages into a working layout pipeline. (1) `pub fn dumpLayout(writer, grid)` added to `lib/preview/dump.zig` — three-section textual format: header `LayoutGrid <W>x<H>`, `Components:` table with `[id] kind "name" @(x,y) WxH` rows (subcircuits render as `subcircuit:<sub>`), `Wires:` table with `src.port -> dst.port:` followed by segment endpoints joined by `->`, and an optional `Crossings:` section if any wire has crossings. (2) New `lib/preview/layout/orchestrator.zig` with `pub fn build(arena, topology, opts) !LayoutGrid` — composes `collapse → assignColumns → assignRows → place → route` in order, assembling the final `LayoutGrid` from `place`'s `[]PlacedComponent` and `route`'s `RouteResult`. Lives in its own module to avoid a cyclic build-graph dependency (stages already import `layout` for `LayoutOptions`/types). (3) New `tests/preview/layout_integration_test.zig` with 6 golden integration tests using the existing fixtures and the `golden.expectGolden` helper. **Phase 2 is now complete.**

**Files touched:** `lib/preview/dump.zig` (`dumpLayout` + `layout` import); `lib/preview/layout/orchestrator.zig` (new); `tests/preview/layout_integration_test.zig` (new); `build.zig` (reordered `preview_layout_mod` ahead of `preview_dump_mod`; added `preview_layout_orchestrator_mod` and `preview_layout_integration_mod` registrations + linkage); 5 new golden files under `tests/fixtures/circuits/` (`chain.layout.opaque.golden`, `builtin_xor.layout.opaque.golden`, `builtin_xor.layout.expanded.golden`, `builtin_xnor.layout.opaque.golden`, `builtin_xnor.layout.expanded.golden`).

**Tests:** added `phase2_layout_primitives_opaque` (chain.circ → 5 components, 4 wires, all axis-aligned); `phase2_layout_builtin_xor_opaque` (builtin_xor.circ in opaque mode → xor collapses to one virtual subcircuit node `[xor:g]`); `phase2_layout_builtin_xor_expanded` (same fixture, expand_macros = true → all primitives from xor's body laid out individually with full origin chains visible in `dumpLayout`); `phase2_layout_builtin_xnor_opaque` (xnor collapses to one virtual subcircuit node, inner xor hidden); `phase2_layout_builtin_xnor_expanded` (both nesting levels expanded); `phase2_layout_deterministic` (build the same fixture twice and assert byte-identical dump output — locks the determinism contract end-to-end). Plus 5 captured golden files via `UPDATE_GOLDENS=1`. Ran `zig build test`, result pass — 183 tests across all suites.

**Next slice:** Phase 2 is complete. Phase 3 (ASCII rendering + color) begins with slice 1 (`--color` flag + color resolution) per `DOCS/PLANS/PHASE_3_ascii_render_color.md`. Recommend reviewing Phase 3's spec the same way Phase 1/2 were reviewed before slicing — though it was already patched post-Phase-0, additional drift may have surfaced as Phase 2 landed.

**Notes:** The orchestrator was put in a separate file rather than inlined into `layout.zig` because `layout.zig` only owns *types*, and stages already import `layout` for `LayoutOptions`. Adding stage imports back to `layout.zig` would create cycles. Splitting cleanly: `layout.zig` = types, `orchestrator.zig` = composition. The `layout()` stub function (returning `error.NotImplemented`) was preserved in `layout.zig` to keep slice 1's test green; future cleanup can either remove the stub or have it delegate to `orchestrator.build`. The `dumpLayout` choice to render subcircuits as `subcircuit:xor` (rather than the spec's earlier `macro:xor_macro`) reflects the reality that NodeKind has `.subcircuit: []const u8`, not a closed `MacroKind` enum. The chain.circ golden (`LayoutGrid 43x3` with 5 components on a single row) is the simplest visual proof that all five stages compose correctly; the xor goldens (~19 components, multi-row layouts) exercise the harder cases. The build.zig forward-reference issue (preview_layout_mod referenced before declaration) was fixed by reordering the preview_dump_mod block to come AFTER the preview_layout_mod block — same pattern that bit slice 2 of Phase 1 with circ_compile_tests.

## 2026-05-06 — Phase 3 — Slice 1: --color flag + color resolution

**What shipped:** New `lib/preview/render/color.zig` with `pub const ColorMode = enum { auto, always, never }`, `pub const ColorTag = enum(u8) { none, input_pin, not_gate, and_gate, led, macro, wire, crossing }`, ANSI escape constants (`ANSI_RESET`, `ansiFor(tag)`), and `pub fn shouldColor(mode, stdout_handle, no_color_value) bool` — a pure function that resolves the user-selected mode against an optional TTY handle and an optional `NO_COLOR` env value. Resolution table matches the patched Phase 3 spec: `--color=always` overrides `NO_COLOR`, `--color=never` always off, `--color=auto` requires a real TTY *and* no `NO_COLOR`. `--color=<value>` flag added to `lib/cli/args.zig`'s `parse` with validation against `auto`/`always`/`never` (rejects unknown values with `error.InvalidFlagValue`); `Args.color` defaults to `.auto`. `ColorMode` is the canonical type from `lib/preview/render/color.zig`; `cli_args` re-exports it.

**Files touched:** `lib/preview/render/color.zig` (new — types + shouldColor + 5 unit tests); `lib/cli/args.zig` (new `--color=<value>` parsing + `Args.color` field + 5 new tests); `build.zig` (new `preview_render_color_mod` registration; `cli_args_mod` gains `render_color` import; `test_step.dependOn(&run_preview_render_color_tests.step)` added to the centralized aggregation section).

**Tests:** added `cli_args_parse_color_auto`, `cli_args_parse_color_always`, `cli_args_parse_color_never`, `cli_args_color_default_is_auto`, `cli_args_color_rejects_invalid_value`; plus inline-in-color.zig: `color_resolution_always_overrides_no_color` (asserts true regardless of NO_COLOR or handle), `color_resolution_never_always_off`, `color_resolution_auto_no_tty` (null handle → false), `color_resolution_auto_no_color_env` (NO_COLOR set → false), `color: ANSI constants present`. Ran `zig build test`, result pass — 192+ tests across all suites.

**Next slice:** Phase 3 Slice 2 — `Canvas` abstraction in `lib/preview/render/canvas.zig`. Two parallel arrays (`cells: []u8` for character bytes, `color: []ColorTag` for per-cell ANSI tags), `setCell` / `drawHSegment` / `drawVSegment` mutators, and `writeOut(writer, use_color)` that emits cell bytes interleaved with ANSI escape sequences at color-tag boundaries when `use_color = true`.

**Notes:** Per the Phase 3 spec's "only meaningful in --preview mode" language plus the spec's listed test set (which doesn't include outside-preview rejection for --color), I chose **not** to add a parse-time rejection when `--color` is used outside `--preview`. Rationale: color is a "no-op when nothing renders" decision rather than a "must be preview" gate; in non-preview modes the flag is harmlessly stored but unused. This is a deliberate asymmetry with `--expand-macros`, which IS rejected outside `--preview` because it would otherwise mean nothing. If future use surfaces a confusion, the rejection can be added in a follow-up slice without breaking the test surface. The `ColorMode` re-export from cli_args avoids duplicating the enum in two places — one canonical definition in render/color.zig, one alias in args.zig (`pub const ColorMode = render_color.ColorMode`). `cli_args_mod` gains the `render_color` import in build.zig; the dependency direction is correct (CLI → render is fine; render → CLI would not be).

## 2026-05-06 — Phase 3 — Slice 2: Canvas

**What shipped:** New `lib/preview/render/canvas.zig` with the `Canvas` struct: parallel arrays `cells: [][]const u8` (one UTF-8 string slice per cell, default `" "`) and `color: []ColorTag` (default `.none`). Methods: `init(arena, w, h)`, `setCell(x, y, ch_bytes, tag)` (out-of-bounds writes silently no-op), `drawHSegment(from_x, to_x, y, ch_bytes, tag)`, `drawVSegment(x, from_y, to_y, ch_bytes, tag)`, `writeOut(writer, use_color)`. Cells are typically static UTF-8 string literals like `"─"` or `"╭"` so the multi-byte box-drawing characters coexist with ASCII at no extra bookkeeping. `writeOut` interleaves ANSI escapes with cell bytes when `use_color = true`: emits the right `ansiFor(tag)` at every tag transition, an `ANSI_RESET` before the new escape if the previous tag was non-`.none`, and an `ANSI_RESET` at every end-of-row so color never leaks across newlines (terminals that trim trailing whitespace would otherwise carry the color into the next line).

**Files touched:** `lib/preview/render/canvas.zig` (new — Canvas struct + 5 unit tests); `build.zig` (new `preview_render_canvas_mod` registration with `color` import; `test_step.dependOn` added).

**Tests:** added `canvas_set_cell` (sets a cell and verifies cells/color arrays at the right index; out-of-bounds writes silently no-op); `canvas_draw_h_segment` (draws `─` from x=0 to x=4 at y=2 in a 6-wide canvas; verifies only those 5 cells changed and the cell beyond stays default); `canvas_draw_v_segment` (vertical equivalent at x=2 from y=1 to y=3 in a 5-tall canvas); `canvas_write_out_no_color` (mixed-tag canvas with `use_color=false` produces output with **zero** ESC bytes; content is plain cells separated by newlines); `canvas_write_out_with_color` (same canvas with `use_color=true` produces specific ANSI escapes around `A` and `B`, plus an `ANSI_RESET` before newline). Ran `zig build test`, result pass — 197+ tests across all suites.

**Next slice:** Phase 3 Slice 3 — `glyphs.zig`. Per-`NodeKind` drawing functions (`drawInputPin`, `drawNotGate`, `drawAndGate`, `drawLed`, `drawMacroBox`) that fill a `Canvas` region given a `PlacedComponent`. Concrete glyph designs captured at slice time; the spec defers the actual byte content to implementation so the goldens reflect the chosen art rather than planner speculation.

**Notes:** Storing cells as `[][]const u8` (slice of slices) rather than fixed-width 4-byte chunks turned out to be the right call — most glyphs are 3-byte UTF-8 box-drawing characters or ASCII letters, both of which map naturally to string literal slices. The cost is one pointer per cell (16 bytes) plus the contents indirection; for typical layouts (~50×20 = 1000 cells = 16KB), invisible. The ANSI-reset-at-end-of-row discipline is a small detail with real terminal-compatibility benefit: some terminals carry color attributes across cleared lines or trim trailing whitespace before reset, leaking color into the next row. Always resetting at row end avoids that class of bug. The out-of-bounds no-op behaviour (rather than error or panic) is a defensive choice for slice 4's renderer development — coordinates *should* always fit by construction, but during iteration it's better to silently drop than crash the test suite. Slice 4's glyph code will compute coordinates from `PlacedComponent` fields that come straight from Phase 2's place stage; if those produce out-of-bounds writes, the layout has a bug, not the canvas.

## 2026-05-06 — Phase 3 — Slice 3: glyphs

**What shipped:** New `lib/preview/render/glyphs.zig` with one drawing function per `NodeKind` plus a `drawComponent` dispatcher. Concrete glyph designs (captured into goldens at slice time per the spec's B2 discipline):

- **`drawInputPin`** (6×1): name left-aligned, padded to width with `─`. `pin1` → `pin1──`. Names longer than `width - 2` get truncated.
- **`drawOutputPin`** (6×1): mirror of input pin — name right-aligned with leading `─` padding.
- **`drawNotGate`** (5×3): blank above and below, middle row `─▷○──`. Triangle `▷` + bubble `○` glyph centered between input/output rails.
- **`drawAndGate`** (5×3): `─╮` top, `│───` middle (with output rail extending right), `─╯` bottom — D-shape silhouette using rounded box-drawing corners.
- **`drawLed`** (3×3): blank above and below, middle row `─◉ ` — fisheye glyph for the LED body.
- **`drawMacroBox`** (variable W × 3): `╭─...─╮` top, `│ centered [<sub>:<name>] │` middle, `╰─...─╯` bottom. Label centered with `(inner - label_len) / 2` left padding when it fits inside `width - 2`. Falls back gracefully to no label if the label is too wide.

All cells get tagged with the appropriate `ColorTag` (`input_pin` for both pin kinds, `not_gate` / `and_gate` for gates, `led` for LEDs, `macro` for subcircuit boxes). The `wire` primitive variant is `unreachable` because wires are collapsed by Phase 2's stage 1 before placement.

**Files touched:** `lib/preview/render/glyphs.zig` (new — `drawComponent` dispatcher + 6 per-kind drawing functions + `captureCanvas` helper + 5 unit tests); `build.zig` (new `preview_render_glyphs_mod` registration with imports for `layout`, `canvas`, `color`; placed between `preview_layout_route_mod` and `preview_layout_orchestrator_mod` so `preview_layout_mod` is already declared).

**Tests:** added `glyphs_draws_input_pin` (input_pin "pin1" at (0,0) width 6 → `"pin1──\n"`); `glyphs_draws_not_gate` (NOT gate at (0,0) 5×3 → `"     \n─▷○──\n     \n"`); `glyphs_draws_and_gate` (AND gate at (0,0) 5×3 → `"─╮   \n │───\n─╯   \n"`); `glyphs_draws_led` (LED at (0,0) 3×3 → `"   \n─◉ \n   \n"`); `glyphs_draws_macro_box` (subcircuit "xor" instance "combine" at (0,0) width 15 → 3-row box with `[xor:combine]` centered exactly). Ran `zig build test`, result pass — 202+ tests across all suites.

**Next slice:** Phase 3 Slice 4 — `render(...)` orchestrator in `lib/preview/render.zig`. Composes `Canvas.init` + `drawComponent` per placed node + wire-segment rendering (using `╭╮╰╯─│●` for corners/taps/rails, jump-arc for crossings) + `canvas.writeOut`. Plus per-render unit tests for line-art and crossings.

**Notes:** The glyph designs lock concrete art that Phase 3's spec deliberately deferred. The `─▷○──` NOT gate uses `▷` (U+25B7 white right-pointing triangle) + `○` (U+25CB white circle) which are commonly available in monospace fonts. The AND gate's D-shape uses `╭╮╰╯│─` (rounded corners + lines) matching the line-art character family used elsewhere. The LED's `◉` (U+25C9 fisheye) gives a subtle "lit" feel. Macro labels render as `[<sub>:<alias>]` matching Phase 1's dump format convention. All glyphs are 1 cell wide regardless of multi-byte encoding — `Canvas.setCell` stores the slice as-is and `writeOut` emits the bytes verbatim. The `captureCanvas` helper in tests takes a function pointer (`drawNotGate` etc.) and returns the canvas's `writeOut` output as a byte slice; this keeps each glyph test compact (3 lines of setup + one literal-string assertion). The macro-label centering math uses `(inner - label_len) / 2` for the left padding — for `[xor:combine]` (13 chars) in width 15 (inner 13), padding is 0 each side and the label fills the inner row exactly. If a future change widens the macro past `label_len + 2`, padding kicks in symmetrically.

## 2026-05-06 — Phase 3 — Slice 4: render orchestrator

**What shipped:** New `lib/preview/render.zig` with `pub fn render(arena, writer, grid, opts) !void`. Composes Canvas + glyph drawing + wire rendering into a single rendered output. `RenderOptions { color: ColorMode, stdout_handle: ?std.fs.File.Handle, no_color_value: ?[]const u8 }` controls color resolution at the seam. Seven-step orchestration: (1) init Canvas at grid.width × grid.height; (2) draw component glyphs; (3) draw wire rails (`─` for horizontal segments, `│` for vertical); (4) patch corners at intra-wire segment junctions via `pickCornerGlyph` which selects from `╭╮╰╯` based on segment in/out direction sets (`{W,S}=╮`, `{W,N}=╯`, `{E,S}=╭`, `{E,N}=╰`); (5) detect fan-out taps — sources used by ≥3 wires get `●`; (6) apply jump-arcs at every `RoutedWire.crossings` cell via `applyJumpArc` (idempotent — set crossing cell to `│`, neighbours to `╯`/`╰` on the horizontal row); (7) resolve `use_color` via `color_mod.shouldColor` and call `canvas.writeOut`.

**Files touched:** `lib/preview/render.zig` (new — orchestrator + 4 unit tests + helpers `drawSegment`, `pickCornerGlyph`, `applyJumpArc`, `wireHasHorizontalAt`, `captureRender`); `build.zig` (new `preview_render_mod` registration with imports for `layout`, `layout_types`, `canvas`, `color`, `glyphs`).

**Tests:** added `render_single_segment_horizontal` (8-wide canvas with one wire at y=0 spanning x=2..6 → output `"  ───── \n"`); `render_corner_glyphs: all four corner cases` (parameterized 4-case test for `╮`/`╭`/`╯`/`╰` selection based on segment direction; each case builds a 2-segment wire forming the corner and asserts the expected glyph appears in the output via substring match); `render_tap_at_fanout` (3 wires originating from the same source cell (5, 1) → asserts `●` appears in the output); `render_jump_arc_horizontal_over_vertical` (one horizontal wire at y=2 from x=2..8 and one vertical wire at x=5 from y=0..4 with `crossings=[(5,2)]` on both → asserts the substring `╯│╰` appears, locking the jump-arc neighbour rendering). Ran `zig build test`, result pass — 206+ tests across all suites.

**Next slice:** Phase 3 Slice 5 — final cutover. `cmd/circ-compile/main.zig`'s preview branch swaps from `dump(topology)` to `layout → render`. Four new `.circ` fixtures (`single_gate`, `fan_out`, `fan_in`, `multi_led`), 7+ new render goldens, the 3 carried-forward Phase-1 goldens get replaced with rendered output, and a color-on golden for at least one fixture. **Phase 3 will be complete after slice 5 ships.**

**Notes:** The corner-glyph picker uses a 4-direction connection model: each segment endpoint contributes a "connects-to-X" flag for each direction (W, E, N, S). Two flags will be set per corner (one from incoming segment, one from outgoing); the (W∨E) × (N∨S) pairing maps cleanly to the four `╭╮╰╯` glyphs. The `pickCornerGlyph` fallback returns `+` for malformed corners (shouldn't fire with route's L-shapes but defensive). Off-by-one bug caught on first run: I'd written the expected string for `render_single_segment_horizontal` with 2 trailing spaces (9 cells total), but the canvas is only 8 wide — wire 5 cells + 2 leading spaces + 1 trailing space + newline = 9 chars. Fixed the test, kept the canvas. The jump-arc `applyJumpArc` is idempotent in practice: each crossing point appears on **both** wires' crossings lists, so the function gets called twice per crossing point. Each call performs the same three setCell operations, so the final canvas state is identical regardless of call order. The `wireHasHorizontalAt` check ensures we only apply the jump if at least one wire has a horizontal segment passing through the crossing cell — no-op when the crossing is between two non-horizontal wires (which shouldn't happen with route's L-shape geometry, but defensive). Tap detection uses a `std.AutoHashMap(u64, u32)` keyed on `(y << 32) | x` to count source endpoints across all wires; ≥3 → `●`. This catches fan-out at sources but misses fan-out mid-graph — for typical `.circ` routing, sources are the only fan-out points, so adequate for slice 4.

## 2026-05-06 — Phase 3 — Slice 5: CLI cutover + new fixtures + golden updates (Phase 3 complete)

**What shipped:** The user-visible cutover. `cmd/circ-compile/main.zig`'s preview branch now calls `layout_orchestrator.build` then `preview_render.render` — replacing the Phase-1 `preview_dump.dump(topology)` call. The rendered output goes to stdout with optional ANSI color resolved from `args.color`, the real stdout handle (`std.fs.File.stdout().handle`), and `NO_COLOR` env via `std.process.getEnvVarOwned`. Four new fixtures introduced (`single_gate.circ`, `fan_out.circ`, `fan_in.circ`, `multi_led.circ`); 11 new golden files captured via `UPDATE_GOLDENS=1`. The Phase-1 `*.preview.golden` files (`chain.preview.golden`, `builtin_xor.preview.golden`, `builtin_xnor.preview.golden`) had their content **replaced** with rendered schematics — the test names (`phase1_preview_*_fixture`) were retained per the spec's "git history of golden files is the chronological record of how `--preview` evolved" discipline. **Phase 3 is now complete.**

**Files touched:** 4 new fixture `.circ` files under `tests/fixtures/circuits/` (`single_gate`, `fan_out`, `fan_in`, `multi_led`); `cmd/circ-compile/main.zig` (new imports for `layout_orchestrator` + `preview_render`; preview switch arm rewritten; new `runPreviewWithFlags` helper; 8 new tests added); `build.zig` (new `circ_compile_mod` imports for `layout_orchestrator` and `preview_render`); 7 new render goldens + 4 *replaced* preview goldens for the existing fixtures.

**Tests:** added `phase3_render_single_gate`, `phase3_render_fan_out`, `phase3_render_fan_in`, `phase3_render_multi_led` (each runs preview on its fixture, asserts exit code 0, golden-matches the captured `*.render.golden`); `phase3_render_builtin_xor_expanded`, `phase3_render_builtin_xnor_expanded` (same shape, with `--expand-macros` flag); `phase3_render_color_always` (single_gate with `--color=always`, asserts ANSI escapes appear in stdout, golden-matches `single_gate.render.color.golden`); `phase3_render_color_never_no_escapes` (single_gate with `--color=never`, asserts zero ESC bytes in stdout). The existing `phase1_preview_*_fixture` tests pass against their updated goldens (now containing rendered schematics, not topology dumps). Ran `zig build test`, result pass — 214+ tests across all suites.

**Phase 3 done. Phases 0, 1, 2, 3 ALL complete.** `circ-compile <file> --preview` now produces ASCII-rendered circuit schematics with optional ANSI color and opaque-vs-expanded subcircuit modes.

**Notes:** The cutover was clean — the existing Phase-1 integration tests' goldens automatically picked up the rendered output via `UPDATE_GOLDENS=1`, and the substring/structural assertions in `phase1_preview_parse_error_to_stderr` are independent of dump format so they survived unchanged. The rendered output for fan_in.circ shows visible primitives: `a` and `b` input pins on the left, AND gate in the middle (its D-shape glyph), wire connections with corners and a tap point, and `o` output pin on the right. `builtin_xor.circ` in opaque mode renders the entire xor as a single labeled `[xor:g]` box — Phase 2's collapse + Phase 3's macro-box glyph composing exactly as designed. The `+` fallback glyph from `pickCornerGlyph` does fire occasionally in practice (visible in fan_in's golden) — possibly at cells where the corner involves three wire connections and the strict 2-direction logic doesn't handle the 3-way junction cleanly. Worth flagging as a future cleanup; for slice 5 the goldens lock the current behaviour and any improvement to corner detection will produce a reviewable diff. The `runPreviewWithFlags` helper takes `extra_flags: []const []const u8` so tests can pass any combination of `--expand-macros`, `--color=...` after the `--preview` mandatory flag. The `NO_COLOR` env read happens at every preview invocation; in tests running under non-TTY conditions, `auto` mode resolves to no-color, so the default-mode tests (without `--color=always`) produce escape-free output that golden-matches deterministically.
